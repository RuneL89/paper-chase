{
  "phase": "06",
  "status": "reopened-amendment-shipped",
  "goal": "All 8 technical gates in PHASE_06_dox_writer.md pass (npm test is green), all 3 UAT steps are documented with expected output, compliance log shows no unresolved contradictions, the status file shows all gates passed and no blockers, every folder has an LLM-enriched index.md with deterministic children/statistics, and total LLM cost is logged.",
  "lastAction": "2026-07-21 per-wiki workspace segments amendment shipped: writeWorkspaceIndex reworked (per-wiki prose segments + catalog lines, byte-for-byte preservation, per-wiki fallback, removal drops segments); gates 6.9-6.11 reworked for segment semantics; new gates 6.12-6.14 green; full suite 215 passed + 1 skipped",
  "gatesPassed": [
    "6.1",
    "6.2",
    "6.3",
    "6.4",
    "6.5",
    "6.6",
    "6.7",
    "6.8",
    "6.9",
    "6.10",
    "6.11",
    "6.12",
    "6.13",
    "6.14"
  ],
  "gatesFailed": [],
  "gatesPending": [],
  "llmCost": "$0.00 for automated gates (LLM-free; the 5 new workspace tests use injected stubs). Live coca-cola-wiki re-ingests: $0.2979 all-time DOX spend on that wiki. The workspace pass adds 1 LLM call per ingest in production (doxLlm: true). Incidental live Phase 2 gate spend on the 2026-07-20 full-suite run: ~$0.07, charged to the Phase 2 budget.",
  "tokenBudgetRemaining": "Quality-first; no hard cap (phase doc §9). Every call logged.",
  "blocker": null,
  "deviations": [
    "Gates 6.7 and 6.8 are verified LLM-free: gate 6.7 ingests via the injected extractChunkFn stub, gate 6.8 and the fallback test inject writeDoxIndexFn stubs (tests/AGENTS.md contract forbids live LLM calls by default).",
    "doxLlm defaults to false in the library (writeDoxContracts/IngestOptions) so the deterministic path stays byte-identical and all pre-existing tests pass unchanged; production callers opt in (CLI ingest defaults to LLM-driven with a --no-dox-llm negated-flag opt-out; the TUI ingest screen passes doxLlm: true) per the pre-check decision that production runs are LLM-driven.",
    "For the wiki-root prompt, the LLM sees the freshly-written top-folder index pages (folders are processed post-order in doxLlm mode) instead of every page in the wiki, keeping the prompt bounded per the phase doc §3.1 input list.",
    "DOX Writer LLM calls use maxTokens 2048 (implementer's documented choice; index pages are small contracts), callType 'dox-writer', context = folder path or '(root)', logPath defaults to <wiki>/.state/llm-calls.json.",
    "Unparseable LLM output or output missing required sections falls back to the deterministic body for that folder with a console warning (the phase doc mandates preserving deterministic children/statistics but leaves malformed-output handling to implementation).",
    "Ingest pipeline order corrected to the phase doc §3.5 order: the pre-existing code validated before synthesis; it now runs synthesis -> first validation -> DOX Writer -> final validation. The persisted validation-report.json reflects the final pass.",
    "The prompt uses one extra {navigation} placeholder (pre-computed parent/sibling titles) beyond the phase doc §3.2 bullets so the LLM's ## Navigation section uses real folder titles.",
    "Stale contract lines fixed during the DOX pass: src/AGENTS.md 'Extractor is the ONLY LLM call' (already stale since Phase 5) reworded to list Extractor/Synthesis/DOX Writer calls; tests/AGENTS.md verification counts updated to 166 passed + 1 skipped with key (154 + 13 self-skipped without).",
    "Verifier-discovered (pre-existing, non-blocking): the phase doc's literal gate 6.5 snippet counts entities/**/*.md (which would include index.md files), while the implementation and its pre-landed test count content pages excluding index.md; this matches vision 03 §4.4's example ('Pages: 2' in a folder whose children list includes index.md), so it is a spec-snippet imprecision, not a defect. Also pre-existing: writeFolderIndex skips empty folders, so an empty top folder (e.g. zero topics) would lack an index.md while the root children hardcode all four; untested by any gate fixture.",
    "2026-07-19 user-directed refinement (pre-check logged in .state/compliance-log.md [2026-07-19 01:05], COMPLIANT as two planned EXTENSIONS): bottom-up child-index synthesis — every folder with child folders (wiki root included) now receives `childIndexes` in its DoxIndexContext: the freshly-written full markdown of each direct child folder's index.md from the same run. Folders were already processed deepest-first post-order; confirmed for all levels, no order change needed. The root's top-folder indexes moved from the root-only pages/pageContents special case into the uniform childIndexes mechanism (root `pages` is now empty unless the root has direct content pages; the root's AGENTS.md is excluded from pages, mirroring the link checker).",
    "2026-07-19 refinement: zero-page-parent catalog rule — the `## Pages` catalog (deterministic body AND prompt instructions) now lists each child folder as `- [[Folder Title]]` (resolved by the link checker's folder-index fallback) in addition to direct pages, and 'No pages in this folder.' is only emitted when a folder has neither pages nor sub-folders. This deliberately changes the deterministic (doxLlm: false) output for folders that have sub-folders but zero direct pages (e.g. entities/people/); the change is the sanctioned fix for the live topics/index.md fallback and no existing test asserted the old line. Required-sections validation (# + ## Pages + ## Navigation + ## Statistics for folders) is unchanged; the prompt now makes a well-formed response natural for zero-page parents, with the deterministic body kept as the fallback safety net.",
    "2026-07-19 refinement: deterministic wikilink safeguard — the prompt now carries an EXACT LINK TARGETS section (per page: deterministic linkText rule, title when slugify(title) === fileSlug else the file slug; per child folder: folder title; parent/sibling folder titles), and the enforced LLM body is post-processed before writing: every [[...]] is tested against a wiki-wide link index, unresolvable links that uniquely match (case-insensitive) a page title are REWRITTEN to that page's exact resolvable link text (chosen over de-linking because the target is unambiguous and the link is valuable), [[target|alias]] is tested as a whole first (the link checker's behavior) then retried on the pre-| target, and anything still unresolvable is DE-LINKED (brackets stripped, display text kept — chosen over guessing because targets are never invented). One summary console.warn per folder at most, only when repairs occurred. Acceptance bar: zero broken links from index.md pages in the final validation (covered by a test asserting checkLinks reports none).",
    "2026-07-19 refinement: link-resolution duplication — buildWikiLinkIndex/repairWikilinks in src/dox-writer.ts MIRROR the resolution semantics of src/validation/link-checker.ts (slug map from slugified file basenames, folder-index fallback, wiki-root fallback, root AGENTS.md exclusion, hasContent gating matching writeFolderIndex's skip rule). The link checker's map-building is internal and disk-driven so it could not be reused directly; the mirrored logic operates on the already-scanned folder tree. If link-checker resolution semantics ever change, the mirror must be updated in step.",
    "2026-07-19 refinement: tests — 3 LLM-free tests added to tests/phase-06.test.ts (bottom-up child-index synthesis with order assertions, zero-page-parent synthesis without fallback, wikilink repair + de-link with a final checkLinks assertion). The 9 pre-existing deterministic tests are byte-for-byte untouched and green; the 3 pre-existing LLM-mode tests needed no changes (the context change is purely additive). Suite counts are now 157 passed + 13 self-skipped without a key and 169 passed + 1 skipped with the repo .env key (verified 2026-07-19; the only spend remains the incidental live Phase 2 gates).",
    "2026-07-19 Verifier-discovered (non-blocking, no code change sanctioned): (1) pipe-link repair rewrites [[target|alias]] to [[target]], dropping the alias display text — checker-consistent, cosmetic only; (2) the dox-writer link-index mirror excludes raw/ (per the vision 03 §6 DOX scan rule) while the link checker scans any .md outside .state/ — a stray .md in raw/ would resolve for the checker but be repaired/de-linked by the mirror; conservative failure direction, no realistic fixture hits it; the mirror-maintenance note above now covers this asymmetry.",
    "2026-07-19 UAT 6.3 aliases fix (user decision Option A, compliance-log entry [2026-07-19 02:30]): the shared helper lives in the NEW file `src/utils/aliases.ts` (`aliasesForTitle` + `enforceAliasesInMarkdown`) rather than extending `utils/slug.ts`, because the rule is a frontmatter concern (gray-matter re-serialization) and not a slug derivation; every page writer imports it — `pages/entity-page.ts`, `pages/topic-page.ts`, `pages/source-page.ts`, the inline document-page writer in `commands/ingest.ts` (always a no-op there: document title == chunk-id basename), and both DOX Writer frontmatter paths in `dox-writer.ts` (deterministic and LLM-enforced; one code change covers both because the LLM path re-imposes the deterministic frontmatter). Schema validator needed no change: it only checks required fields (title/type/updated), so the additive optional `aliases` field is accepted. The link checker is untouched (its semantics are unchanged).",
    "2026-07-19 UAT 6.3 aliases fix: synthesis enforcement is DETERMINISTIC AT WRITE TIME, not prompt-only — `ingest.ts` wraps all four synthesized-page write sites (entity/topic × strict/permissive) with `enforceAliasesInMarkdown(markdown, title, slug)`, which parses the model-written frontmatter, sets or deletes `aliases` per the rule, and re-serializes with gray-matter. Prompt-level coverage comes from the wiki constitution: one line added to `templates/AGENTS.md` §Page Frontmatter documenting the optional `aliases` convention (the constitution is appended to every synthesis prompt); the four `prompts/synthesis*.prompt.txt` files are unchanged (they never mentioned frontmatter — frontmatter instruction flows via the constitution), so `prompts/AGENTS.md` needed no update. Pages whose LLM output has NO frontmatter block are returned unchanged by enforcement (never inventing a block) — the pre-existing schema-invalid-pages problem stays flagged for the Phase 7/8 maintenance loop per the compliance-log entry. The structured-template fallback path needs no enforcement: materializer-written pages already carry the correct aliases.",
    "2026-07-19 UAT 6.3 aliases fix: alias values use the RAW page title (js-yaml escapes it, e.g. `aliases: ['Source: X.pdf']` round-trips cleanly). Noted pre-existing quirk NOT changed by this fix: `entity-page.ts` pre-quotes its `title:` value with `escapeYamlString`, which double-quotes YAML-sensitive titles on parse (title would read back as '\"Source: X.pdf\"' with literal quotes); out of scope for the sanctioned aliases fix, flagged for a future cleanup.",
    "2026-07-19 UAT 6.3 aliases fix: tests — 13 LLM-free tests added, ZERO existing tests modified (no existing test asserted exact frontmatter shape): 10 in `tests/phase-03.test.ts` (aliasesForTitle match/case-insensitive/mismatch/special-chars, enforceAliasesInMarkdown add/remove/preserve, entity/topic/source writer rule, ingest document pages carry no aliases), 1 in `tests/phase-05.test.ts` (synthesized entity page gains the alias, synthesized topic page with case-insensitive title/slug match gains none), 2 in `tests/phase-06.test.ts` (end-to-end materialize + deterministic DOX contracts: folder/root index aliases plus content-page rule; LLM-mode re-imposition over hallucinated stub frontmatter). Suite counts: 170 passed + 13 self-skipped without a key; with the repo .env key, 174 passed + 8 Phase-2 live-gate network failures (ENOTFOUND/ECONNRESET to api.anthropic.com — sandbox has no network) + 1 skipped in this environment, i.e. 182 passed + 1 skipped where the API is reachable. `npx tsc --noEmit` clean. Existing wikis under `wikis/` were NOT touched; the coca-cola-wiki backfill is a separate orchestrator-run deterministic step.",
    "2026-07-20 user-directed change (UAT 6.3 re-failure; compliance-log entry [2026-07-20 00:15], 'research how obsidian creates these references and make sure this is how the links are created'): all CLI-written wikilinks now use Obsidian's native target-plus-display pipe form. The shared helper lives in the NEW file `src/utils/wikilinks.ts` (`formatWikilink(target, display?)` — pipe form, bare only when display is absent or EXACTLY equals the target, case-sensitive equality, both parts trimmed; `parseWikilinkTarget(inner)` — splits on the FIRST `|` so display text may contain pipes; targets in our wikis never do). Placed next to `utils/aliases.ts` because it is a pure string concern with no gray-matter dependency; aliases (Option A) are KEPT unchanged as frontmatter search/suggester aids per the pre-check.",
    "2026-07-20 pipe-form change: link checker semantics (`src/validation/link-checker.ts`) — the part before the first `|` is the resolution target; it is matched by EXACT vault-relative path (`.md` suffix stripped and tolerated, case-sensitive, first-wins on duplicates) FIRST, then by the existing slugified-basename map (case-insensitive via slugify) with the folder-index and wiki-root fallbacks untouched. Every legacy bare form (`[[Page Title]]`, `[[slug]]`, `[[Folder Name]]`, `[[Wiki Title]]`) stays resolvable so older wikis still validate; broken-link reporting still records the full inner link text; orphan detection unchanged. The DOX Writer's buildWikiLinkIndex mirror was rewritten in step (byPath + bySlug + titleToFiles, registering the to-be-written index pages up front as before); the mirror-maintenance note from the 2026-07-19 deviation still applies.",
    "2026-07-20 pipe-form change: normalization choice (repairWikilinks) — pipe-form links with resolvable targets are kept VERBATIM (the LLM's display text is its own choice; this also fixes the Verifier-noted cosmetic wart where `[[target|alias]]` was rewritten to `[[target]]`). Resolvable-but-BARE legacy links from the LLM ARE normalized to the canonical pipe form of the exact target the checker resolves them to (unambiguous: same resolution order, path map then slug map first-wins) — chosen for uniformity with the deterministic catalogs and because resolution is provably identical before and after. Unresolvable links with a unique case-insensitive page-title match are rewritten to that page's canonical pipe form (e.g. `[[Source: Golden Master]]` -> `[[golden-master|Source: Golden Master]]`); anything else is de-linked (display text kept). Targets are never invented. Gate 6.7's acceptance bar is unchanged: zero broken links on index pages, now under pipe-aware checker semantics.",
    "2026-07-20 pipe-form change: deterministic writers — entity/topic page writers render relationship/claim entity links as `[[slug|Title]]` (bare `[[slug]]` fallback for unknown slugs kept; bare when title == slug exactly). DOX deterministic catalogs: page entries `[[<basename>|<Title>]]` (the em-dash title suffix was dropped — the pipe form already shows the title), child-folder entries `[[<folder-path>/index|<Title>]]`, Parent/Sibling links in the same folder form, root parent `[[index|<Wiki Title>]]`, root Start Here `[[entities/index|Entities]]` etc. Source-page document links (`[[<doc-slug>]]`) needed no change: display equals target. DoxIndexContext gained additive `parentLinkText`/`siblingLinkTexts` fields; `linkText` fields now hold the full bracketed link form.",
    "2026-07-20 pipe-form change: prompt/template edits — `prompts/dox-writer.prompt.txt` (EXACT LINK TARGETS section explains the pipe form and basename-vs-path rule; rules line now requires the supplied pipe forms verbatim and forbids rewriting to bare `[[Page Title]]`; all three examples rewritten to pipe form); the four synthesis prompts (`synthesis`, `synthesis-permissive`, `synthesis-topic`, `synthesis-topic-permissive`) now instruct `[[<entity-slug>|<Page Title>]]` using the slugs present in the injected data; `templates/AGENTS.md` constitution wikilink rule updated to the pipe form with examples and the aliases line reframed as search/suggester aids ({{WIKI_TITLE}}/{{SLUG}} placeholders untouched; citation rules untouched). Existing wikis under `wikis/` were NOT touched; the coca-cola-wiki backfill and the vision-example updates are orchestrator-run steps.",
    "2026-07-20 pipe-form change: test adjustments — MODIFIED (old form was the assertion target): phase-03 gates 3.2b/3.6b now assert `[[acme-corp|Acme Corp]]` and the absence of both `[[Acme Corp]]` and `[[acme-corp]]`; phase-06 repair test expects the canonical pipe form `[[golden-master|Source: Golden Master]]` on unique title match (was `[[golden-master]]`) plus `[[index|Test Wiki]]` normalization of the resolvable-bare parent link; phase-06 zero-page-parent stub and richStubIndex emit pipe forms (preserving the no-warnings assertion); phase-05 `buildCompletePage` fixture renders `[[acme-corp|Acme Corp]]` (fixture-only — the preservation check compares data strings, not link renderings). ADDED (11 LLM-free tests): 6 helper unit tests in phase-03 (pipe/bare/first-pipe split/display with apostrophes+colons/trimming/empty display), 4 pipe-aware checker tests in phase-04 (pre-pipe basename + alias-display resolution, exact-path folder-index/root/`.md`-suffix resolution, legacy bare forms still resolve, broken pipe target reported), 1 deterministic pipe-form catalog/navigation test in phase-06. Counts: targeted suites phase-03 27 + phase-04 17 + phase-05 17 + phase-06 18 = 79 passed; full suite 181 passed + 13 self-skipped without a key and 193 passed + 1 skipped with the repo .env key (network reachable this run; the only spend was the incidental live Phase 2 gates, charged to the Phase 2 budget). `npx tsc --noEmit` clean. Zero live LLM calls in the new/updated tests.",
    "2026-07-20 Verifier findings (independent, non-blocking, no code change sanctioned): (1) leftover bare-title wikilink examples in Project Vision/ outside the orchestrator's assigned edit set — 01 lines 60+159, 02 line 129, 05 lines 239-240 (concrete stale examples), plus 04 lines 155/234 and 07 line 68 using [[Page Title]]/[[Page Name]] as generic rule notation (rules still hold — legacy bare forms remain resolvable); follow-up doc pass recommended. (2) Marginal mirror/checker divergences on degenerate inputs: the mirror maps bare slug 'index' to the ROOT index while the checker's first-wins walk order maps it to the alphabetically-first folder index (path-form [[index|...]] unaffected — byPath first in both), and the mirror registers a folder's name-slug before its files while the checker registers all file slugs before folder-index fallbacks (matters only on file/folder basename collisions). (3) Cosmetic helper bypasses: DOX root Start Here links are hardcoded pipe-form strings (character-identical to folderIndexLink output) and source-page.ts emits [[pageSlug]] literally instead of formatWikilink(pageSlug) (output identical — display == target). Verdict APPROVED; full evidence in .state/phase-6-verification.md [2026-07-20].",
    "2026-07-20 user-ratified vision amendment (compliance-log [2026-07-20 02:30]): the workspace-level index-of-indexes.md — specced as deterministic in vision 03 §4.1 / 05 §3.3 but NEVER implemented (zero references in src/, tests/, prompts/, phase docs) — is now a DOX Writer output written like any other parent index: same bottom-up post-order flow, LLM prose synthesized ONLY from the freshly-written wiki root index.md contracts (never the wikis' content pages), deterministic children/statistics re-imposition, deterministic fallback on LLM failure. User decisions: (A) location wikis/index-of-indexes.md (wikis/ openable as one Obsidian mega-vault); (B) regenerated at the end of every ingest, init writes nothing until first ingest; (C) implemented under reopened Phase 6 (doc v1.1.0, gates 6.9-6.11, UAT 6.4); (D) prose in the output language of the triggering ingest (outputLanguage option, English default until Phase 7 lands per-wiki language config). Validation scope accepted trade-off: the workspace index lives outside every single-wiki vault, so validateWiki() does not check it; its [[<slug>/index|Title]] links resolve when wikis/ is opened as the vault.",
    "2026-07-20 amendment implementation notes: (1) enforceLlmBody/hasRequiredSections generalized from boolean isRoot to a three-value DoxIndexLevel ('folder'|'root'|'workspace') — behavior-preserving for folder/root (Verifier-confirmed); workspace requires '# heading + ## Wikis + ## Statistics'. (2) The workspace pass scans each wiki tree for corpus-total statistics (same semantics as wiki-root stats). (3) Wiki root title read from the root index frontmatter (title-cased slug fallback). (4) writeWorkspaceIndex is a no-op when no wiki has a root index.md (init alone writes nothing — ratified decision B). (5) TUI: TreeBrowser gained an optional workspaceFile picker entry; viewer Escape returns to the picker when no wiki is active. (6) Verifier non-blocking findings recorded: pre-existing Layer 5/6 naming mismatch between phase doc title and vision docs; cosmetic TUI viewer footer label; degenerate zero-PDF early return skips regeneration; pre-existing conservative regex asymmetry in statistics-section enforcement. None block acceptance; flagged for Phase 7/10 cleanup."
  ],
  "nextAction": "Start Phase 7: multilingual ingestion (PHASE_07_multilingual_ingestion.md; outputLanguage hook for the workspace pass already in place).",
  "perWikiSegmentsAmendment": {
    "ratified": "2026-07-21 (user directive; compliance-log [2026-07-21 00:10] + refinement [2026-07-21 00:20]: cross-wiki LLM prose KEPT, per-wiki owned)",
    "vision": [
      "03 §6 amended",
      "04 Step 10 item 7 amended"
    ],
    "phaseDoc": "PHASE_06 v1.2.0 (gates 6.12-6.14 + checklist)",
    "migration": "workspace index cleared and rebuilt 2026-07-21: one LLM entry call per wiki in its own output language (4 calls, $0.0057); each wiki now has a marked per-wiki segment"
  }
}
