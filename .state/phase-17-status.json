{
  "phase": "17",
  "status": "completed",
  "goal": "All 13 technical gates in PHASE_17_entity_graph_and_citation_integrity.md pass (npm test is green), all 3 UAT steps are documented with expected output, compliance log shows no unresolved contradictions, status file shows all gates passed and no blockers, and total LLM cost is $0 (all gate tests are LLM-free).",
  "lastAction": "Phase 17 implemented 2026-07-28, exactly §2.1-§2.6, all 13 gates encoded in tests/phase-17.test.ts (14 tests: gates 17.1-17.12 + two supplementary tests, all LLM-free, $0) and green; npx tsc --noEmit clean; key-less full suite 350 passed + 14 skipped across 23 files (22 passed, 1 skipped) = the post-navigation-fix baseline of 336+14/22 plus the 14 new tests, zero unenumerated regressions; .env stashed for every suite run and restored byte-identical (171 bytes, original mtime 2026-07-20 00:13 — final runs used mv/mv so mtime is preserved exactly). FILES: src/pages/entity-page.ts (new EntityPageIncomingRelationship + EntityPageData.incomingRelationships?; writeEntityPage renders incoming lines after outgoing in ## Relationships exactly as `- [[subject|Title]] — Predicate (incoming) — \"evidence\" [^srcN]` with the evidence verbatim; buildCitationMap and the frontmatter sources aggregation cover incoming records AFTER mentions/outgoing/claims so no-incoming pages keep pre-Phase-17 key numbering; isSparseEntity untouched (outgoing-only, ratified 2026-07-28); the frontmatter build factored into one shared buildEntityFrontmatter/buildEntityFrontmatterSources helper used by BOTH the writer and the new enforcer so the enforced block deep-equals the writer's own; NEW enforceFrontmatterInMarkdown(markdown, pageData) — complete deterministic frontmatter (title, type, aliases via combinedAliases, sparse, wiki, REAL-write-time updated = B2's fix, full aggregated sources, tags) written OVER the model's block, CREATED when absent, body byte-preserved, unparseable model frontmatter returned unchanged; NEW enforceSourcesSectionInMarkdown(markdown, citationMap) — ## Sources definitions rebuilt basename+page-range form, section replaced up to the next ## heading or appended when missing, in-prose markers byte-identical, empty map returns the page unchanged). src/pages/topic-page.ts (same refactor into buildTopicFrontmatter/buildTopicFrontmatterSources; NEW enforceTopicFrontmatterInMarkdown — never carries sparse; NEW enforceTopicSourcesSectionInMarkdown delegating to the entity implementation; no import cycle — entity-page imports no page modules). src/materializer.ts (MaterializedEntity.incomingRelationships; mirror-attach loop after the subject-attach loop — same skip-on-unknown guard, self-loops skipped so they attach ONCE as outgoing; both aggregate creation sites + the curation synthesized-aggregate site carry the field; entity curation merge unions incoming records and the slug-remap pass rewrites incoming subjects; page assembly dedupes incoming (same content-key rule as outgoing) and drops any incoming record a curation merge collapsed into a same-page self-loop so the loop renders once; incomingRelationships set on pageData ONLY when non-empty — undefined drops out of canonicalJson so no-incoming pages keep their pre-Phase-17 aggregate fingerprint and stay skip-eligible/byte-stable under Phase 16). src/agents/synthesis.ts (formatRelationships(relationships, incomingRelationships?) — direction-labeled subsections `Outgoing (this entity is the SUBJECT of these relationships):` / `Incoming (this entity is the OBJECT of these relationships):`, byte-identical to pre-Phase-17 when no incoming records; NEW exported buildRelatedEntities — deduplicated, sorted {slug,title} union of relationship subjects/objects both directions + claim co-entities minus the page itself, titles via slugToTitle with slug fallback — and formatRelatedEntities (`- <slug> — <Title>` lines, documented empty form `(none)`); buildEntitySynthesisValues gains the relatedEntities slot). src/validation/preservation-check.ts (incoming evidence required exactly like outgoing, named in droppedRelationships — data-driven, no structural change). src/validation/link-checker.ts (outgoing wikilink tally per page in the existing parse loop — every [[...]] occurrence counts, resolvable or not; LinkCheckResult.islands: entities//topics/ pages per wikiRelative with zero outgoing links, index.md exempt, orphaned unchanged). src/validation/citation-checker.ts (§2.6 report-only missingFrontmatterSource — every distinct in-prose [^srcN] key (definition lines excluded via (?!:) lookahead) on entity/topic pages must be covered by the page's frontmatter sources, matched by source-file basename between the definition and the frontmatter file entries; absent/unparseable frontmatter covers nothing). src/validation/index.ts (link summary gains the islands count + per-island warnings same posture as orphans; citation summary/warnings count missingFrontmatterSource alongside invalid+missingSource). src/commands/ingest.ts (the four synthesis write points — strict/permissive entity ~1032/1069, strict/permissive topic ~1219/1248 — compose aliases → sparse → enforceFrontmatterInMarkdown/enforceTopicFrontmatterInMarkdown → enforceSourcesSectionInMarkdown/enforceTopicSourcesSectionInMarkdown with buildCitationMap-derived maps; enforcement runs ONLY at these write points so skip-eligible pages are never touched — gate 17.12 proves byte-stability across a re-materialize + re-synthesize-skip). prompts/synthesis.prompt.txt + prompts/synthesis-permissive.prompt.txt (SLOT-ADDITIVE ONLY: the {relatedEntities} slot after `Chunk Context:`, and the wikilink rule updated to require targets from the Related Entities list with first-mention linking; every pre-existing section byte-identical — gate 17.6 hash-pins byte-confinement: slot removed + old rule restored reproduces the pre-Phase-17 template's SHA-256, verified byte-equal to git HEAD at implementation time; the topic prompts are deliberately untouched — §2.2 scope is the two entity prompts). scripts/launcher-entry.ts (asset VERSION 1.0.3 → 1.0.4 per the root-AGENTS.md dist/ rule — the two changed prompts are packaged assets; the bump comment documents the reason; root AGENTS.md's dist/ entry records the bump). DESIGN DECISIONS A VERIFIER SHOULD KNOW: (a) incomingRelationships is omitted (undefined) when empty so canonicalJson drops it — a page with no incoming edges keeps its pre-Phase-17 pageDataHash and stays skip-eligible (Phase 16 byte-stability contract); pages that genuinely gain incoming edges change fingerprint and re-synthesize, which is correct (new on-page data). (b) The merge-collapse filter: outgoing records on a page always have subject === page slug (attach + remap invariant), so an incoming record duplicating an outgoing self-loop on (subject, predicate, page, source, pages) arises only when a curation merge collapsed both endpoints into the same entity; dropping the incoming copy keeps the self-loop rendered once as outgoing (gate 14.6's Odense merge exercises this path and stays green). (c) §2.1's 'Layer 1 guidance notes the entity may be the object' is realized DATA-SIDE in formatRelationships' direction labels, not as prompt text — the master prompt's PROMPT DISCIPLINE (slot-additive only; pre-existing sections byte-identical) and gate 17.6 confine prompt edits to the §2.2 slot + wikilink rule. (d) enforceFrontmatterInMarkdown includes `tags` (same helper as the writer) so the enforced block deep-equals writer output (gate 17.8 asserts exactly that); the model's extra frontmatter fields are dropped. (e) Frontmatter `sources` keep workspace-relative `file` paths (vision `06` §3) — only the `## Sources` DEFINITIONS go basename-form (the form the citation checker resolves, `06` §7); the gates' no-full-path assertions are scoped to the Sources section accordingly. (f) §2.6 coverage is file-granular (definition basename ∈ frontmatter file basenames): frontmatter sources join page ranges per file, so per-key page-range matching is not expressible; report-only, same posture as missingSource. (g) The deterministic definition rebuild enforces buildCitationMap's srcN→source assignment; the model is NOT taught that assignment (the preservation check pins the key SET, not the mapping) — a live model whose in-prose assignment differs would get correct, resolvable definitions under OUR numbering; aligning the model's assignment is a prompt-level follow-up, out of scope for this phase (noted for the Verifier, not a deviation — the phase doc specifies the deterministic rebuild). (h) The islands tally counts every wikilink occurrence (resolvable or not) as outgoing — a page with only broken links still attempts linkage; matches the gate-17.7 fixture semantics. (i) Gate 17.6's byte-confinement is pinned by SHA-256 of the pre-Phase-17 templates (44d3e0c4… / d5045acc…), recorded in the test with recompute instructions. PRE-EXISTING TESTS TOUCHED (every one, with reason — nothing else): (1) tests/phase-04.test.ts — three links literals + three citations literals gain the additive required fields islands/missingFrontmatterSource (type-level necessity; assertion intents unchanged). (2) tests/phase-12.test.ts gate 12.4 — the non-john-smith entity stub page emits incoming evidence (acme-corp is the fixture relationship's object; preservation now requires incoming evidence — fixture-level, gate intent unchanged). (3) tests/phase-14.test.ts entityStubPage — same reason (the Odense fixture's cross-relationships attach incoming records to odense/odense-2). (4) tests/phase-13.test.ts gate 13.3 — the WIKILINK_LINE constant is split per prompt kind: entity prompts carry the Phase 17 rule (the sanctioned §2.2 change), topic prompts the pre-Phase-17 rule byte-identically (scope is the two entity prompts only). (5) prompts/dox-writer.prompt.txt — PRE-EXISTING BASELINE REPAIR, not a behavior change: the uncommitted 2026-07-25 navigation-fix edit had been saved with CRLF line endings (every other prompt file is LF), failing baseline gates 7.2b and 13.3-DOX key-less BEFORE any Phase 17 change (verified on the untouched working tree twice: 334 passed + 2 failed / 22 files); converted CRLF→LF with content byte-preserved (the navigation-fix diff is unchanged — still 9 insertions/1 deletion vs HEAD), restoring the documented 336+14 baseline. DEVIATIONS: none from the phase doc or canon; no blockers. DOX pass: src/AGENTS.md (entity-page/topic-page/materializer/synthesis/preservation-check/link-checker/citation-checker/validation-index/ingest bullets updated with the Phase 17 behavior), prompts/AGENTS.md (both entity-synthesis bullets carry the slot + updated rule; Verification gains the gate-17.6 entry), tests/AGENTS.md (phase-17 ownership entry; Verification updated to 350+14/23 with the enumerated touches + the CRLF repair), Implementation Plan/BACKLOG.md (B1/B2/B10/B12 marked FIXED 2026-07-28 with the phase + gate references). wikis/AGENTS.md intentionally unchanged (it documents .state files and workspace structure — no new .state file or structural change this phase; the validation-report fields are additive and owned by src/AGENTS.md's validation bullets). templates/AGENTS.md, test-pdfs/, dist/, DOX Writer, workspace pass, curation, and extractor surfaces untouched per the invariants. Compliance log: pre-check [2026-07-28 16:21] COMPLIANT with the §2.1 sparse-scope design decision user-ratified [16:35]; no unresolved contradictions. LLM cost $0.00. || VERIFIER (2026-07-28, independent cold check): PHASE 17 VERIFIED — all 13 gates PASS with personally checked evidence (tests genuinely pin their gates; implementation satisfies them); prompt byte-confinement recomputed independently (slot-removal + rule-restore byte-equals git HEAD, SHA-256 44d3e0c4…/d5045acc… — the recorded pins); Verifier's own runs: npx tsc --noEmit clean, key-less npx vitest run 350 passed + 14 skipped across 23 files (22 passed, 1 skipped), exit 0, run twice, .env stashed and restored byte-identical (171 bytes, mtime 2026-07-20 00:13); diff audit clean (Phase 17 set vs the pre-existing 2026-07-25 nav-fix set correctly separated; dist/, templates/, test-pdfs/, wikis/, extractor/curation/DOX-Writer/workspace surfaces untouched); the dox-writer.prompt.txt CRLF repair's end state and failure mechanism verified (7.2b/13.3-DOX assert LF-literal blocks). Two non-blocking observations recorded in .state/phase-17-verification.md: (1) §2.1's Layer-1 guidance realized data-side via formatRelationships direction labels, not prompt prose (no gate requires prose); (2) design note (g) confirmed — in-prose srcN markers stay model-placed while definitions rebuild under buildCitationMap's numbering; a divergent live-model numbering would mis-map silently (prompt-level alignment is a follow-up, out of scope as the phase doc is written). Full evidence: .state/phase-17-verification.md.",
  "reporterNote": "REPORTER (2026-07-28): UAT 17.1-17.3 presented to the user in the §6 box format. UAT 17.1/17.2 are live re-ingest steps (moderate cost, user's discretion — pair naturally with the planned rkkp-afdk/rkkp-akdb re-ingest after the routing fix). UAT 17.3 pre-computed evidence: dist/wikis/rkkp-adhd (283 files) has ZERO files modified on/after 2026-07-28 (newest mtime 2026-07-25 17:59:33); adhd-foreningen.md still shows the pre-fix form (no frontmatter, '## Relationships (none)'); zero files anywhere under dist/ modified after 2026-07-28 16:00 (the only same-day dist activity was the user's own routing-fix ingests of rkkp-afdk/rkkp-akdb, ending 12:00:32, hours before the phase window). Verdict summary: PHASE 17 VERIFIED (independent Verifier) — 13/13 gates PASSED, LLM cost $0.00, COMPLIANT (sparse-scope extension user-ratified 2026-07-28); recommendation presented: user accepts the phase per the Golden Rule, either on gates+verification alone (optionally waiving live UAT as for Phase 16) or after running UAT 17.1/17.2 live. Awaiting user decision.",
  "gatesPassed": [
    "17.1",
    "17.2",
    "17.3",
    "17.4",
    "17.5",
    "17.6",
    "17.7",
    "17.8",
    "17.9",
    "17.10",
    "17.11",
    "17.12",
    "17.13"
  ],
  "gatesFailed": [],
  "gatesPending": [],
  "llmCost": "$0.00",
  "tokenBudgetRemaining": "$0.00 (no live LLM calls in implementation)",
  "blocker": null,
  "nextAction": "Verifier verdict 2026-07-28: PHASE 17 VERIFIED (.state/phase-17-verification.md) — all 13 gates independently confirmed, suite green, no blockers. Remaining: UAT 17.1-17.3 (live re-ingest + run-5 observational) at the user's discretion; existing wikis adopt the new shapes on their next aggregate-changing ingest. Optional follow-up (non-blocking, Verifier observation 2): align the entity-synthesis prompts with the deterministic srcN assignment so live in-prose markers cannot diverge from the rebuilt definitions.",
  "uatEvidence": {
    "17.3": "VERIFIED PASS 2026-07-28 by main agent (orchestrator), direct check: dist/wikis/rkkp-adhd 283 files, ZERO modified on/after 2026-07-28 (newest: topics/regional-variation/index.md 2026-07-25T14:59:33Z); adhd-foreningen.md mtime 2026-07-25T13:39:51Z still pre-fix form — no frontmatter, first line \"# ADHD foreningen\", \"## Relationships (none)\", no (incoming) marker. The phase is not retroactive to skip-eligible wikis, as specified.",
    "17.1": "VERIFIED PASS 2026-07-28/29 (live re-ingests, orchestrator-checked): rkkp-afdk — databasen-for-atrieflimren-i-danmark.md renders 36 incoming relationships in the pinned format ([[subject|Title]] — Predicate (incoming) — \"evidence\" [^srcN]); landspatientregisteret.md shows direction-labeled Outgoing/Incoming subsections with Layer-1 wikilinks. rkkp-akdb — 34/92 entity pages render incoming relationships; landspatientregisteret.md shows both directions (provides-data-for outgoing, uses-data-from incoming). Subject pages unchanged in content.",
    "17.2": "VERIFIED PASS on specified criteria 2026-07-29 (both wikis): missingSource: 0, schema.invalid: 0, islands lists present (afdk 40, akdb 42), orphaned: 0, complete frontmatter on all synthesized pages (real updated, sources, aliases, sparse). Findings (surfaced BY the new validators, not Phase 17 defects): invalid=33+19 model-invented [^srcN] markers beyond the deterministic map (B18 class, caught by new §2.6 check), broken=12+6 model slug paraphrases (existing link checker), repair-rate warnings 32%/27% (>25% threshold), 2 central-registry pages on structured-template (B4 density class).",
    "cost": "afdk 2.13 main + 1.44 repair (incl. 46 curation-variance merges → 54 re-syntheses), akdb 9.35 main; total 42.92 vs 5.13 wasted on the mis-routed Haiku runs."
  }
}
