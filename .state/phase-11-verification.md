# Phase 11 Verification Report (v1.4.0 — base gates 11.1–11.9 + user-directed extension gate 11.10)

**Verifier:** independent Verifier sub-agent (cold check; Implementer reports not trusted)
**Date:** 2026-07-22
**Spec:** `Implementation Plan/PHASE_11_polish.md` v1.4.0 (10 gates, 6 UATs)
**LLM spend:** $0.00 — `.env` (which contains a live key) was renamed aside before every `npm test`/`tsc` run and restored immediately after; no live LLM calls were made by the Verifier.

---

## Per-gate verdicts

| Gate | Verdict | Evidence (self-produced) |
|---|---|---|
| 11.1 Routing persists | PASS | `tests/phase-11.test.ts` drives the real Settings screen through keystrokes and asserts `.paper-chase.json` carries `provider:'anthropic'` + full ids (`extractor: claude-haiku-4-5-20251001`, `synthesis: claude-sonnet-4-5-20251001`, `dox: null`). Green in my run (20/20 in file). `src/tui/settings.ts` save always writes the new name. |
| 11.2 Routing applied | PASS | `src/llm/client.ts` `resolveModel`: override → routing by callType (`extractor`; the four synthesis callTypes → synthesis; `dox-writer` → dox; else default; null = default) → routing default → `ANTHROPIC_MODEL` env → `DEFAULT_MODEL`; no-routing path byte-identical to pre-Phase-11. `ingest()` wires `loadSettings` + `setModelRouting` at the top in try/catch (ingest.ts:349-354). callTypes still passed: extractor.ts:325, synthesis.ts:204/232/256/280, dox-writer.ts:858/1255/1273, agents-updater.ts:250. Mocked-undici body test asserts routed model on the wire. |
| 11.3 Menu cleaned | PASS | `src/tui/menu.tsx` MENU_ITEMS = exactly Create New Wiki / Add PDFs / Ingest PDFs / Settings / Exit in order. All nine dev/review screens + dead helpers (tree-browser, use-document-chunks, line-diff) are absent from disk; no imports anywhere in `src/` (tests reference them only in tests/AGENTS.md prose). `Screen` = menu\|init\|add-pdfs\|ingest\|settings\|exit. Gate test asserts removed labels absent. |
| 11.4 Continuous workflow | PASS | `app.tsx` `flowWiki`: init `onCreated` (fired only after successful `init()`, init-screen.tsx:83) → add-pdfs with `initialWiki`; add-pdfs banner `Copied N file(s) to wikis/<slug>/raw/.` then `Start ingesting now? [Y/n]` (only after a successful add — `finishSuccessfulAdd` is unreachable on failure); Y/Enter → ingest with pre-selected wiki; n/Escape → menu. Escape in add mode without initialWiki steps back to the selector. Gate 11.4 test drives the whole flow hermetically via the `workspace` prop. |
| 11.5 README | PASS | `# Paper Chase`, `The paper chase, automated.`, formerly note, naming rules, five `##` sections all present; `chase` command examples only. Spot-checks vs code: (a) strict→permissive→structured-template chain = ingest.ts:689-754 ✓; (b) `.state/` file list incl. `llm-calls.json` provider field = client.ts:328-338,446 ✓; (c) multi-provider routing section matches MODEL_CATALOG ids/labels, price table, resolution order, legacy fallback, no-OPENAI_MODEL scope ✓; (d) OpenAI transport paragraph matches buildOpenAIRequest ✓. One wording nit (N1 below). |
| 11.6 Metrics | PASS | `src/state/metrics.ts` extended additively — every §2.6 field present (chunksProcessed/Skipped/Failed, entitiesNew/Updated, relationshipsExtracted, claimsExtracted + claimsByType, pagesByType, foldersCreated, brokenLinks, orphanedPages, conflictsManualEdit/Preservation, totalTokens, totalCost, wallClockMs); Phase 8 fields (run/newPdfs/newEntities/updatedEntities/conflicts/totalCost) intact. Populated by `buildRunMetrics` at BOTH write points (ingest.ts:958 preliminary, 1033-1034 final) + `Metrics: …` progress line (1035-1040). Gate test uses `extract: false` (LLM-free). |
| 11.7 Branding sweep | PASS (independently re-run) | My own Node sweep (gate-equivalent logic) over src/, tests/, templates/, scripts/, Project Vision/, Implementation Plan/, root AGENTS.md, README.md, package.json, wikis/AGENTS.md, bin/chase.js: **0 offenders**. 26 raw hits pre-exclusion — every one a sanctioned category: Document ID lines (vision 01 `LLM-WIKI-CLI-ARCH-001` + 11 plan-doc `LLM-WIKI-CLI-IMPL-*` ids), "formerly LLM Wiki CLI" notes (vision 01, root AGENTS.md, README), literal `.llm-wiki-cli.json` references (settings.ts, menu.tsx, tests, docs), the README "Naming rules:" line (spec §2.5 requires stating the forbidden forms — documented deviation; the spec self-contradicts without it). No `paperchase`/`PaperChase`/`PaperCase` anywhere else. |
| 11.8 CLI is `chase` | PASS | Test asserts `program.name() === 'chase'`, pkg name `paper-chase`, bin mapping. Runtime: `node bin/chase.js --version` → `2.0.0` (exit 0); `node bin/chase.js --help` → `Usage: chase [options] [command]` + "The paper chase, automated. Turn PDFs into citation-backed markdown wikis." + init/ingest/test commands (exit 0). The spawn-local-tsx deviation is ACCEPTED — see N3. |
| 11.9 Legacy config | PASS (independently re-run) | My own temp-workspace run (tsx): only `.llm-wiki-cli.json` present → `loadSettings` returns its values (`synthesis:true, updateAgents:true`) with `models` defaulted to `{provider:'anthropic', default:haiku, nulls}`; `saveSettings` writes `.paper-chase.json`; legacy file byte-untouched; when both exist the new file wins. `src/tui/settings.ts` confirms the fallback is read-only (no write/delete path for the legacy name). |
| 11.10 Provider extension | PASS (extra-cold) | settings.ts: `provider` field, `MODEL_CATALOG` (anthropic haiku/sonnet/opus; openai gpt-5.6-luna/terra/sol), `DEFAULT_MODEL_FOR_PROVIDER`, `seedModelsForProvider`; legacy tolerance (missing models → defaults; missing provider → anthropic — verified in my 11.9 run). client.ts: anthropic path builds the pre-extension shape only (model/max_tokens/messages[/system][/temperature], x-api-key + anthropic-version); openai path = `https://api.openai.com/v1/chat/completions`, `authorization: Bearer`, `max_completion_tokens`, `max_tokens`/`temperature` never in the body (temperature silently ignored), leading `role:'system'` message, `choices[0].message.content` + prompt/completion tokens parsing; prices {1,6}/{2.5,15}/{5,30}; exact OPENAI_API_KEY missing-key error; retry classes unchanged (shared transport loop); log entry gains `provider`. settings-screen.tsx: Provider row directly above Default Model; switch re-seeds via `seedModelsForProvider` immediately; labels match §2.2 strings verbatim (both providers); non-TTY fallback includes Provider. Tests: all six §3-gate-11.10 items covered LLM-free incl. raw-request assertions (URL/headers/key-ordered byte-identical anthropic body; openai body shape; cost $0.0100 from terra prices; log entry provider). ingest.ts routing application passes the whole `models` block through (provider-agnostic). |

## Independent runs

- `npx tsc --noEmit` — **clean** (exit 0), `.env` renamed aside during the run, restored after.
- `npm test` (`.env` renamed aside; restored immediately after, verified present) — **Test Files 15 passed | 1 skipped (16); Tests 232 passed | 14 skipped (246); exit 0.** `tests/phase-11.test.ts` 20/20 green.
  - Skips (all acceptable): 12 live Phase 2 gates (skipIf no `ANTHROPIC_API_KEY`), 1 Phase 0 gate 0.4 live smoke, 1 `e2e.test.ts` (RUN_E2E-gated). No other skips; zero failures. Matches the Implementers' claimed key-less profile exactly.
- `node bin/chase.js --version` / `--help` — recorded under 11.8.

## Compliance verdict

- **Vision substance unchanged:** brand-only edits in vision docs (verified diff of 01: name swaps + one formerly line; `LLM-WIKI-CLI-ARCH-001` kept; other vision docs carry no IDs, unchanged in kind). All plan-doc `LLM-WIKI-CLI-IMPL-*` IDs intact.
- **`.state/` / `wikis/` touch scope:** `git status --short wikis/ .state/` → only `M .state/compliance-log.md` (+72 lines, purely the two appended Phase 11 entries — the mandatory compliance loop, same as every prior phase), `?? .state/phase-11-status.json`, and `M wikis/AGENTS.md` (single name-reference line `chase init`, a sanctioned living-doc sweep target). Nothing under `wikis/<slug>/` touched. COMPLIANT.
- **Internal vocabulary:** wiki/source/entity/topic/citation/DOX intact; the only "case files/suspects" hit is the phase doc's own prohibition sentence. Forbidden brand forms absent (my sweep).
- **DOX pass:** root AGENTS.md documents `bin/` (with the launcher contract), the 2026-07-22 multi-provider preference, the five-item menu preference; src/AGENTS.md records the deleted screens/helpers, provider-aware client/settings contracts, routing resolution order; tests/AGENTS.md lists phase-11 (20 tests) + e2e and the verified key-less counts (232+14) matching my run. Current and accurate.
- **Phase doc amendment:** v1.4.0 header, provider dimension in §2.2, Gate 11.10, UAT 11.6, checklist counts 10 gates / 6 UATs — coherent.

## Nits (non-blocking)

- **N1 (README wording):** README lines 57/63 lump "invalid JSON/schema" into the ≤3-attempt quality-retry class. Extractor JSON/schema failures are actually never retried (`ExtractorError`, vision 04 §6); the quality-retry class covers synthesis-preservation and DOX-unparseable output only. Cosmetic overgeneralization.
- **N2 (Chunk Size row):** the §2.2 ASCII mockup shows a "Chunk Size" row; it is not implemented. Documented decision (compliance-log 2026-07-22 00:00 item 8): no gate, UAT, or files-to-modify entry specifies it; flagged for the user as final arbiter.
- **N3 (bin launcher form — ACCEPTED deviation):** spec-literal `npx tsx` + `shell:true` breaks on this machine's spaced project path (`Wiki v5`); the launcher resolves the local `node_modules/tsx/dist/cli.mjs` and spawns `process.execPath` with an argv array (no shell), falling back to `npx tsx`+shell only if tsx is missing. Spec intent (thin tsx launcher, no build step, stdio inherit, verbatim argv, exit-code propagation) fully preserved; verified working; documented in the file header, status file, and root AGENTS.md.
- **N4 (first-launch splash in the real workspace):** the project root contains a legacy `.llm-wiki-cli.json` (`{synthesis:true, updateAgents:true}`), so the welcome splash (shown only when neither config file exists) will NOT appear for the user in this workspace. Per-spec behavior; noted for UAT 11.5 step 2.
- **N5 (stale comment, pre-existing):** `src/state/metrics.ts` doc comments claim "the Extractor does not log per-call cost" — stale since the 2026-07-18 log feature (`extract-chunk.ts:72` passes `logPath`). Pre-Phase-11 text; harmless.
- **N6 (scanner exclusion breadth):** gate 11.7's scanner needs the extra `Naming rules:` line exclusion beyond the spec's listed categories (spec §2.5 forces the README to name the forbidden forms — self-contradiction otherwise). Documented deviation; my independent sweep applied the same set and found zero offenders.

## Recommendation

**READY FOR UAT** — all 10 gates PASS with independently produced evidence; tsc clean; key-less suite 232 passed + 14 acceptable skips, exit 0; branding sweep clean under my own scanner; compliance loop intact with no unresolved contradictions. No blockers. UAT notes: 11.5 step 1 (`npm link`) and the GitHub repo rename are manual steps outside the suite; mind N4 when checking the splash; UAT 11.6 is LLM-free.

---

# Phase 11 Verification Report — v1.5.0 extension (API Keys in Settings, gate 11.11)

**Verifier:** independent Verifier sub-agent (cold check; Implementer claims not trusted)
**Date:** 2026-07-23
**Spec:** `Implementation Plan/PHASE_11_polish.md` v1.5.0 (11 gates, 7 UATs); binding design = compliance-log entry 2026-07-23 00:00
**LLM spend:** $0.00 — `.env` (171 bytes, contains a live key) was renamed to `.env.verifier-bak` before every `tsc`/`npm test` run and restored immediately after (restoration verified: same size/mtime, backup gone). No live LLM calls were made.

## Gate 11.11 — per-sub-check verdicts (all evidence read cold from code + tests)

| # | Sub-check | Verdict | Evidence |
|---|---|---|---|
| 1 | Schema: missing block loads as nulls; save always writes the block; no format validation | PASS | `src/tui/settings.ts`: `ApiKeys{anthropic,openai}` null-defaulted; `normalizeApiKeys` maps non-string/empty → null and never rejects (fail-loud-at-call-time per design); `parseSettings`/`defaultSettings` always populate `apiKeys`, so `saveSettings` always writes the block. Test (phase-11.test.ts:890) round-trips `{anthropic: FAKE, openai: null}` through save→raw-file→load and asserts a pre-v1.5.0 config (`{synthesis:true}` only) loads with both nulls. |
| 2 | Resolution order: stored → env (incl. .env fallback) → error, both providers; auth headers carry the resolved key | PASS | `src/llm/client.ts` `resolveApiKey` (187-194): routing-carried stored key wins; else `process.env.ANTHROPIC_API_KEY`/`OPENAI_API_KEY` (`loadEnvFile()` populates process.env from project-root `.env` first, once per process). Missing-key error thrown BEFORE model resolution/request, exact extended text naming Settings + env + .env for both providers (443-447). Tests (914 anthropic, 954 openai) assert `x-api-key` = stored fake key over env, env key when nothing stored, exact error when neither, and undici called exactly 2× (no request on the error path). The tests pre-fire the one-time .env load so a real project-root key can never leak into a mock call. |
| 3 | Wire hygiene: key ONLY in auth header, never URL/body/query; llm-calls.json entry has no key material | PASS | `buildAnthropicRequest`: key appears only in `x-api-key` header; body = model/max_tokens/messages[/system][/temperature]; URL is a module constant. `buildOpenAIRequest`: key only in `authorization: Bearer` header; body = model/max_completion_tokens/messages. `LlmCallLogEntry` = timestamp/callType/context/provider/model/inputTokens/outputTokens/cost with an explicit SECURITY comment banning key material; the `appendLlmCallLog` call site (512-521) passes exactly those fields. Console line is `LLM Call \| Tokens: i/o \| Cost: $x` only. Log-hygiene test (1179) asserts the exact pre-v1.5.0 sorted key set, no `sk-ant-test` in the serialized entry or any console line, and that the header DID carry the stored key (the one sanctioned place). |
| 4 | Masking: source + last4 only in every state; editor echoes mask chars only; getApiKeyStatus never returns >4 chars | PASS | `keyStatusText` (settings-screen.tsx:94-103) renders only `configured ••••last4` / `from environment ••••last4` / `not set` from `getApiKeyStatus`, which returns `slice(-4)` (≤4 chars; client.ts:210-221). Editor opens with an EMPTY draft (stored key never pre-filled), `TextInput mask="•"`, placeholder is instructional text only. Non-TTY fallback renders the same masked strings. Error boxes carry fs error messages only. Screen test (1024) asserts `[configured ••••ab12]` + `[not set]` on load, `[configured ••••4321]` after staging, and that neither the full fake key nor the typed `sk-ant-test-typed-4321` appears in ANY captured frame. Helper test (992) asserts last4 ≤ 4 chars and ≠ the full key. |
| 5 | Stage/clear: editing stages locally; Save persists; empty submit clears (null); Esc cancels without persisting | PASS | `submitKeyEdit` (233-241): trimmed non-empty stages, empty stages null; Esc (250-255) closes the editor and discards the draft; persistence only via `save()` → `saveSettings`. Screen-driven test (1083) proves all three beats end-to-end against the real `.paper-chase.json`: stage→Save→config has the key; Esc mid-edit (junk typed)→Save→config unchanged; empty submit→Save→config null. |
| 6 | Integration: ingest() passes `{ ...models, apiKeys }` to setModelRouting — single point for CLI+TUI | PASS | `src/commands/ingest.ts:352-353`: `loadSettings(workspace)` → `setModelRouting({ ...tuiSettings.models, apiKeys: tuiSettings.apiKeys })` (best-effort try/catch). Repo-wide grep: only 4 src files reference apiKeys/getApiKeyStatus/resolveApiKey — the expected four (settings.ts, settings-screen.tsx, client.ts, ingest.ts). |
| 7 | Gitignore + committed-key hygiene | PASS | `.gitignore:27-28` = `.paper-chase.json` + `.llm-wiki-cli.json` (`.env` at line 4). `git ls-files \| grep -i paper-chase / llm-wiki-cli` → nothing tracked; legacy file shows `D` (index removal staged) but sits on disk with the old booleans `{synthesis:true, updateAgents:true}` (`cat` verified). `git grep -nE "sk-(ant\|openai\|live)" -- .` over tracked files → exactly 2 hits, both in `PHASE_11_polish.md`: the truncated doc placeholder `sk-ant-…` (line 155) and the explicitly-labeled FAKE UAT example `sk-ant-test-0000-ab12` (line 488). No real key material anywhere in the index. (Phase 11 is uncommitted; the untracked test file contains only `sk-ant-test-*`/`sk-openai-test-*` fakes — safe to commit.) |
| 8 | Docs: phase doc v1.5.0 coherent; README precedence + security note; root/src/tests AGENTS.md updated | PASS | Phase doc: header v1.5.0 + dated amendment, §2.2 API Keys subsection (storage shape, resolution order, UI contract, getApiKeyStatus contract, SECURITY-absolute paragraph, mockup), gate 11.11 with all 7 numbered items, UAT 11.7, checklist "11 gates / 7 UATs" — internally coherent. README: line 90 documents the full precedence chain (Settings-stored → env → .env) and the masked row strings verbatim; line 138 carries the security note ("gitignored, never commit it") + precedence again. Root AGENTS.md:102 = the 2026-07-23 user preference verbatim. src/AGENTS.md client entry covers v1.5.0 resolution, getApiKeyStatus, and the never-logged rule. tests/AGENTS.md updated with both-modes counts and the 27-test phase-11 file. |

## Regression verdict on gates 11.1–11.10

PASS — no regressions. Full key-less suite green (below); `tests/phase-11.test.ts` standalone 27/27. Gate 11.1's keystroke sequence updated exactly per gate-11.11 item 7: the two key rows sit after the model rows, so the model-row Down-counts are unchanged and `[ Save ]` moved from focus index 7 to 9 (verified in the test source, lines 183-211). Gate 11.10's missing-OPENAI_API_KEY test was updated to the extended v1.5.0 error string with an in-comment explanation (lines 675-692) and still asserts no request is fired. Gate 11.2's env-then-default no-routing behavior intact (`resolveModel` unchanged; `resolveApiKey` defaults to env when no routing/stored key — `setModelRouting` normalizes absent apiKeys to nulls).

## Security audit (wire / masking / logs / git)

CLEAN on all four axes: (wire) key material transits only in `x-api-key` / `authorization: Bearer` headers — URLs constant, bodies key-free; (masking) no code path renders, returns, or echoes more than the last 4 characters of a key — idle rows, masked editor, staged re-render, non-TTY fallback, and error strings all verified in source and by test; (logs) llm-calls.json entry field set frozen and test-asserted key-free, console cost line key-free, `src/state/metrics.ts` contains no key references at all; (git) both config names gitignored, nothing tracked, index contains no key-shaped strings beyond the phase doc's placeholder + labeled fake.

## Independent runs (`.env` renamed aside for all; restored + verified after)

- `npx tsc --noEmit` — **clean (exit 0)**.
- `npm test` — **Test Files 15 passed | 1 skipped (16); Tests 239 passed | 14 skipped (253); exit 0.** Exactly the Implementer's claimed key-less profile. Skips unchanged and acceptable: 12 live Phase 2 gates + Phase 0 gate 0.4 + RUN_E2E-gated e2e. Zero failures, zero new skips.
- `npx vitest run tests/phase-11.test.ts` — **27/27 passed (exit 0).**

## Nits (non-blocking)

- **N7 (cosmetic):** `submitKeyEdit` trims the submitted key. Whitespace-padded pastes are forgiven rather than failing loud at call time — harmless, arguably friendlier; the phase doc neither requires nor forbids it.
- **N8 (pre-existing, carried):** the provider-error path (`${providerName} API error (HTTP …): ${JSON.stringify(json)}`) echoes the provider's error body verbatim. A provider echoing the presented key back in an auth-error body would put it in a thrown error message. No known provider does this; not introduced by v1.5.0; flagged for awareness only.
- N1–N6 from the v1.4.0 report still stand as recorded there.

## Recommendation

**READY FOR UAT** — gate 11.11 PASS on all 8 sub-checks with cold-read evidence; no regression on gates 11.1–11.10; security audit clean on wire/masking/logs/git; tsc clean; key-less suite 239 passed + 14 acceptable skips, phase-11 file 27/27. UAT 11.7 is LLM-free with a fake key (the optional real-key variant spends ~$0.01 and is explicitly user-optional); remind the user that `.paper-chase.json` must never be committed.

---

# Phase 11 Verification Report — v1.6.0 extension (post-ingest AGENTS.md proposal review shortcut, gate 11.12)

**Verifier:** independent Verifier sub-agent (cold check; Implementer claims not trusted)
**Date:** 2026-07-23
**Spec:** `Implementation Plan/PHASE_11_polish.md` v1.6.0 (12 gates, 8 UATs); binding decisions = compliance-log entry 2026-07-23 04:00 (Reject is a NO-OP superseding the 2026-07-21 reject-deletes preference; review screen FLOW-ONLY; Accept unchanged; restored from git HEAD)
**LLM spend:** $0.00 — `.env` (171 bytes, contains a live key) was renamed to `.env.verifier-hidden` before every `tsc`/`npm test`/`vitest` run and restored immediately after (restoration verified: same size/mtime 2026-07-20 00:13, 1 API_KEY line, hidden name gone). No live LLM calls were made.

## Gate 11.12 — per-sub-check verdicts (all evidence read cold from code + tests)

| # | Sub-check | Verdict | Evidence |
|---|---|---|---|
| 1 | Reject is a NO-OP: no file touched; "No changes made." message; tests assert both files byte-identical | PASS | `src/tui/agents-review-screen.tsx` `reject()` (120-137) contains ZERO filesystem calls — the fs import is `{ copyFile, readFile }` only (no `rm`/`unlink`/`writeFile`), and `copyFile` appears solely in `accept()`. Message is exactly `Rejected proposed AGENTS.md updates for ${activeWiki}. No changes made.` Diff vs HEAD (`git show HEAD:src/tui/agents-review-screen.tsx`): the ONLY semantic change is the reject body — HEAD had `await rm(join(dir,'.state','proposed-agents.md'),{force:true})` + the old message; v1.6.0 removes the `rm` import and the deletion. `tests/tui/agents-review-screen.test.tsx:215-241` asserts `proposalPath` still exists, proposal bytes `toBe(proposalBefore)`, AGENTS.md bytes `toBe(agentsBefore)`, and the exact message. Gate 11.12 test 3 (phase-11.test.ts:1381-1404) re-asserts all three at the app level. |
| 2 | Accept fidelity: copyFile proposal → AGENTS.md; proposal kept; byte-identity asserted | PASS | `accept()` (103-118): `await copyFile(join(dir,'.state','proposed-agents.md'), join(dir,'AGENTS.md'))` — source (proposal) untouched. Screen test (192-213) asserts AGENTS.md gains the proposal line and `existsSync(proposalPath)` is true; gate 11.12 test 2 (1350-1379) asserts `readFileSync(agentsPath) === proposalText` (byte-identical) AND the proposal remains on disk. |
| 3 | Shortcut only when proposed: hint only in success state with `agentsUpdateProposed===true` + raw mode; `p` no-op otherwise; `onReviewAgents` wired; app routes with the ingested wiki pre-loaded | PASS | `ingest-screen.tsx`: `proposalWiki` state is set ONLY inside `if (result.agentsUpdateProposed)` (228-233) and reset to null at every run start (208). Hint render gate is `status === 'success' && proposalWiki && isRawModeSupported` (401-405) — the non-TTY fallback never renders it. The `p`/`P` handler (281-284) is reachable only in the success state and only when `proposalWiki` is set; without a proposal `p` falls through to the Enter-only branch and does nothing. App wiring: `app.tsx:110-113` `onReviewAgents={(wiki) => { setFlowWiki(wiki); setScreen('agents-review'); }}`; the review screen receives `wiki={flowWiki}` (124) and auto-loads via the `initialWiki` effect (agents-review-screen.tsx:91-101 — frame shows `Wiki: review-wiki`, asserted in gate test 2). Gate test 1 (1288-1326): with stub `agentsUpdateProposed:true` the hint appears; with `false` there is no hint AND the `onReviewAgents` spy never fires after `p`. |
| 4 | Menu still five: MENU_ITEMS unchanged; 'agents-review' absent; gate 11.3 unmodified and passing | PASS | `src/tui/menu.tsx:19-25` MENU_ITEMS = exactly `{init, add-pdfs, ingest, settings, exit}` with the five production labels. `app.tsx:23` adds `'agents-review'` to the `Screen` union only (flow-only routing target — allowed by the binding decision). Gate 11.12 test 4 (1406-1409) asserts `MENU_ITEMS.map(value)` `toEqual(['init','add-pdfs','ingest','settings','exit'])` and no `agents-review` entry. Gate 11.3 (313+) still asserts the five production labels render and passed unmodified in my run (31/31). |
| 5 | Restoration fidelity: line-diff matches HEAD; ratified UX kept (inline compact diff, [A]/[R] same-screen, [V] full scrollable, Escape collapse/leave) | PASS | `git show HEAD:src/utils/line-diff.ts > /tmp/head-line-diff.ts && diff /tmp/head-line-diff.ts src/utils/line-diff.ts` → **BYTE-IDENTICAL** (no output, exit 0). Review-screen diff vs HEAD shows only the reject-body change, the dropped `rm` import, and the doc-comment rewrite. UX intact: `COMPACT_DIFF_LINES=5` inline preview with `[A] Accept   [R] Reject   [V] View Full Diff` on the same screen (287); `v` switches to the `FULL_DIFF_LINES=12` scrollable view with A/R still available (291-311); Up/Down scroll by `DIFF_LINE_STEP=4`; Escape collapses `diff → ready` (151-153) and otherwise calls `onBack`. Screen test (243-263) exercises V-expand, A/R presence in the expanded view, and Escape collapse. |
| 6 | Restored test suite passes; reject test encodes no-op semantics | PASS | `tests/tui/agents-review-screen.test.tsx` = 6 tests (summary+diff render, accept, reject-no-op, full-diff view, no-proposal guidance, non-TTY fallback) — all green inside my full-suite run. The reject test (215-241) is explicitly titled "reject is a no-op: AGENTS.md and the proposal file stay byte-identical (Phase 11 v1.6.0)" and asserts byte-identity of both files plus the exact "No changes made." message. |
| 7 | Docs: phase doc v1.6.0 coherent; README + src/tests/root AGENTS.md updated; 2026-07-21 preference explicitly superseded | PASS | Phase doc: header v1.6.0 + dated amendment quoting the directive verbatim (line 11), §2.4 "Post-ingest AGENTS.md review shortcut" subsection (232-236), Gate 11.12 with all 6 numbered items (456-483), UAT 11.8 with the optional-cost warning (536-545), checklist "12 gates / 8 UATs" + the v1.6.0 checklist bullet (553-554, 563) — internally coherent. Root `AGENTS.md:99`: the 2026-07-21 preference is explicitly marked "**Superseded 2026-07-23 (Phase 11 v1.6.0 user directive):** Reject now does NOTHING — the proposal file is KEPT", plus flow-only reachability and "the five-item main menu is unchanged". `src/AGENTS.md:10` documents the restoration (reject no-op, accept copyFile, flow-only, `onReviewAgents` contract) and `:21` documents `line-diff.ts` as restored byte-for-byte. `tests/AGENTS.md:23-24,40` document the restored 6-test suite, gate 11.12's 4 tests, and both-modes counts (249+14 key-less / 261+2 with-key). `README.md:105` documents the `p` shortcut with accept/replaces and reject-keeps semantics. |

## Regression verdict on gates 11.1–11.11

PASS — no regressions. Full key-less suite green (below) with zero failures and zero new skips; `tests/phase-11.test.ts` standalone 31/31 covers gates 11.1–11.12 (27 pre-v1.6.0 tests + 4 new). The only production-code touch points for v1.6.0 are additive: the `onReviewAgents` prop + `proposalWiki` state in the Ingest screen, the `ingestFn` pass-through in App, the `Screen` union member, and the two restored files — gate 11.3's menu assertion and all prior gate behavior are untouched (the v1.6.0 amendment's own "Everything else is unchanged" claim holds).

## Independent runs (`.env` renamed aside for all; restored + verified after)

- `npx tsc --noEmit` — **clean (exit 0)**.
- `npm test` — **Test Files 16 passed | 1 skipped (17); Tests 249 passed | 14 skipped (263); exit 0.** Exactly the Implementer's claimed key-less profile. Skips unchanged and acceptable: 12 live Phase 2 gates (skipIf no `ANTHROPIC_API_KEY`) + Phase 0 gate 0.4 live smoke + RUN_E2E-gated `e2e.test.ts`. Zero failures, zero new skips. (The DOX Writer missing-key retry noise in stderr is the sanctioned deterministic-fallback path with no key; tests asserting it pass.)
- `npx vitest run tests/phase-11.test.ts` — **31/31 passed (exit 0).**

## Nits (non-blocking)

- **N9 (cosmetic wording):** `ingest-screen.tsx:230` still appends "(review and apply manually)" to the success summary when a proposal is written. Slightly dated now that the same screen offers the direct `[P]` review path one line below — but still literally true (manual review of `.state/proposed-agents.md` remains possible, and Reject deliberately keeps the file for it). Harmless.
- **N10 (by design, noted for UAT):** the `p` shortcut lives only inside the Ingest screen's success state; pressing Enter/Escape first returns to the menu and the shortcut is gone (flow-only per spec §2.4 — a proposal can still be reviewed later only programmatically or via a future proposal-writing ingest). UAT 11.8 should press `p` while the success state is on screen.
- N1–N8 from the v1.4.0/v1.5.0 reports still stand as recorded there.

## Recommendation

**READY FOR UAT** — gate 11.12 PASS on all 7 sub-checks with cold-read, self-produced evidence (including byte-level restoration diffs against git HEAD); no regression on gates 11.1–11.11; tsc clean; key-less suite 249 passed + 14 acceptable skips (17 files), phase-11 file 31/31. UAT 11.8 is optional-cost: a live ingest with the *Propose AGENTS.md Updates* toggle spends real money (~$0.01–0.05) and no dry TUI variant exists — or the user can exercise steps 3-6 against a pre-existing `.state/proposed-agents.md` after any proposal-writing ingest; mind N10 (press `p` from the success state) and use a throwaway wiki.
