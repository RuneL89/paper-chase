{
  "phase": "16",
  "status": "completed",
  "goal": "All 12 technical gates in PHASE_16_run_resilience.md pass (npm test is green), all 3 UAT steps are documented with expected output, compliance log shows no unresolved contradictions, status file shows all gates passed and no blockers, and total LLM cost is $0 (all gate tests are LLM-free).",
  "lastAction": "Phase 16 implemented 2026-07-25, exactly §2.1-§2.5, all 12 gates encoded in tests/phase-16.test.ts (17 tests, all LLM-free, $0) and green; npx tsc --noEmit clean; key-less full suite (.env stashed to .env.bak-phase16 for every run, restored and verified 171 bytes with original mtime after each run) 332 passed + 14 skipped across 22 files (21 passed, 1 skipped) = the Phase 15 baseline of 315+14/21 plus the 17 new tests, zero unenumerated regressions. FILES: src/llm/client.ts (LARGE_CALL_MAX_TOKENS=32768/LARGE_CALL_HEADERS_TIMEOUT_MS=600_000 headers timeout on large-output calls only; transportRetryDelayMs exponential 5s/15s/45s replacing linear 1s*attempt, attempt counts and error classes unchanged; setTransportRetrySleeper test hook; exported isTransientTransportError matching both providers' thrown shapes: 'API transport error after N attempt(s)' = network/timeout, 'API error (HTTP 429|5xx)' = transient after retries, 4xx and everything else false). src/utils/worker-pool.ts (options staggerMs/sleepFn: pickup slot chain spreads pickups deterministically ~staggerMs apart, cap/order/reject-after-settle semantics untouched, default 0 = Phase 15 behavior). src/state/synthesis-state.ts (NEW: readSynthesisState absent->empty/malformed->descriptive throw; isSkipEligible = strict-synthesis|permissive-synthesis only; synthesisPagePath; pageDataHash; recordSynthesisPage). src/state/metrics.ts (additive required transportFailures). src/state/synthesis-report.ts (additive finalMode 'transport-fallback'). src/materializer.ts (skip-eligibility check BEFORE the manual-edit conflict path in both write loops: record skip-eligible + dataHash match + file exists -> preserve byte-for-byte, page still in entityPages/topicPages, path+current disk hash reported in new MaterializeResult.preservedPages; no records on disk -> byte-identical pre-Phase-16 path). src/commands/ingest.ts (per-page transport fallback wrap in both pool page bodies; OutageDetector per stage with TRANSPORT_OUTAGE_CONSECUTIVE_LIMIT=5 and TRANSPORT_OUTAGE_RATE=0.1; resume partitionStage + reconstructedSkipEntry; per-page recordSynthesisPage checkpoint inside pool tasks; per-PDF checkpoint writeIngestionState right after state.sources[sourceSlug] assignment with state.pageHashes=workingPageHashes; run-scoped transportFailuresThisRun -> buildRunMetrics; pool calls pass staggerMs: options.poolStaggerMs ?? SYNTHESIS_POOL_STAGGER_MS=250; result.synthesisSkipped/synthesisTopicsSkipped + skip progress line; run-level transport summary warning). src/agents/curation.ts (slim schema: keep bucket optional in parse; validation derives keep as candidates minus collapsed merges minus drops (into+unsure fold in); legacy emitted keep must be EXACTLY consistent with the raw buckets else a contradiction error naming the slugs; overlap/unknown/self-merge/into-dropped/no-unique-survivor rules unchanged; CURATION_SIZE_TRIGGER_TOKENS=24576 (75% of 32768) + exported estimateDecisionListTokens; bucketCandidatesSized closes buckets on count OR size; curateWithScaling triggers two-round on overCount OR overSize). prompts/curation-topics.prompt.txt + curation-entities.prompt.txt (keep bucket removed from OUTPUT FORMAT, derived-keep instruction, justification cap 'cap it at a few words'; LANGUAGE block + placeholders intact). tests/phase-16.test.ts (NEW, 17 tests). FINGERPRINT SCHEME: pageDataHash(pageData, language) = SHA-256 hex over canonicalJson({ aggregate, language }) where canonicalJson sorts object keys recursively, keeps array order, drops undefined; aggregate = the full EntityPageData/TopicPageData MINUS slugToTitle (global cross-page context — including it would re-synthesize every page when any unrelated page changes); title+folder+structured data+language pair are all inside the hash; ONE exported helper shared by the Materializer preservation check and the ingest skip partition so the gates can never disagree. OUTAGE-DETECTOR STATE MACHINE: per stage (fresh detector each stage): total = pages ATTEMPTED this run (resume-skipped pages make no calls and are not counted); recordSuccess on any completed chain (strict/permissive/quality-template — the LLM demonstrably answered) resets consecutive to 0; recordDetectorTransportFailure increments failed+consecutive and RETHROWS the page's transport error when consecutive >= 5 OR failed > total*0.1 — the throw rejects the pool after in-flight settles (unstarted items never start) so the run aborts with THE transport error; below both thresholds the page lands on the structured template (report finalMode 'transport-fallback', loud warning 'Transport failure for <slug> after retries — template fallback.', metrics.transportFailures++) and the run completes with a run-level summary warning. CHECKPOINT WRITE POINTS: (1) per-PDF, inside the per-PDF loop immediately after state.sources[sourceSlug] = { hash, documentPages, ingestedAt: now, language: input } — state.pageHashes = workingPageHashes then writeIngestionState(dir, state), exactly the record the end-of-run write produces for that PDF (which stays final); (2) per-page, inside each synthesis pool task after the outcome is determined — recordSynthesisPage through the Phase 15 serialized write queue: read -> prune records whose page file no longer exists -> set -> write with keys sorted (deterministic bytes regardless of pool completion order); every outcome recorded (pass modes skip-eligible; structured-template and transport-fallback retried); skipped pages never rewrite their record (synthesizedAt preserved, keeping resume byte-stable). PRE-EXISTING TEST UPDATES (every one, with reason — nothing else touched): (1) tests/phase-15.test.ts, all six ingest-level gates (15.1, 15.2 runOnce, 15.3, 15.4, 15.5, 15.6): added poolStaggerMs: 0 — the new 250ms production dispatch stagger would serialize pickups and destroy the overlap/scramble timing those pool-SEMANTICS gates pin; the stagger itself is covered by gate 16.8 with an injected clock. (2) tests/phase-07.test.ts gate 7.10: inject setTransportRetrySleeper(async () => {}) with finally-restore — the exponential backoff (5s/15s/45s) would otherwise cost ~30s real time against the test's 30s timeout; the gate pins attempt counts and error classes, which are unchanged. (3) tests/phase-14.test.ts gate 14.3 (transient exhaustion): same instant-sleeper injection (two waits = 20s real otherwise). (4) tests/phase-14.test.ts gate 14.1 'missing from every bucket': under the keep-complement, omission is legal (derived keep), so the legacy keep list ['alpha','beta'] over four candidates is now a CONTRADICTION of the derived keep — the test now asserts the contradiction error naming gamma/delta (renamed accordingly). DESIGN DECISIONS A VERIFIER SHOULD KNOW: (a) Gate 16.1's '10-page pool run, 2 transport failures, run completes' is only consistent with the ratified >10% rate when the two failures are split across the TWO 10-page stages — encoded as one entity-stage and one topic-stage failure (1/10 = 10% each, at not over the threshold); single-stage 2/10 = 20% would abort. (b) Synthesis-state record `mode` reuses the report's finalMode vocabulary (strict-synthesis/permissive-synthesis/structured-template/transport-fallback) instead of a literal 'template-fallback' string — the pinned decision's 'recorded with mode: template-fallback or unrecorded' is satisfied semantically: non-pass modes are never skip-eligible and always retried. (c) Skipped pages contribute RECONSTRUCTED report entries (timestamp = record.synthesizedAt, mode = record.mode, attempts reconstructed as 1) so every stage's ordered report covers every page and gate 16.11's byte-comparison of synthesis-report.json holds; per-attempt detail of a killed leg does not survive an abort by definition. (d) metrics.json is EXCLUDED from gate 16.11's byte-comparison set by design: it is a run-history audit (its chunksSkipped legitimately reflects the resume's hash-skips) — every reader-facing artifact IS byte-compared equal (full .md tree incl. DOX indexes and AGENTS.md, synthesis-report.json, synthesis-state.json, ingestion.json, rolling-memory.json, curation-report.json, validation-report.json, structural-changes.json, language.json, extracted JSON). (e) The Materializer's preservation check runs BEFORE the manual-edit conflict path for skip-eligible pages: preservation never overwrites anything, so a human edit to a skip-eligible page is kept (silently, without a conflict entry — outcome identical to the conflict path, log entry differs); changed-fingerprint pages keep the full manual-edit conflict behavior. (f) The outage rate denominator is the stage's ATTEMPTED pages (post-skip partition), documented in code. (g) Gate 16.2's completion-order dependence is made deterministic with per-index increasing stub delays (pickup times are non-decreasing in index, so completion stays index-ordered) — never wall-clock assertions. (h) Gate 16.5's aggregate flip edits .state/extracted/*.json directly because the PDF is hash-skipped and the extraction stub never re-runs — exactly what a changed PDF + re-extraction would produce downstream. DEVIATIONS: none from canon; no blockers. DOX pass: src/AGENTS.md (new synthesis-state.ts bullet; ingest/materializer/client/worker-pool/curation/metrics/synthesis-report bullets updated; Local Contracts: four-class retry contract now carries the per-page fallback + outage detector, curation contract the keep-complement + sizing, concurrency boundary the stagger + checkpoints, NEW run-resume contract), prompts/AGENTS.md (both curation bullets: keep removed, derived keep, justification cap), tests/AGENTS.md (phase-16 entry + Verification counts 332+14/22), wikis/AGENTS.md (.state gains synthesis-state.json), README.md (Layer-4 item, rejection-loops paragraph, LLM client paragraph, state-files list, new Run resilience paragraph). Root AGENTS.md intentionally unchanged: no index line claims anything now false. Compliance log: [2026-07-25 10:10] COMPLIANT, no unresolved contradictions. LLM cost $0.00.",
  "gatesPassed": [
    "16.1",
    "16.2",
    "16.3",
    "16.4",
    "16.5",
    "16.6",
    "16.7",
    "16.8",
    "16.9",
    "16.10",
    "16.11",
    "16.12"
  ],
  "gatesFailed": [],
  "gatesPending": [],
  "llmCost": "$0.00",
  "tokenBudgetRemaining": "$0.00 (no live LLM calls in implementation)",
  "blocker": null,
  "nextAction": "None — phase closed. Open tracks live in Implementation Plan/BACKLOG.md.",
  "uatAcceptance": "User-accepted 2026-07-25. UAT 16.1 accepted on gate 16.11 evidence (kill-and-resume byte-equality) — live drill waived. UAT 16.2 accepted on gates 16.1-16.4 (run 5 transportFailures: 0 — no live occurrence). UAT 16.3 optional, superseded by run 5 completion."
}
