{
  "phase": "19",
  "status": "completed",
  "goal": "All gates in PHASE_19_stale_pagehash_convergence.md pass (npm test green, LLM-free, $0), compliance log shows no unresolved contradictions; unified verification with the sibling phases passes.",
  "lastAction": "Phase 19 implemented 2026-07-29 under the parallel-execution file ownership (src/materializer.ts, tests/phase-19.test.ts, this status file; src/commands/ingest.ts convicted but NOT edited \u2014 patch proposals below for the orchestrator). ROOT CAUSE (\u00a72.1, investigation-first, live evidence + fixture proof): the CONVICTED STAGE is the Phase 16 PER-PDF CHECKPOINT in src/commands/ingest.ts (~line 868: `state.pageHashes = workingPageHashes; await writeIngestionState(dir, state);` inside the per-PDF loop) combined with the four synthesis write points (~1047 entity strict, ~1092 entity permissive, ~1251 topic strict, ~1288 topic permissive) which REPLACE page content after that checkpoint without folding the new content hash into the persisted state. Mechanism: the checkpoint persists PRE-SYNTHESIS (structured-template) hashes; the ONLY reconciliation is the end-of-run re-hash from disk (ingest.ts:1416-1431). A run that aborts between a synthesis write and that re-hash (fatal HTTP 4xx mid-stage / outage-detector abort / kill) leaves recorded(template hash) != disk(synthesized page). The Phase 16 skip-eligibility/preservedPages convergence masks it whenever .state/synthesis-state.json holds a matching record (the preserve path folds the disk hash before the conflict check); with the records absent (the afdk condition) the manual-edit guard false-flags tool-written pages, and every completed re-ingest keeps the stale record because conflict-skipped pages are never covered by the re-hash \u2014 self-perpetuating. LIVE EVIDENCE: dist/wikis/rkkp-adhd (UNREPAIRED): 138 of 164 pageHashes entries != sha256(disk), 139 'Page was manually edited' conflicts 2026-07-25T13:45 \u2014 pages on disk are synthesized (strict records in synthesis-state.json) while recorded hashes match NO current render. dist/wikis/rkkp-afdk (hand-repaired): 9 standard-reason conflicts 2026-07-28 (8 at 17:45 + 1 at 19:39, all write-loop checkPageConflict flags on entity pages living under topics/ via extractor folder slips, e.g. entity slugs apixaban/atrieflimren with folder topics/medications / topics/medical-conditions). FIXTURE PROOF (gate 19.1, tests/phase-19.test.ts): run 1 aborts mid-topic-synthesis on a stubbed fatal HTTP 404 (gate 16.4 pattern) AFTER the checkpoint persisted template hashes; the recorded hash for topics/topic-one/topic-one.md then equals the deterministic TEMPLATE render of the same aggregate (proven by materializing an identical copy with synthesis records removed) and != sha256(disk), while the never-synthesized topic-two converged \u2014 pinning the divergence to pages replaced after the checkpoint; after deleting synthesis-state.json (the afdk exposure condition) a COMPLETED resume false-flags both pages ('Skipping update of ... (manually edited). Conflict logged.', 2 manual-edit entries) and still leaves recorded != disk. \u00a72.2: the convicted stage is NOT in my ownership \u2014 TWO EXACT PATCHES proposed for the orchestrator (anchors + code in this file's nextAction and the Implementer's final message): (P1, the fix) hoist the end-of-run re-hash into a finally around the synthesis section so an aborting run also converges writtenPagePaths from disk before the error propagates; (P2, the cure-blocker) assign `state.pageHashes = workingPageHashes;` before the writeIngestionState at ~line 897 so a no-checkpoint run (every PDF hash-skipped) persists the working folds (preservedPages convergence, rewrittenLinks, removedPages deletions) instead of re-persisting the pre-run map; (P3, optional observability) surface MaterializeResult.convergedPages as progress lines in runMaterialize. \u00a72.3 IMPLEMENTED in src/materializer.ts (my ownership): checkPageConflict now takes the deterministic render of the current aggregate and returns 'write' | 'converge' | 'conflict' \u2014 on disk != recorded with disk == current render the page is provably tool-written and CONVERGES (new MaterializeResult.convergedPages, console convergence note via logHashConvergence, the write proceeds so writtenPages folds the disk hash; NEVER a conflicts.json entry); anything else conflicts exactly as today (human-edit protection NOT weakened \u2014 the curation veto filter and fork-reconciliation paths are untouched). \u00a72.4 encoded as assertHashInvariant (entities/topics walk, recorded == sha256(disk) both directions, stale-key check). GATES in tests/phase-19.test.ts (4 tests, all LLM-free, $0, phase-15/16/17 harness patterns, Date-only fake timers, poolStaggerMs: 0): 19.1 reproduction + stage-naming PASSES; 19.2 encoded as test.fails (verified to fail for exactly the right reason \u2014 recorded template hash != disk hash at the post-abort assertion; the orchestrator flips test.fails -> test when applying P1); 19.3 convergence (bogus recorded hash + disk == render converges for entity AND topic, no conflict, page updated, writtenPages fold == disk hash, convergence note logged; true human edit still conflicts, edit survives, 2 conflicts.json entries) PASSES; 19.4 two-PDF ingest with curation ACTIVE (topic-b -> topic-a merge, beta -> alpha entity merge, deletions folded) leaves the invariant green PASSES. npx tsc --noEmit clean. npx vitest run tests/phase-19.test.ts: 4 passed (19.2 green as an expected failure). FILES CHANGED: src/materializer.ts (checkPageConflict signature + converge verdict, logHashConvergence, MaterializeResult.convergedPages additive field, entity/topic write loops render before the conflict check and handle 'converge'), tests/phase-19.test.ts (new), .state/phase-19-status.json (this file). NOT TOUCHED (ownership): src/commands/ingest.ts, src/agents/, src/validation/, src/pages/, prompts/, all AGENTS.md \u2014 the DOX updates for src/AGENTS.md (materializer bullet: convergedPages + checkPageConflict converge verdict) and tests/AGENTS.md (phase-19 ownership entry + Verification line) are listed for the orchestrator's integration pass. Full suite NOT run (parallel-execution rule; gate 19.5's full-suite leg is the orchestrator's). Pre-existing-test risk review for the materializer change: the converge path only fires where a conflict previously fired AND disk == current render; every pre-existing conflict fixture (phase-08 8.5, phase-14 14.8 veto, fork reconciliation) uses disk != render and stays a conflict. LLM cost $0.00.",
  "gatesPassed": [
    "19.1",
    "19.3",
    "19.4"
  ],
  "gatesFailed": [],
  "gatesPending": [
    "19.2 (encoded as test.fails \u2014 green as expected-failure now; passes for real once the orchestrator applies ingest.ts patch P1 and flips test.fails -> test)",
    "19.5 (orchestrator's unified full-suite leg; this file's legs \u2014 phase-19 suite green + npx tsc --noEmit clean \u2014 are done)"
  ],
  "llmCost": "$0.00",
  "tokenBudgetRemaining": "$0.00 (no live LLM calls in implementation)",
  "blocker": null,
  "nextAction": "Orchestrator integration: apply the two ingest.ts patches (P1 finally-guarded re-hash around the synthesis section \u2014 anchor `if (extract && synthesis && lastMaterializeResult) {` ~line 930 and the re-hash block at ~1416-1431; P2 `state.pageHashes = workingPageHashes;` before the writeIngestionState at ~line 897; optional P3 convergedPages progress lines in runMaterialize), then flip test.fails -> test for gate 19.2 in tests/phase-19.test.ts line 413, re-run npx vitest run tests/phase-19.test.ts (all 4 must pass as real passes), then the unified full-suite verification with Phases 18/20 (gate 19.5). DOX pass for the orchestrator: src/AGENTS.md materializer bullet (convergedPages + checkPageConflict converge verdict), tests/AGENTS.md (phase-19 ownership entry + Verification update). UAT 19.1/19.2 are observational on the next live ingests (zero false 'manually edited' lines; convergence notes instead).",
  "verifierNote": "verifierNote (unified verification 2026-07-29, cold check): PHASE 19 VERIFIED \u2014 gates 19.1-19.5 all PASS. The convicted stage was independently confirmed against HEAD: the per-PDF checkpoint (HEAD:868) persisted pre-synthesis hashes and the only reconciliation was the end-of-run re-hash (HEAD:1417-1430), so an abort between a synthesis write and the re-hash leaks recorded(template) != disk(synthesized) \u2014 the recorded root cause is correct. The orchestrator applied P1 (finally-guarded rehashWrittenPagesFromDisk around exactly the synthesis section), P2 (state.pageHashes carry before the pre-synthesis write), and P3 (convergedPages progress lines), and flipped gate 19.2 from test.fails to test (tests/phase-19.test.ts:408). Gate 19.1 post-fix rewrite still proves the stage via the inverted stage-proof (recorded == synthesized, != template render). Gate 19.3 leg B: true human edits STILL conflict (2 conflicts.json entries, edit survives) \u2014 human-edit protection unweakened. Independent runs: tsc clean; 375+14/26 key-less; phase suites 39 passed. Full report: .state/phase-18-19-20-verification.md.",
  "uatAcceptance": "User-accepted 2026-07-29 (one declaration covering Phases 17-20). Evidence: all 5 gates + unified verification; root cause convicted (per-PDF checkpoint pre-synthesis hashes; rkkp-adhd live proof 138/164 mismatched); human-edit protection proven unweakened (gate 19.3 leg B)."
}
