# Phase 22 Verification — Composite Pages (Option C: five ratified rollup classes)

**Verifier:** cold-verify sub-agent (no knowledge of Implementer rationale beyond the status file)
**Date:** 2026-07-29
**Contract:** `Implementation Plan/PHASE_22_composite_pages.md` §2.1–§2.4, gates 22.1–22.10 (incl. the 2026-07-29 gate-22.10 ingest-wiring addendum), canon basis = the five-class rollup amendment
**Canon checked:** `02_WIKI_concept_detailed.md` §2, §3, §4.5, §4.6 (amended), §4.8; `05_page_types_specification.md` §2, §6 (amended); `07_validation_and_quality.md` §2.3–§2.6, §3, §5
**Implementer account:** `.state/phase-22-status.json` (claims 10/10 gates, 425+14/28 key-less, tsc clean, $0, plus the gate-22.10 addendum)

---

## 1. Diff audit (HEAD = dcb60b4; Phase 21 files uncommitted too — separated, not re-verified)

**Verdict: IN SCOPE.** Phase 21 changes (already verified in `.state/phase-21-verification.md`): `src/agents/pre-merge.ts` + `src/state/curation-decisions.ts` + `tests/phase-21.test.ts` (NEW), the pair/confirm-deny/sticky parts of `src/agents/curation.ts`/`src/materializer.ts`/`src/state/curation-report.ts`, `prompts/curation-{entities,topics}.prompt.txt` PROPOSED PAIRS, launcher 1.0.5→1.0.6, the DOX chain, `.state/phase-21-status.json`, the three arc phase docs.

Phase 22 changes (verified here):

| Path | Verdict |
|---|---|
| `src/agents/pre-merge.ts` (reroute: region families → class-5 clusters, indicator number↔concept leg → class-3 cluster; `proposedClusters` additive; cluster-internal merge edges dropped; neverMerge vetoes whole proposals) | §2.1 — in scope |
| `src/state/curation-decisions.ts` (`action: 'cluster'` record, `class` 1-5 + `rationale` validated on read; members re-derive `[into, ...from]`) | §2.1 — in scope |
| `src/agents/curation.ts` (CurationClusterDecision/ClusterVerdict, `"clusters"` parse + validation with exact error lists, cluster verdict bookkeeping, keep-complement excludes clustered slugs, topics never cluster) | §2.1 — in scope |
| `src/pages/composite-page.ts` (NEW, 353 lines) | §2.2/§2.3 — in scope |
| `src/materializer.ts` (cluster application, sticky cluster pre-application, member pull-out, remap/rewrite sharing, intra-cluster mirror dedupe, all-or-nothing cluster veto, frontmatter-gated reversed-cluster deletion, already-pooled-into guard, composite write loop, rolling memory) | §2.2 — in scope |
| `src/agents/synthesis.ts` (composite value builders + two writers) | §2.3 — in scope |
| `src/validation/preservation-check.ts` (`checkCompositePreservation`), `src/validation/schema-validator.ts` (composite known type + members/class rules), `src/dox-writer.ts` (compositeMembers + catalog suffix) | §2.4 — in scope |
| `src/commands/ingest.ts` (gate 22.10 composite stage), `src/state/synthesis-state.ts` (CompositePageData fingerprint), `src/state/synthesis-report.ts` + `src/state/conflicts.ts` (pageType unions) | gate 22.10 — in scope |
| `prompts/composite.prompt.txt` + `prompts/composite-permissive.prompt.txt` (NEW), `prompts/curation-entities.prompt.txt` (Composite-clusters block + PROPOSED CLUSTERS section + `"clusters"` output — slot-additive) | §2.1/§2.3 — in scope |
| `scripts/launcher-entry.ts` VERSION 1.0.6→1.0.7, dated comment | exactly one increment over Phase 21's — in scope |
| `Project Vision/02_WIKI_concept_detailed.md` §4.6 + `05_page_types_specification.md` §6 amendments | the ratified amendment — in scope |
| `tests/phase-22.test.ts` (NEW, 1811 lines, 23 tests); enumerated touches: `tests/phase-21.test.ts` (3 tests — reroute expectations), `tests/phase-13.test.ts` (13.1 ceiling-site count 4→6), `tests/phase-15.test.ts` (15.6 runPool count 2→3) | gates — in scope (touches verified intent-preserving, §4 probe g) |
| `AGENTS.md`, `src/AGENTS.md`, `prompts/AGENTS.md`, `tests/AGENTS.md`, `wikis/AGENTS.md` (splits hatch + cluster records documented), `Implementation Plan/*` (B22 struck SCHEDULED→phase doc, phase rows), `.state/compliance-log.md` [2026-07-29 14:40], `.state/phase-22-status.json` | DOX pass + bookkeeping — in scope |
| `.state/phase-23-status.json` (`scheduled` placeholder), `.zcode/` (session tooling) | arc planning / noise — not Phase 22 scope creep (same verdict as Phase 21) |

**Untouched as required:** `dist/`, `test-pdfs/`, `templates/`, `.env` — `git diff --stat` empty for all four; `.env` 171 bytes, mtime 2026-07-20 00:13:00. No TUI files modified (approval-checklist "No TUI changes" holds).

## 2. Independent runs (personally executed)

| Run | Expected | Actual | Verdict |
|---|---|---|---|
| `npx tsc --noEmit` | clean | **clean** (exit 0, no output) | PASS |
| `npx vitest run tests/phase-22.test.ts` | 23 passed | **23 passed** (3.01s) | PASS |
| Full key-less suite (`.env` → `.env.bak-verify22`, `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` unset) `npx vitest run` | 425 passed + 14 skipped / 28 files | **425 passed + 14 skipped / 28 files (27 passed, 1 skipped)** (32.02s) | PASS |
| `.env` restored | byte-identical | 171 bytes + mtime 2026-07-20 00:13:00.000 post-restore; no `.env.bak-verify22` left behind; `git status` clean of env entries | PASS |

Baseline arithmetic checks: 402 (Phase 21) + 23 (phase-22) = 425; 27 + 1 = 28 files. Zero unenumerated regressions.

## 3. Per-gate cold check

**Gate 22.1 (five-class validation, exact error lists) — PASS.** 7 tests genuinely pin the requirement, and the source produces every pinned error: accepts class-3 (indicator+concept) and class-5 (same-stem) with member-order normalization (into-first-then-sorted, curation.ts lines 991–999); rejects two-concepts class 3 ("needs exactly one indicator slug"), 3-member class 3 ("1:1 only"), different-stem class 5 ("must share the same slug-stem"), class 6 ("must be 1-5"), into-∉-members ("must be one of its members" — checked on the RAW entry at line 987 BEFORE normalization, the ordering the status file flags), unknown member ("unknown slug"), over/under-cap ("takes 2-4 members"), double membership ("appears in two clusters"), bucket overlap ("is decided by a cluster"), confirmed-pair overlap ("overlaps a cluster"), neverMerge member pair ("contains the neverMerge pair … the human veto stands"), judged-twice, mismatched-copy ("copy the proposal exactly"), cannot-deny-unproposed. The reask leg proves the exact error list feeds back (`=== CORRECTION REQUIRED ===` + "takes 2-4 members" in prompt 2, attempts 2). The detection reroute leg proves region families emit ONE class-5 cluster (rank prefix<suffix<bare, into first), indicator number↔concept emits a class-3 cluster (into = indicator), the strict-identity indicator leg stays a merge pair, and a neverMerge pair vetoes a whole proposal into `vetoed` — all confirmed in pre-merge.ts source (lines 570–598, 617–686, 836–903). Cluster members stay out of every bucket and the derived keep (lines 1066–1085, 1324–1332); topics reject any `"clusters"` output (line 804).

**Gate 22.2 (assembly) — PASS.** The test drives a full materialize with the deterministic proposal visible to the stub (seenClusters exact), then pins: composite at the into path with `type: composite` + `class: 3`; per-member evidence groups with both members' items verbatim; aliases union carrying both member titles; `result.compositePages` member-tagged (roles indicator/concept); member pages absent from `entityPages` and from disk; the third entity's relationship reference remapped (`[[indikator-…|…]]`, no `[[antibiotikabehandling`); the pre-existing document page's member-targeted wikilinks rewritten (bare form gains the member title as display) with `rewrittenLinks` recording the path; the decision recorded as `action: 'cluster'` with class/rationale/signal; `checkLinks` zero broken; `validateSchema` zero invalid. Source confirms the mechanism: `applyEntityClusterToMaps` pulls members out of `entityMap` (member pages never written; on-disk member pages delete like merged-away pages), seeds `entitySlugRemap`/`mergeRewrites` (the shared Phase 14 passes), and the composite write loop pools member-tagged aggregates (materializer.ts lines 1341–1382, 1755–1764, 2032–2137).

**Gate 22.3 (deterministic shell) — PASS.** The test pins the complete frontmatter (type/class/members block with role+significance/aliases union/real ISO `updated`/aggregated `sources`/member-type tags; `'sparse' in data === false`), the `## Members` block format, per-member `### <Title>` groups in member order, the intra-cluster relationship rendered ONCE as the plain member title (never a self-link; the incoming mirror deduped), the external incoming record as a pipe-form wikilink with evidence, and basename `## Sources`. Source confirms every rendering rule in composite-page.ts (`buildCompositeFrontmatter` lines 177–201, `compositeWikilink` 208–217, `writeCompositePage` 227–329).

**Gate 22.4 (synthesis values + prompts slot-additive) — PASS.** The filled-prompt test spies `callLLM` and pins both members' slots (title/slug/role/significance), both members' evidence groups, relatedEntities carrying only the external target (fellow members excluded — `buildCompositeRelatedEntities` subtracts the composite and its members, synthesis.ts lines 317–342), the `=== CITATION KEYS ===` union map, the en/en LANGUAGE block removed, and the constitution appended. The template test pins the Phase 7 `{languageDirective}` slot, the composite data slots, the Phase 17 relatedEntities slot, the Phase 18 `{citationMap}` slot, and — the load-bearing check — the wikilink rule and citation-keys rule paragraphs BYTE-EQUAL to the Phase 18 entity baselines, with the strict/permissive framing preserved ("two layers" / "**hybrid format**"). Both new prompts personally read: LF-only (0 CR bytes), slot-conformant, composite-specific sections present (ONE woven article; per-member verbatim Layer 2 groups; members never wikilink targets; Journalist Test task).

**Gate 22.5 (per-member preservation) — PASS.** `checkCompositePreservation` (preservation-check.ts lines 176–227) iterates every member group: mention contexts, outgoing AND incoming relationship evidence, claim texts as verbatim substrings, plus the union citation keys and the Phase 18 off-map-marker rule. The test proves pass-on-complete, fail on member A mention drop, member B claim drop, member B incoming-evidence drop, and off-map `[^src9]` — with the exact dropped item strings surfaced (this is what feeds the reask block).

**Gate 22.6 (sticky re-application, oscillation impossible) — PASS.** Two-run test: run 1 sticks the class-5 cluster (record carries class + signal); run 2 re-aggregates the SAME extraction and the sticky record rebuilds the composite BEFORE candidates — `entityCalls === 0`, `compositePages` length 1, `entityPages` EMPTY (zero member pages), `fromSticky.entityClusters` distinguished from `decidedThisRun`, the location member page never recreated, both members' evidence intact on the composite, nothing new recorded. Source: sticky cluster records pre-applied at materializer.ts lines 1415–1422, `clusteredMemberSlugs` excluded from `buildEntityCandidates` (line 1440) — thin member pages cannot oscillate back because the model never sees the members again.

**Gate 22.7 (split escape hatch) — PASS.** Hand-edited `splits: ['hovedstaden']` un-applies the cluster on the next materialize: BOTH member pages rebuilt as `type: entity` pages (the into path's composite replaced — no `class` field), the composite page removed, `splitReversals` logged with reason `split`, the family re-proposed as a class-5 cluster to the next curation call (members out of the OPEN list, judged only in `"clusters"`), the split slug NOT consumed while nothing is re-decided, schema validator clean on the rebuilt pages. Source: reversed records collected at lines 1044–1052; composite deletion gated on the on-disk page being `type: composite` (frontmatter check, lines 1765–1776 — a hand-maintained page at that path is never deleted); the supplementary decisions-file test proves re-decision consumes the split (including via the `into`).

**Gate 22.8 (fingerprint) — PASS.** Unit leg: `pageDataHash` ignores `slugToTitle` (unrelated-page changes never flip it — the global context exclusion in synthesis-state.ts), flips on a member-set change and on a member-evidence change. Materialize leg: with a skip-eligible record seeded, an unrelated entity's new mention leaves the composite byte-identical in `preservedPages` (not `writtenPages`) while the unrelated page IS rewritten — the Phase 16 resume contract holds for composites (composite write loop lines 2112–2121).

**Gate 22.9 (full key-less suite + tsc) — PASS.** My independent numbers in §2 (not the Implementer's): 425+14/28, tsc clean.

**Gate 22.10 (ingest wired for composites) — PASS.** Four tests pin the chain end-to-end and I verified the stage is a FAITHFUL mirror of the entity/topic stages line-by-line (ingest.ts 1513–1696 vs. the topic stage 1300–1511): (1) strict pass replaces the shell with the rich article — `trySynthesisMode` reask with `checkCompositePreservation`, write-point enforcers exactly `enforceCompositeFrontmatterInMarkdown` + `enforceSourcesSectionInMarkdown` over `buildCompositeCitationMap` with `repairPageLinks` outermost and NO sparse enforcer (correct — composites are never sparse; the composite frontmatter enforcer already covers the aliases union), report entry `pageType: 'composite'`, `finalMode: 'strict-synthesis'`, attempts 1; (2) strict preservation failure → 3 attempts with attempt 2's correction block carrying the EXACT dropped claim (the shared `preservationFeedbackErrors` covers the composite result's droppedMentions/Relationships/Claims/Citations/extraMarkers), then permissive pass (`finalMode: 'permissive-synthesis'`); (3) double failure → the deterministic shell kept (`## Members` intact, `finalMode: 'structured-template'`, `compositeConflicts` 1, conflicts.json entry with `pageType: 'composite'`); (4) run 2 unchanged → ZERO composite/entity/topic LLM calls, page byte-identical, `synthesisCompositesSkipped` 1, second report entry reconstructed as strict-synthesis. Pool shape identical (`runPool` cap `SYNTHESIS_POOL_SIZE` + stagger), per-stage `makeOutageDetector`, `recordDetectorTransportFailure`/`recordDetectorSuccess` wiring identical, `chainPhase` transport-fallback entry identical, per-page `recordSynthesisPage` checkpoint with the composite fingerprint, `partitionStage`/`reconstructedSkipEntry` widened to CompositePageData, counters folded into `formatIngestSummary` (lines 506–512), progress lines in the existing vocabulary.

## 4. High-value probes mandated by the brief

(a) **Five-class validation rejects out-of-class/over-cap/double-membership/neverMerge with exact errors — CONFIRMED** (gate 22.1's seven tests + source lines 1002–1096; class-3 one-indicator rule, class-5 same-stem rule, cap, disjointness, veto all produce the pinned strings).

(b) **Graph stays entity-granular — CONFIRMED.** Extraction JSON keeps member identities; only the page layer pools. Relationship subject/object, incoming subjects, claim entity lists, and timeline entity lists on every REMAINING entity remap member→into (materializer.ts 1694–1718); member-targeted wikilinks on all pre-existing content pages rewrite via the shared `mergeRewrites` pass; member pages are pulled out of `entityMap` and their on-disk pages delete. Inside the composite, member-tagged groups deliberately KEEP member-slug references (the member association is the design); the intra-cluster self-loop renders once via the mirror dedupe, externals link out. `buildCompositeRelatedEntities` excludes the composite and its members from legal link targets.

(c) **Sticky re-application makes member-page oscillation impossible — CONFIRMED** (gate 22.6; candidates built after pre-application with cluster members excluded).

(d) **Split escape hatch dissolves cleanly — CONFIRMED** (gate 22.7; composite removed only when the on-disk page IS `type: composite`; members rebuilt; reversal logged; consumption on re-decision covers the into).

(e) **Gate 22.10 faithful mirror + exactly-right enforcer set — CONFIRMED** (§3 gate 22.10; chain modes, feedback contents, pool, detector, checkpoint, report vocabulary, reconstructed skips all mirror; no sparse enforcer; no aliases enforcer needed — the composite frontmatter enforcer unions aliases).

(f) **Already-pooled-into guard — SOUND.** `applyEntityClusterToMaps` no-ops when `clusterMap.has(cluster.into)` (materializer.ts 1342–1350). In production the scenario is unreachable: sticky members leave the candidate set (`clusteredMemberSlugs`), proposals are filtered to candidate slugs, and validation only accepts slugs in the (cluster-free) candidate set — so a decided cluster can never duplicate a sticky one. Without the guard, a stub-driven duplicate application would re-pull members from an emptied `entityMap` and CLOBBER the pooled evidence with empty on-disk-meta aggregates. Defensive, seam-only, zero production behavior change.

(g) **Enumerated pre-existing test touches intent-preserving — CONFIRMED.** phase-21 gate 21.1 region test now asserts 5 class-5 clusters of 3 members (into=region-X, rank order, same stem) with autoApply/proposed still empty — the detection-precision intent intact; gate 21.1 indicator test splits the concept leg (class-3 cluster) from the strict-identity leg (still a merge pair, distinct numbers still rejected); gate 21.2 mixed fixture moves `ct-skanning ↔ indikator-2-ct-skanning` to `proposedClusters` while the auto-tier-is-exactly-two-signals assertions stand; phase-13 gate 13.1's count 4→6 reflects the two composite writers using the SAME shared `SYNTHESIS_MAX_TOKENS` constant (single-constant intent unchanged, no 8192 literal); phase-15 gate 15.6's runPool count 2→3 reflects the composite stage pooling (fixed-cap/sequential-everything-else contract and its behavioral proofs untouched).

(h) **Vision amendment text matches the phase doc's canon basis + marker — CONFIRMED.** Both `02` §4.6 and `05` §6 carry the user directive quote and the five classes VERBATIM from the phase doc's canon basis (abbreviation/name-variant; brand↔generic `eliquis`↔`apixaban`; indicator↔concept 1:1 `indikator-1`↔`antibiotikabehandling`; facility↔city `holbaek-hospital`↔`holbaek`; same-name different-type `region-hovedstaden`), the member cap 2-4, sticky records, split hatch, out-of-class-is-a-validation-error — each marked "amended 2026-07-29, user-ratified", and the application is logged at compliance-log [2026-07-29 14:40]. The implementation honors the five classes and ONLY those (class ∈ 1-5 validated; classes 3/5 deterministic-proposed; classes 2/4 open model judgment with rationale; neverMerge absolute).

## 5. Compliance against canon

- **`02` §2 (self-contained/cited/linked/preserved) — MATCH.** The composite is self-contained (members block + all unioned evidence on one page), cited (union citation map + aggregated `sources` frontmatter), linked (external targets via relatedEntities; member names resolve via aliases and rewrites), preserved (per-member Layer 2 groups verbatim).
- **`02` §3 (two-layer model) — MATCH/EXTEND.** The composite is a content page with the same two layers: the shell is the deterministic Layer 2; gate 22.10 wires the Layer 1 synthesis (strict → permissive → template) with the Phase 7 language rule carried by the prompts' `{languageDirective}` slot (Layer 1 output language, Layer 2 verbatim source language).
- **`02` §4.5 (Journalist Test) — MATCH.** The strict prompt's TASK explicitly requires the Journalist Test on the composite (name every member, explain the relation, cite three facts, name two related pages); even the deterministic shell names every member with type/role/significance and preserves every fact with citations — the test is achievable on a composite, easier than on the thin members (the phase objective).
- **`02` §4.6 / `05` §6 (amended) — MATCH.** Implementation honors the five ratified classes and only those; cap 2-4; class-3 1:1 with exactly one indicator; class-5 same-stem; member pages not written; aliases union; wikilinks rewritten; sticky records; splits hatch; out-of-class is a validation error, never a silent judgment. The graph stays entity-granular (probe b). `05` §6's "a cluster is NOT a merge" holds — nothing is unioned away in the extraction data; identities persist in the graph.
- **`02` §4.8 (sparse) — MATCH.** Sparse never applies to composites (never emitted by the shell; no sparse enforcer at the write points) — consistent with "composites are rich by construction" and the §4.8 sparse-honesty intent (thin stubs are exactly what composites eliminate).
- **`07` §2.3 (decision-list validation) — MATCH/EXTEND.** Cluster rules extend the same validated-list discipline: exact error lists fed back through the §5 reask loop, exhaustion → keep-all fallback (a null decision list applies no clusters — every candidate written as-is), all-or-nothing application, keep-complement excludes clustered slugs, legacy-keep consistency check cluster-aware.
- **`07` §2.4 (completeness check) — MATCH.** `checkCompositePreservation` is the same verbatim-substring mechanism iterated per member, union citation keys, off-map markers; failure → reask → permissive → shell (the structured-template fallback preserved).
- **`07` §2.6 (page schema) — MATCH/EXTEND.** `composite` joins the known types; members (2-4, each with slug) + class (integer 1-5) required; a valid composite passes clean (supplementary test).
- **`07` §3 (human-edit protection) — MATCH.** The composite write loop runs the same `checkPageConflict`/convergence postures as entity pages; the cluster manual-edit veto is all-or-nothing (a hand-edited/untracked member page vetoes the WHOLE composite, logged with `action: 'cluster'` in `manualEditSkips`); reversed-cluster deletion is frontmatter-gated so a hand-maintained page is never deleted.
- **`07` §5 (error handling) — MATCH.** The composite stage inherits the bounded-retry posture: quality failures reask ≤3 with exact findings, transient transport → per-page template fallback + outage detector, HTTP 4xx aborts; curation validation failures keep-all without data loss.
- **Approval checklist extras:** split escape hatch documented in BOTH the decisions-file contract (curation-decisions.ts header) and wikis/AGENTS.md Local Contracts (including the cluster-record shape and the five classes) — present. No TUI changes — confirmed. B22 struck SCHEDULED in BACKLOG.md. Compliance log shows no unresolved contradictions for Phase 22; the [2026-07-29 12:30] contradiction is user-resolved and the [2026-07-29 14:40] entry records the amendment application.

## 6. Residual observations (none gate-blocking)

1. Gate 22.1's "family beyond the member cap proposes NOTHING" fixture (`a`, `a-region`, `region-a`, `region-of-a`, `a-of-region`) does not actually reach the `>4` branch in `regionClusterProposals` — the cores do not unify (`of-a` vs `a-of`) and the length-1 core is skipped by the ≥3 guard, so the assertion holds vacuously for its stated reason. In practice a region name-form family is capped at 3 members by construction (bare/prefix/suffix), making the >4 guard defensive-only. Test-quality nit, not a behavior gap.
2. The phase-21 zero-false-positive control test does not assert `proposedClusters` is empty (it would be — the fixture carries no region/indicator cluster shapes). A one-line strengthening opportunity for a future hardening pass.
3. A hand-edited sticky cluster record missing `class` re-derives with `class: 1` (materializer.ts line ~1087). Tool-written records always carry the class; the default only covers hand-damaged files and the read-side validator bounds it to 1-5. Defensive, documented here for completeness.

## 7. Verdict

**PHASE 22 VERIFIED.** All 10 gates pass on personally-checked evidence: the tests genuinely pin the requirements, the implementation satisfies them in source (cluster detection/validation/recording, composite assembly/shell/synthesis/preservation/schema/DOX, sticky re-application, split hatch, fingerprint, and the gate-22.10 ingest stage as a faithful mirror with the exactly-right enforcer set), the diff is in scope with Phase 21 changes cleanly separated, my independent runs reproduce the claimed numbers exactly (425 passed + 14 skipped / 28 files key-less; 23/23 phase-22; `npx tsc --noEmit` clean), `.env` is byte-identical post-run (171 bytes, mtime 2026-07-20 00:13:00, no leftover), the already-pooled-into guard is a sound seam-only defensive no-op, all five enumerated pre-existing test touches are intent-preserving, the vision amendments quote the canon basis verbatim with the 2026-07-29 user-ratified marker, and the build matches or soundly extends canon `02` §2/§3/§4.5/§4.6(amended)/§4.8, `05` §2/§6(amended), `07` §2.3–§2.6/§3/§5 with no contradictions. Three low-severity residual observations recorded in §6. LLM cost $0. UAT 22.1/22.2 (live re-ingest: one composite per ratified class, member stubs gone, names findable, Journalist Test on one composite with the rich Layer 1) remain for post-acceptance per the arc's deferred-UAT pattern (compliance-log [2026-07-29 13:05]).

Checked by: Verifier sub-agent (cold verification per MASTER_IMPLEMENTATION_PROMPT.md §1.3)
