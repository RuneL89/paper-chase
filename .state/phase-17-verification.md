# Phase 17 Verification — Entity Graph and Citation Integrity

**Verifier:** independent sub-agent (cold check — Implementer's rationale not assumed)
**Date:** 2026-07-28
**Contract:** `Implementation Plan/PHASE_17_entity_graph_and_citation_integrity.md` §2.1–§2.6, gates 17.1–17.13
**Inputs read in full:** root `AGENTS.md`, the phase doc, vision `02` §2/§3/§4.3/§4.5/§4.8, `04` §3.2 Step 6/§4, `05` §2/§6, `06` §1–§3/§7, `07` §2.5/§2.6, `.state/phase-17-status.json`.

---

## 1. Per-gate results

| Gate | Verdict | Evidence personally checked |
|---|---|---|
| 17.1 Incoming attachment | **PASS** | Test `tests/phase-17.test.ts:208-297` genuinely pins the gate: materialized fixture (2 chunks, distinct sources/pages) asserts beta's two incoming records with verbatim evidence/page/source/pages, alpha's three outgoing (incl. the unknown-object `ghost` record — pre-existing subject-attach preserved), no ghost page and no incoming record naming ghost, self-loop on beta exactly once as outgoing only. Code: mirror-attach loop `src/materializer.ts:705-722` (self-loop `continue` at :708, skip-on-unknown guard at :710-713); field at :154; both aggregate creation sites carry it (:648, :992 — only two exist, verified by grep for `contexts: new Set`). |
| 17.2 Template renders both directions | **PASS** | Test `:302-331` asserts the unchanged outgoing format `- [[beta\|Beta]] — Self Reports [^src1]`, incoming lines exactly `- [[alpha\|Alpha]] — Is Chair Of (incoming) — "Alpha chairs the Beta board" [^src1]`, citation map covering the incoming-only source (`source-two\|4-6` → src2), frontmatter `sources` covering both. Code: render loops `src/pages/entity-page.ts:339-356`; `buildCitationMap` assigns incoming keys LAST (:237-239) so no-incoming pages keep pre-Phase-17 numbering; `buildEntityFrontmatterSources` includes incoming (:269-271). |
| 17.3 Sparse scope (ratified outgoing-only) | **PASS** | Test `:337-383`: ≤2 mentions + no claims + ONLY incoming → `sparse: true`; one outgoing → no sparse field. Code: `isSparseEntity` (`src/pages/entity-page.ts:124-128`) reads `Pick<…, 'mentions'\|'relationships'\|'claims'>` — the incoming array is not even in its type. Ratification recorded: phase doc §2.1 + compliance-log [2026-07-28 16:35]. |
| 17.4 Preservation covers incoming | **PASS** | Test `:388-421`: dropped incoming evidence fails with `droppedRelationships: ['Alpha chairs the Beta board']`; complete page passes. Code: `src/validation/preservation-check.ts:45-54` — same `writtenPage.includes(evidence)` rule as outgoing, data-driven. |
| 17.5 `relatedEntities` computation | **PASS** | Test `:426-494`: exact sorted deduplicated union (obj-a appearing in both a relationship and a claim listed once), self excluded even though present in claim entities, unknown-slug title fallback to the slug, empty form `(none)`. Code: `src/agents/synthesis.ts:144-170` (`buildRelatedEntities`), `:173-183` (`formatRelatedEntities`). |
| 17.6 Prompt slots + byte-confinement | **PASS** | Test `:518-577`: slot and new wikilink rule present exactly once, old rule absent, SHA-256 byte-confinement pin, filled prompt (callLLM spy) renders the slot and the direction labels, LANGUAGE-block removal intact. **Independently recomputed by the Verifier:** removing the slot block + restoring the old rule yields bytes identical to `git show HEAD:` for BOTH prompts — SHA-256 `44d3e0c4…f3417` (synthesis) and `d5045acc…68c4c6` (permissive), exactly the recorded pins. `=== LANGUAGE ===\n{languageDirective}\n` block intact; no CR bytes in either working file. |
| 17.7 Island detection | **PASS** | Test `:607-665`: `islands` lists exactly the zero-outgoing entity + topic pages; `orphaned` semantics unchanged (the island entity with an incoming link is NOT orphaned; the linked entity and topic are); `index.md`/`sources/*`/`documents/*` exempt from both lists. Code: outgoing tally in the existing parse loop `src/validation/link-checker.ts:136-143` (every `[[…]]` occurrence counts, resolvable or not — counted BEFORE resolution); islands filter :165-180 (entities//topics scope + index.md exemption; sources/documents subsumed by scope); report-only wiring `src/validation/index.ts:34-35, 85-88`. |
| 17.8 Frontmatter re-imposition over LLM frontmatter | **PASS** | Test `:751-780` (Date-only fake timers, pinned write time): `updated` = real write time not the constitution example date; `sources` = full deterministic aggregation incl. the incoming record's source; fabricated `confidence` field gone; enforced block deep-equals `writeEntityPage(data)`'s frontmatter; body byte-identical. Code: shared `buildEntityFrontmatter` `src/pages/entity-page.ts:283-308` used by BOTH writer and enforcer; `enforceFrontmatterInMarkdown` :388-408 (overwrites the model block; unparseable model frontmatter returned unchanged). |
| 17.9 Frontmatter creation when absent | **PASS** | Test `:786-843`: enforced page `startsWith('---\n')` and `endsWith(modelPageNoFrontmatter)` — block prepended, body byte-identical; `sparse: true` on the textbook-sparse `adhd-foreningen` analog (incoming-only). Code: the no-leading-`---` path of the same enforcer (:397-407). Ends the 53/119 no-frontmatter class from run 5. |
| 17.10 `## Sources` normalization | **PASS** | Test `:848-879`: full-workspace-path definitions rebuilt as `[^src1]: source-one.pdf, pages 1-3` (basename + deterministic ranges); the `## Sources` section contains no `wikis/test-wiki/raw/`; everything before `## Sources` byte-identical (in-prose markers untouched); a live `checkCitations` on the fixture wiki (real PDFs in `raw/`) reports zero `missingSource` and zero `invalid`. Code: `enforceSourcesSectionInMarkdown` `src/pages/entity-page.ts:410-447` (sorted by citation index, `sourceFileName` basename at :161-163, replace-to-next-`##`/append-when-missing, empty map returns page unchanged). |
| 17.11 Topic pages enforced | **PASS** | Test `:885-957`: topic frontmatter created when absent (title/type/wiki/updated/sources), never `sparse`, definitions normalized, prose preserved. Code: `src/pages/topic-page.ts:49-91` (`buildTopicFrontmatter` — no sparse field), `enforceTopicFrontmatterInMarkdown` :144-158, `enforceTopicSourcesSectionInMarkdown` :160-175 delegating to the entity implementation. No import cycle (entity-page imports no page modules). |
| 17.12 Resume byte-stability | **PASS** | Test `:1059-1155` (hermetic ingest, injected stubs, `poolStaggerMs: 0`, keep-all curation): run 1 synthesizes both pages with enforcement exercised at the write points (asserts `updated`, full `sources`, basename definitions, incoming evidence, `sparse: true` on the object page); run 2 (identical corpus) skips both (`synthesisSkipped === 2`, zero stub calls) and both pages are byte-identical. Code: skip partition `src/commands/ingest.ts:964-985` is upstream of the four write points (:1040-1055, :1089-1104, :1246-1260, :1286-1300) which compose aliases → sparse → frontmatter → sources ONLY on actually-synthesized pages; `canonicalJson` drops `undefined` (`src/state/synthesis-state.ts:127-130`) so no-incoming pages keep pre-Phase-17 fingerprints (`src/materializer.ts:1227`). |
| 17.13 Full-suite regression | **PASS** | Verifier's own runs: `npx tsc --noEmit` → clean (exit 0). Key-less `npx vitest run` with `.env` stashed (171 bytes, mtime 2026-07-20 00:13 — restored afterward byte-identical, md5 `946b4029d38b2f3685e104332d2a1b8f`) and `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` unset: **350 passed + 14 skipped (364) across 23 files (22 passed, 1 skipped), exit 0** — run TWICE (second run's full log: `tests/phase-17.test.ts` 14/14 in 843 ms; `tests/phase-02.test.ts` 12 live gates self-skip). Matches the documented arithmetic exactly: post-navigation-fix baseline 336+14/22 + 14 new Phase 17 tests = 350+14/23. Zero failures; nothing to rationalize. |

Supplementary tests (§2.1 `formatRelationships` direction labels — no-incoming output byte-identical to the pre-Phase-17 form, asserted at `:583-602`; §2.6 `missingFrontmatterSource` — entity/topic scope, basename matching, absent frontmatter covers nothing, document pages unchecked, pre-existing checks untouched, `:963-1012`): both read and confirmed to pin what they claim. Code: `src/agents/synthesis.ts:64-91`; `src/validation/citation-checker.ts:56-76, 109-112, 158-180`.

## 2. Invariant spot-checks (probed hard)

1. **Prompt byte-confinement — VERIFIED independently.** `git diff` on both entity prompts is exactly two changes (the slot block after `Chunk Context:`, the wikilink-rule line). Reconstruction → byte-equal to `git show HEAD:` (hashes above). LANGUAGE block and all pre-existing sections byte-identical. Topic synthesis prompts untouched (git status clean).
2. **Resume byte-stability — VERIFIED.** Enforcement exists only inside the four synthesis write points (full `ingest.ts` diff read — nothing else in the file changed); skip partition is upstream; gate 17.12 proves byte-identity end-to-end; phase-16 suite green in my run.
3. **Sparse scope — VERIFIED.** `isSparseEntity` reads mentions/relationships/claims only; `incomingRelationships` omitted (undefined) when empty so fingerprints are unchanged for no-incoming pages.
4. **`## Sources` normalization — VERIFIED.** Definitions rebuilt basename-form; in-prose markers byte-identical (gate 17.10 asserts the pre-`## Sources` half byte-for-byte); frontmatter `sources` legitimately keep workspace-relative paths per vision `06` §3 (the no-full-path assertions are correctly scoped to the Sources section).
5. **Frontmatter enforcement — VERIFIED.** Creates the block when absent; model extras dropped (only deterministic fields written); `updated` = `new Date().toISOString()` at enforcement time (B2's fix); enforced block deep-equals the writer's own via the shared helper.
6. **`prompts/dox-writer.prompt.txt` CRLF claim — END STATE VERIFIED, mechanism VERIFIED.** Current working file is LF (`file` reports no CRLF; `git diff --numstat` == `git diff -w --numstat` = 9+/1-, and `git diff --ignore-all-space` shows exactly the 2026-07-25 nav-fix content — Start Here + complete `## Pages` catalog, the catalog-completeness/self-link rule, the wiki-level example — matching compliance-log [2026-07-25 18:05]). Gates 7.2b (`tests/phase-07.test.ts:186-206`) and 13.3-DOX (`tests/phase-13.test.ts:549-567`) assert LF-literal blocks (`'=== LANGUAGE ===\n{languageDirective}\n'`) against `readFileSync` contents — a CRLF working copy WOULD fail them key-less, so the claimed baseline failure is mechanically sound. The pre-repair CRLF state itself is unobservable post-hoc (uncommitted); accepted on mechanism + the recorded testimony. The repair is a baseline repair, correctly not credited to Phase 17 gates.
7. **Asset VERSION bump — VERIFIED.** `scripts/launcher-entry.ts:24-30`: `1.0.2 → 1.0.4` with dated comments — the `1.0.2→1.0.3` hunk belongs to the 2026-07-25 nav fix; Phase 17's increment is exactly `1.0.3→1.0.4`, matching the root `AGENTS.md` dist/ rule (prompts are packaged assets) and recorded in that entry.
8. **Pre-existing test edits — all intent-preserving.** `phase-04` (:249, :276, :285, :313): additive required fields `islands`/`missingFrontmatterSource` in literals (type-level necessity; assertions unchanged). `phase-12` gate 12.4 and `phase-14` `entityStubPage`: fixture stubs emit incoming evidence (preservation now requires it) with explanatory comments; gate intents unchanged. `phase-13` gate 13.3: the wikilink-line constant split per prompt kind — the TOPIC assertion retains the pre-Phase-17 line byte-identically; the entity assertion carries the sanctioned §2.2 rule. `phase-07` (7.12), `phase-12` (12.7), `phase-13` (13.1), `phase-15` (15.6), `phase-06` (richStubIndex + gates 6.17a–6.17d): the 2026-07-25 nav-fix set, matching compliance-log [2026-07-25 18:05]; phase-06's 1426+/1232- raw diff collapses to 200+/6- under `-w` (the remainder is line-ending normalization of a mixed-CRLF/CR blob — pre-existing, not Phase 17).

## 3. Diff-audit verdict

`git status` + full `git diff` read. Every change is accounted for:

- **Phase 17 scope (all sanctioned by the phase doc):** `src/materializer.ts`, `src/pages/entity-page.ts`, `src/pages/topic-page.ts`, `src/agents/synthesis.ts`, `src/validation/{preservation-check,link-checker,citation-checker,index}.ts`, `src/commands/ingest.ts` (write points only), `prompts/synthesis.prompt.txt`, `prompts/synthesis-permissive.prompt.txt` (slot-additive), `scripts/launcher-entry.ts` (1.0.3→1.0.4 hunk), `tests/phase-17.test.ts` (new, 14 tests), the four enumerated test accommodations, DOX updates (`AGENTS.md` root, `src/AGENTS.md`, `prompts/AGENTS.md`, `tests/AGENTS.md`, `Implementation Plan/{AGENTS.md, IMPLEMENTATION_PLAN_MASTER_INDEX.md, MASTER_IMPLEMENTATION_PROMPT.md, BACKLOG.md}` — B1/B2/B10/B12 struck FIXED with phase references), `.state/phase-17-status.json`, the phase doc itself, compliance-log entries.
- **Pre-existing (2026-07-25 nav fix, not credited to Phase 17):** `src/dox-writer.ts` (full diff read — 100% nav-fix: `linkTargetOf`, root `## Pages`, `missingCatalogLinks`, catalog plumbing), `prompts/dox-writer.prompt.txt` content, `Project Vision/03_DOX_concept_detailed.md` (§4.3 example), `.state/phase-6-status.json` (gate 6.17 + entry), `tests/phase-06/07/15.test.ts` + the 12.7/13.1 hunks, BACKLOG B3, launcher 1.0.2→1.0.3 hunk, corresponding AGENTS.md hunks, compliance-log [2026-07-25 18:05]. BACKLOG also gains documentation-only audit entries B11/B13/B14/B15 (no code).
- **Untouched as required:** `dist/` (gitignored), `templates/`, `test-pdfs/`, `wikis/`, `src/agents/extractor.ts`, `src/agents/curation.ts`, `src/agents/agents-updater.ts`, DOX Writer/workspace-pass/curation/extractor surfaces (verified via `git status --porcelain` on those paths — empty). `.zcode/` is untracked agent tooling, not project content.

**No out-of-scope changes found.**

## 4. Compliance vs vision

- `02` §4.3 B (bidirectional relationships): **implemented** — object pages now carry and render incoming relationships with links back to the subject.
- `02` §2 (no page is an island): **extended toward canon** — zero-outgoing detection, report-only, matching `07` §2.5's posture (compliance pre-check note c).
- `02` §4.5 (Journalist Test "name two related entities"): **supported** — the relatedEntities slot + first-mention link rule.
- `02` §4.8 (sparse): **implemented per the user-ratified outgoing-only scope** (vision predates bidirectionality; ratification recorded [2026-07-28 16:35]).
- `04` §3.2 Step 6 / §4: **consistent** — aggregation extended; curation unions/remaps incoming records (`05` §6 identity semantics preserved); the synthesis input list is non-exhaustive ("e.g."), so the slot is a lawful extension.
- `05` §2 (universal frontmatter incl. `updated`): **moved toward canon** — complete deterministic frontmatter on every synthesized entity/topic page, real write time.
- `06` §1–§3 (frontmatter source map): **consistent** — workspace-relative `file` + joined page ranges; §2.6's coverage check is file-granular because frontmatter joins ranges per file (status note f).
- `06` §7 (verification workflow): **implemented** — definitions in exactly the documented basename form (`[^src1]: annual-report-2024.pdf, pages 12-15`).
- `07` §2.5/§2.6: **posture preserved** — islands and `missingFrontmatterSource` are report-only warnings; no new gates or fallbacks.

**Observations (non-blocking, recorded for the user):**

1. Phase doc §2.1's "Layer 1 guidance notes the entity may be the object of a relationship" is realized DATA-SIDE — the filled prompt's direction labels (`Incoming (this entity is the OBJECT of these relationships):`, asserted in gate 17.6) — not as added prompt prose. No gate requires prose, and the master prompt's PROMPT DISCIPLINE confines prompt edits to the §2.2 slot + wikilink rule. Accepted interpretation.
2. Status note (g) is accurate: the model is not taught the deterministic srcN assignment; definitions are rebuilt under `buildCitationMap`'s numbering while in-prose markers stay model-placed (phase doc §2.5 sanctions exactly this). A live model whose numbering differs would get resolvable but potentially mis-mapped definitions, silently (validators stay green). A prompt-level alignment is a legitimate follow-up, out of this phase's scope as written.
3. §2.6 coverage is file-granular (definition basename ∈ frontmatter file basenames) — per-key page-range matching is not expressible against the range-joined frontmatter form. Report-only; consistent with `07` §2.5.

## 5. Independent run results (Verifier's own, this session)

- `npx tsc --noEmit` → **clean** (exit 0, no output).
- Key-less `npx vitest run` (`.env` stashed to `.env.bak-verify17` before the run, restored afterward: 171 bytes, mtime 2026-07-20 00:13 preserved, md5 `946b4029d38b2f3685e104332d2a1b8f`; `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` unset): **Test Files 22 passed + 1 skipped (23); Tests 350 passed + 14 skipped (364); exit 0.** Confirmed on two separate runs; the second run's full log shows `tests/phase-17.test.ts` 14/14 passed (843 ms) and the 12 phase-02 live gates self-skipping.
- LLM cost of verification: **$0.00**.

## 6. Final verdict

# PHASE 17 VERIFIED

All 13 gates PASS on independently checked evidence (tests genuinely pin their gates; the implementation satisfies them; the full key-less suite is green with the exact expected arithmetic and zero failures). All invariants hold. No out-of-scope changes; the pre-existing 2026-07-25 nav-fix working-tree set is correctly distinguished and not credited to the phase. Compliance: matches or lawfully extends canon; the one design decision (sparse scope) is user-ratified; the two observations above are disclosed, non-blocking, and out of gate scope. UAT 17.1–17.3 (live re-ingest + run-5 observational) remain for the user at their discretion per the phase doc.
