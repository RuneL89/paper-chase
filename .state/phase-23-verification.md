# Phase 23 Verification — Comparison-Table Articles

**Verifier:** cold-verify sub-agent (no knowledge of Implementer rationale beyond the status file)
**Date:** 2026-07-29
**Contract:** `Implementation Plan/PHASE_23_comparison_articles.md` §2.1–§2.4, gates 23.1–23.7 + 23.6b, including the three 2026-07-29 Objective refinements ((a) subject-entity identity over drifting titles; (b) per-source dated sections for cross-PDF drift; (c) row-value preservation + the deterministic prose bridge)
**Canon checked:** `05_page_types_specification.md` §9; `03_DOX_concept_detailed.md` §3.1; `02_WIKI_concept_detailed.md` §3; `06_citation_and_provenance.md` §1–§3; `07_validation_and_quality.md` §2.1/§5
**Implementer account:** `.state/phase-23-status.json` (claims 8/8 gates, 31/31 phase tests, 456 passed + 14 skipped / 29 files key-less, tsc clean, $0) — every claim independently checked below.

---

## 1. Diff audit (HEAD = dcb60b4; Phases 21/22 uncommitted too — separated, not re-verified)

**Verdict: IN SCOPE.** Phase 21/22 changes (already verified in `.state/phase-21-verification.md` / `phase-22-verification.md`): `src/agents/pre-merge.ts`, `src/state/curation-decisions.ts`, `src/pages/composite-page.ts` + composite prompts (NEW), curation prompts' PROPOSED PAIRS/CLUSTERS, pair/cluster parts of `curation.ts`/`materializer.ts`/`curation-report.ts`, vision amendments `02` §4.6 / `05` §6, `Implementation Plan/*` arc docs, launcher 1.0.5→1.0.7 parts, `tests/phase-{21,22}.test.ts` + status/verification files, `.zcode/` (session noise).

Phase 23 changes (verified here):

| Path | Verdict |
|---|---|
| `prompts/extractor.prompt.txt` — slot-additive `tables` output (personally diffed: tables JSON example + 3 field rules + the top-level field-list and empty-chunk edits only) | §2.1 — in scope |
| `src/validation/extractor-schema.ts` — tables validation + additive `warnings` channel | §2.1 — in scope |
| `src/agents/extractor.ts` — ExtractorTable, optional `tables`, slug normalization, always-filled array | §2.1 (pass-through only) — in scope |
| `src/pages/comparison-page.ts` (NEW, 405 lines) | §2.2 — in scope |
| `src/materializer.ts` — comparison collection/assembly/write loop, rewrite scope, structural-change log, `MaterializeResult.comparisonPages` | §2.2 — in scope |
| `prompts/comparison.prompt.txt` (NEW, 57 lines), `src/agents/synthesis.ts` (comparison builders + 2 writers) | §2.3 — in scope |
| `src/commands/ingest.ts` (fourth pooled stage + counters + row-value feedback), `src/state/{synthesis-report,conflicts,synthesis-state}.ts` (comparison unions/fingerprint) | §2.3/§2.4 — in scope |
| `src/validation/preservation-check.ts` (`checkComparisonPreservation`), `link-checker.ts` (island scope), `schema-validator.ts` (known type), `src/dox-writer.ts` (conditional comparisons chain) | §2.4 — in scope |
| `templates/AGENTS.md` (comparison type documented: folder contract line 42, type table line 62, dedicated paragraph line 65) | §2.2/gate per `05` §9 — in scope |
| `scripts/launcher-entry.ts` VERSION 1.0.7→1.0.8 + dated comment | exactly one increment over Phase 22 — in scope |
| `tests/phase-23.test.ts` (NEW, 1264 lines, 31 tests); enumerated touches `tests/phase-13.test.ts` (13.1 ceiling-site count 6→8), `tests/phase-15.test.ts` (15.6 runPool count 3→4) | gates — in scope (touches verified intent-preserving below) |
| `AGENTS.md`, `src/AGENTS.md`, `prompts/AGENTS.md`, `tests/AGENTS.md`, `wikis/AGENTS.md` (comparisons/ folder + UAT 23.2 posture documented), `.state/compliance-log.md` [2026-07-29 16:55], `.state/phase-23-status.json` | DOX pass + bookkeeping — in scope |

**Untouched as required:** `dist/`, `test-pdfs/`, `.env` — `git diff --stat` empty for all three; `.env` 171 bytes, mtime 2026-07-20 00:13:00.000; **no `.env.bak-*` leftovers** (neither the Implementer's `.env.bak-phase23` nor my `.env.bak-verify23`). No TUI files modified.

## 2. Independent runs (personally executed)

| Run | Expected | Actual | Verdict |
|---|---|---|---|
| `npx tsc --noEmit` | clean | **clean** (exit 0, no output) | PASS |
| Full key-less suite (`.env` → `.env.bak-verify23`, `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` unset) `npx vitest run` | 456 passed + 14 skipped / 29 files | **456 passed + 14 skipped / 29 files (28 passed, 1 skipped)** (33.36s) | PASS |
| `npx vitest run tests/phase-23.test.ts` | 31 passed | **31 passed** (3.65s) | PASS |
| `.env` restored | byte-identical | 171 bytes + mtime 2026-07-20 00:13:00.000 post-restore; no `.env.bak-verify23` left behind; `git status` clean of env entries | PASS |

Baseline arithmetic checks: 425 (Phase 22 verified baseline) + 31 (phase-23) = 456; 28 + 1 = 29 files. Zero unenumerated regressions.

## 3. Per-gate cold check

**Gate 23.1 (extractor schema: tables accepted/rejected correctly, warnings channel) — PASS.** 7 tests genuinely pin the requirement and the source produces every pinned behavior: well-formed tables pass with `issues: []` and `warnings: []`; missing title → `tables[0]: title must be a non-empty string` (extractor-schema.ts line 265); missing page → `page must be a number` (line 75 via `validatePage`); empty markdown → line 269; off-range page 21 in chunk 16-20 → the exact pinned string (line 79); unknown entity slugs warn-but-pass — one warning naming `odense` and `tables[0].entities[1]`, `valid` stays true (lines 286–290); absent `tables` stays valid (line 254 `!== undefined` guard — pre-Phase-23 JSON compatibility); non-array `tables` rejected (line 256). The additive `warnings` channel **never affects `valid`** (`valid: issues.length === 0`, line 298) and **never feeds the reask loop** — the extractor's validate closure only consults `validation.valid`/`validation.issues` (extractor.ts lines 409–413). Slug normalization covers `table.subject` + `table.entities` with input-language transliteration (`Søren Møller` → `soeren-moeller` da; byte-identical en default) (lines 317–326); the agent always fills `[]` when the model omits the array (line 419), proven by the callLLM-stub test.

**Gate 23.2 (assembly identity rule) — PASS.** 5 tests pin all four identity legs, and I confirmed each in materializer.ts lines 1935–2014: (a) same title from two sources → ONE page, two dated sections, the in-chunk duplicate table deduped (`source|page|title|markdown` seenKeys); (b) **DRIFT** — same subject, different caption AND different columns across sources → ONE page at the canonical subject slug with both column layouts verbatim (`| 2022/23 (%) | 95% CI | 2021/22 (%) |` and `| 2023/24 (%) | 2022/23 (%) | 2021/22 (%) |` both present) and both captions unioned into `aliases`; (c) curation-remap reconciliation — the 2024 extraction's old subject slug `indikator-2` merges into the canonical slug and its table lands on the SAME page (`remapTableSlug` applies `entitySlugRemap` at lines 1954/1963 BEFORE the `slugToTitle` lookup at line 1964 — the lookup covers entities and composite into-slugs alike); (d) unresolvable subject → `slugify(tableTitle, input-language)` fallback (line 1965, page at `comparisons/cost-per-region-2023.md`); empty/absent tables → `comparisonPages: []` and NO `comparisons/` directory on disk (`folderStructure.add('comparisons')` only when the map is non-empty, lines 2009–2014).

**Gate 23.3 (deterministic shell) — PASS.** The test pins byte-for-byte table preservation inside the dated sections, section ordering, complete frontmatter (`type: comparison`, subject title, real ISO `updated` via fake timers, aggregated `sources` exactly `[{file: 2023, pages: '16, 16-20'}, {file: 2024, pages: '15'}]`, aliases union, `'sparse' in data === false`), per-table entity links (known slug → `[[danmark|Danmark]]` pipe form; own subject never a self-link; unknown `odense` plain text, never a broken link), and `## Sources` in basename form consistent with `buildComparisonCitationMap` (src1 = 2023|16, src2 = 2024|15, src3 = 2023|16-20 bridge range), with per-section `Summary: … [^srcN]` citations. Source confirms every rule: comparison-page.ts `writeComparisonPage` (251–298), `buildComparisonCitationMap` (128–149 — tables cite `file|<table page>`, bridge claims cite `file|<chunk pages>`), `buildComparisonFrontmatterSources` (156–177), `comparisonEntityLink` (208–217), `escapeYamlString` house style (100–105).

**Gate 23.4 (synthesis values + generic slot-additive prompt) — PASS.** 3 unit + 2 stage tests. The prompt test pins the Phase 7 `=== LANGUAGE ===\n{languageDirective}` block, `{comparisonTitle}`/`{tables}`/`{relatedEntities}`/`{citationMap}` slots, the `=== CITATION KEYS ===` section, the wikilink rule and citation-keys rule BYTE-EQUAL to the Phase 17/18 entity baselines, and genericity — I personally read `prompts/comparison.prompt.txt`: no `rkkp`/`registr`/`indicator`/`hospital`/`atrieflimren`/`ekkokardiografi`/`danmark`/`region` anywhere; the analysis vocabulary is generic (leaders/trailers, targets met/missed, trends across dated sections with the honest "a single dated section supports no trend claim" rule, outliers). The callLLM-spy test pins the filled prompt: `Subject:` title, both dated sections with markdown + dimensions line, relatedEntities minus the subject itself (`- danmark — Danmark` present, `- indikator-2-ekkokardiografi —` absent), citation map in assignment order, LANGUAGE block removed byte-identically for en/en, constitution appended. Source: synthesis.ts `formatComparisonTables` (409–427), `buildComparisonRelatedEntities` (436–456 — sorted deduped table+bridge union minus the page slug; unknown slug title falls back to the slug itself, pinned), `buildComparisonSynthesisValues` (458–470), the two writers (680–733 — ONE prompt file per the phase doc's file list, shared `SYNTHESIS_MAX_TOKENS`, distinct `synthesis`/`permissive-synthesis` callTypes, feedback appended to the same prompt).

**Gate 23.5 (row-value preservation) — PASS.** 6 unit + 2 stage tests genuinely pin the requirement, and the reformatted-pass leg proves this is NOT byte-substring (the reflowed `|Area|Target met|…` table would fail a byte check legitimately): `comparisonRowValues` parses header/separator away and keeps exact per-row numbers (`['12.876','16.252','79,2','78,6','79,8','76,8']` — CI ranges split, thousand-dots intact; comparison-page.ts 377–405); the deterministic shell passes; a deleted row fails naming the row (`row "Syddanmark": the row itself is missing`); the altered-number fixture (82,2 → 82,9) fails naming the exact original `report-2023.pdf, p. 16 row "Syddanmark": value "82,2"`; dropped citations and off-map `[^src9]` fail at entity/composite parity. Source: preservation-check.ts 296–342 — section-scoped by the `## Table:` heading + source basename + exact `p. <page>\b` (p. 16 never matches p. 160), whole-page fallback when the heading was renamed, so a value repeated in ANOTHER section can never mask a drop; `droppedRowValues` feeds the reask verbatim. Stage tests pin the loop: strict failure → 3 attempts, attempt 2's `=== CORRECTION REQUIRED ===` block carries `Dropped table row value (restore this exact value): report-2023.pdf, p. 16 row "Syddanmark": value "82,2"` (ingest.ts 487–489), permissive pass wins; double failure keeps the deterministic shell on disk with both tables verbatim, `finalMode: 'structured-template'`, conflict logged with `pageType: 'comparison'` (droppedRowValues ride the conflicts `dropped.claims` channel, conflicts.ts 112–117).

**Gate 23.6b (deterministic prose bridge) — PASS.** 3 tests: the bridge lists EXACTLY the claims sharing the table's entities (the performance claim with `[syddanmark, subject]` included; the entity-less governance claim excluded — `Noter steg` never appears), rendering `"<claim>" — see [[performance|Performance]] ([[syddanmark|Syddanmark]]) [^srcN]`; the honest empty form `- No prose claims in the corpus reference this comparison's subjects yet.` when nothing shares; the enforcer inserts a missing section before `## Sources` and replaces fabricated content up to the next `##` heading. Source: materializer.ts 2348–2370 — the bridge is computed from the CURATED `topicMap` claims (intersection `claim.entities ∩ tableEntities`, deduped by claim key), so a dropped topic's claims can never produce a dead topic link; comparison-page.ts `buildComparisonBridgeLines` (226–240) and `enforceComparisonBridgeInMarkdown` (334–354) shared by writer and write-point.

**Gate 23.6 (validators + DOX) — PASS.** 2 tests. Full-pipeline leg: `comparisons/index.md` catalogs the page by its on-disk title; the wiki root's deterministic children re-imposition includes `comparisons/index.md`; the root carries `- [[comparisons/index|Comparisons]]` and `- Comparison pages: 1`; `validateWiki` reports zero broken links, zero schema invalids, zero citation invalids/missing sources, and the comparison page is neither orphaned nor an island. Island leg: a comparison page with zero outgoing links IS an island; one with links is not. Source: dox-writer.ts 335–340 (children), 371–375 (statistic), 438–442 (catalog) — all three CONDITIONAL on the folder having content, so table-less wikis stay byte-identical (the UAT 23.2 posture); link-checker.ts 295–311 (`comparisons/` joins the island scope — comparison pages are content pages); schema-validator.ts line 15 (`comparison` joins KNOWN_TYPES); the structural-change log records the new folder with the comparison-page count + table entities (materializer.ts 2550–2568).

**Gate 23.7 (full key-less suite + tsc) — PASS.** My independent numbers in §2 (not the Implementer's): 456 passed + 14 skipped across 29 files; `npx tsc --noEmit` clean; phase-23 file alone 31/31.

## 4. High-value probes mandated by the brief

(a) **Identity rule — CONFIRMED.** The DRIFT fixture (same subject, different title AND columns) lands on ONE page via the canonical subject slug with per-source dated sections and drift aliases; the remap leg reconciles a renamed subject through curation (`entitySlugRemap` before `slugToTitle`); title-slug only as fallback; empty tables produce no artifacts. Tests read and personally re-run (31/31).

(b) **Row-value preservation — CONFIRMED.** Altered/dropped values fail with the exact original named; reformatted-but-value-complete passes; section scoping prevents cross-section masking (the 82,2-vs-82,9 fixture); byte-substring would have failed the reflow leg legitimately.

(c) **Prose bridge — CONFIRMED.** Exactly the sharing claims from live curated topics, deterministic render + honest empty form, enforcer re-imposes over model output (insert/replace), never trusted from the model.

(d) **Comparison stage mirrors the composite stage — CONFIRMED line-by-line** (ingest.ts 1747–1938 vs. the Phase-22-verified composite stage): shared `trySynthesisMode` reask (maxAttempts 3, `preservationFeedbackErrors` gains the row-value line), write-point enforcers exactly `enforceComparisonFrontmatterInMarkdown` + `enforceComparisonBridgeInMarkdown` + `enforceSourcesSectionInMarkdown` over `buildComparisonCitationMap` with `repairPageLinks` outermost and NO sparse enforcer (correct — comparisons are never sparse; the comparison frontmatter enforcer already covers the aliases union), report entries `pageType: 'comparison'` with the strict/permissive/template/transport vocabulary, `runPool` cap `SYNTHESIS_POOL_SIZE` + stagger, per-stage `makeOutageDetector`, `recordDetectorTransportFailure`/`recordDetectorSuccess`, `chainPhase` transport-fallback entry, per-page `recordSynthesisPage` checkpoint driven by `pageDataHash(ComparisonPageData)` (slugToTitle excluded as global context), `partitionStage`/`reconstructedSkipEntry` widened, additive counters folded into `formatIngestSummary` (554–561). Run-2 resume test: zero comparison/entity/topic LLM calls, page byte-identical, `synthesisComparisonsSkipped: 1`, reconstructed strict-synthesis report entry.

(e) **Extractor prompt generic — CONFIRMED.** Wording exactly per phase doc §2.1 ("structured tables comparing multiple subjects (e.g., regions, organizations, periods) against shared measures"); the JSON example uses the prompt's pre-existing acme-corp/revenue world; the reconstruction note (structure the extractor's, values the PDF's verbatim) present; the diff is slot-additive (personally diffed — only the tables example, three field rules, and the two field-list edits); LF preserved (0 CR bytes in both prompts).

(f) **Additive `warnings` channel — CONFIRMED.** Never affects `valid` (extractor-schema.ts 298), never consulted by the reask closure (extractor.ts 409–413), absent `tables` stays valid (254) so every pre-Phase-23 fixture type-checks untouched.

**Enumerated pre-existing touches — intent-preserving.** phase-13 gate 13.1 count 6→8 counts `maxTokens: SYNTHESIS_MAX_TOKENS` sites — I confirmed both comparison writers use the same shared constant (the single-constant intent and the no-8192-literal assertion unchanged); phase-15 gate 15.6 count 3→4 counts `await runPool(` sites — the comparison stage pools at the same fixed cap (the ratified-sequential-stages assertions untouched).

**Design decisions reviewed:** (a) basename collision `comparisons/<subject-slug>.md` vs the entity page — the link checker's alphabetical first-wins map resolves bare-slug links to the comparison page; in thin wikis the entity page can be flagged orphaned (report-only warning; observed island/orphan warnings in my own phase-23 run output are this class — links still resolve, both pages on-topic). Documented, acceptable for v1; path-scoped linking a possible future refinement. (b) ONE comparison prompt reused by the permissive leg — the phase doc's §2.3 file list sanctions one file; its Layer-2 contract IS the row-value contract; distinct callTypes preserve the report vocabulary. Sound. (c) `tables` optional at the schema level with the agent always filling `[]` — pre-Phase-23 JSON compatibility, no fixture churn. Sound. (d) table sections cite `file|<table page>` (more precise than the chunk range), bridge claims cite chunk ranges (entity convention), frontmatter aggregates both — matches `06` §3. Sound. (e) bridge reads only live curated topics. Sound. (f) `escapeYamlString` house style — the phase-22 precedent; the DOX catalog test derives its expectation from disk. Sound. (g) section-scoped row-value checking with whole-page fallback. Sound (see residual 1).

## 5. Compliance against canon

- **`05` §9 (custom page types) — MATCH/EXTEND.** Items 1–3 satisfied: the Extractor includes `tables` in the JSON output; the Materializer creates pages with `type: comparison`; the DOX Writer documents the type in the folder-level `comparisons/index.md` catalog and the root statistics. Item 4 (`.state/proposals/page-types.json`) — that mechanism has never existed project-wide (no code references it; `composite` did not add it either); the established equivalent — the structural-changes log's `knownPageTypes` tracker plus the new-folder entry with the comparison-page count and table entities (materializer.ts 2540–2568) — covers the same after-the-fact-review intent. Consistent with existing practice, not a contradiction. The type is additionally documented in the constitution template `templates/AGENTS.md` (folder contract, page-type table, dedicated paragraph) per the phase doc's requirement.
- **`03` §3.1 (top-level folders) — EXTEND (ratified).** The `comparisons/` extension is limited to the folder list, ratified at compliance-log [2026-07-29 12:30] note (b) and applied at [2026-07-29 16:55]; §3.2/§3.3 LLM sub-folder rules are untouched (the folder is deterministic, never LLM-proposed, holds no sub-folders); the folder joins the structure only when content exists (no ghost folders in table-less wikis). Durable documentation lives in `templates/AGENTS.md` and `wikis/AGENTS.md`.
- **`02` §3 (two-layer pages) — MATCH/EXTEND.** Comparison pages carry the same two layers: Layer 1 synthesis in the output language (the Phase 7 `{languageDirective}` slot), Layer 2 verbatim dated table sections in the source language, never translated — enforced by the row-value preservation check and the prompt's Layer-2 contract ("every row label and every value of the markdown table intact … never alter, drop, add, or reorder").
- **`06` §1–§3 (citation + provenance) — MATCH.** Every table value is traceable: each dated section cites its source at the table's own page (`file|<page>`) with per-section summary citations; bridge claims cite their chunk ranges; `[^srcN]` format per §2; frontmatter `sources` aggregate per-file with comma-separated page refs per §3.1; basename `## Sources` definitions resolve through the citation checker (gate 23.6's clean citation summary). Verbatim provenance of table values is enforced by gate 23.5 (the values are the PDF's; only the reconstructed structure is the extractor's).
- **`07` §2.1 (schema validation) — MATCH/EXTEND.** Tables join the extraction schema with `title`/`page`-in-range/`markdown` required; the relationships-style "references must resolve" rule is deliberately relaxed to warn-but-pass for table entities — explicitly sanctioned by phase doc §2.1 ("warning, not rejection — tables may mention unextracted names") and logged at [2026-07-29 16:55]. An extension with recorded grounds, not a contradiction.
- **`07` §5 (error handling) — MATCH.** Bounded reask unchanged (content defects ≤3 attempts with the validator's exact findings — the row-value feedback line); deterministic failures never retried; transient transport failures fall back per page to the deterministic shell with the outage detector armed (5-consecutive / >10% thresholds, shared machinery); HTTP 4xx aborts; warnings never enter the loop. LLM cost $0.00 for the whole phase.

## 6. Residual observations (none gate-blocking)

1. Row-value checking is section-scoped but substring-based WITHIN a section: a value duplicated across two rows of the SAME section could mask one row's drop. Same limitation class as the entity/composite verbatim-substring checks (accepted project-wide since Phase 5); cross-section masking is prevented by the section scoping. Low severity.
2. Design decision (a)'s basename collision means bare-slug wikilinks resolve to the comparison page rather than the same-named entity page (alphabetical first-wins), and the entity page can be flagged orphaned in thin wikis — report-only, links resolve. Documented by the Implementer; verified consistent with my own run output.
3. `05` §9 item 4's `.state/proposals/page-types.json` remains unimplemented project-wide (pre-existing); the structural-changes log covers the type. Noted for completeness, not a Phase 23 gap.
4. The dropped-citation check's includes-semantics mean a definition line alone keeps a key "present" (an in-prose-only drop with a surviving definition would pass) — deliberate entity/composite parity, pinned by the gate-23.5 parity test itself.

## 7. Verdict

**PHASE 23 VERIFIED.** All 8 gates pass on personally-checked evidence: the 31 tests genuinely pin the requirements; the implementation satisfies them in source (tables schema + warnings channel, subject-identity assembly incl. DRIFT and curation-remap reconciliation, verbatim deterministic shell, generic slot-additive synthesis, section-scoped row-value preservation, the deterministic prose bridge, validators/DOX conditionality, and the fourth ingest stage as a faithful mirror of the 22.10 composite stage with the exactly-right enforcer set); the diff is in scope with Phase 21/22 changes cleanly separated; my independent runs reproduce the claimed numbers exactly (**456 passed + 14 skipped / 29 files key-less; 31/31 phase-23; `npx tsc --noEmit` clean**); `.env` is byte-identical post-run (171 bytes, mtime 2026-07-20 00:13:00, no `.env.bak-*` leftover); both enumerated pre-existing test touches are intent-preserving; all seven recorded design decisions are sound; and the build matches or soundly extends canon `05` §9, `03` §3.1 (ratified extension limited to the folder list), `02` §3, `06` §1–§3, `07` §2.1/§5 with no contradictions. Four low-severity residual observations recorded in §6. LLM cost $0. UAT 23.1/23.2 (live re-ingest: indicator tables as independent `comparisons/` articles with both years' verbatim tables + a synthesized reading; no ghost artifacts in table-less wikis) remain for post-acceptance per the arc's deferred-UAT pattern (compliance-log [2026-07-29 13:05]).

Checked by: Verifier sub-agent (cold verification per MASTER_IMPLEMENTATION_PROMPT.md §1.3)
