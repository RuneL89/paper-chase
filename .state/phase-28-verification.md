# Phase 28 — Independent Verification Report

**Verifier:** independent cold-check sub-agent (not involved in the implementation)
**Date:** 2026-09-07
**Subject:** `Implementation Plan/PHASE_28_fine_grained_crash_resume.md` (gates 28.1–28.9) against vision `04_orchestration_detailed.md` §1 (2026-09-07 rider) + Step 11 extension, `01` §4, `07` §2.3/§5, root `AGENTS.md` 2026-09-07 entries, `BACKLOG.md` B25 removal.
**Method:** spec read first; implementation read cold; full key-less suite re-run independently (`.env` stashed to `.env.keyless-tmp`, restored byte-identically at 171 bytes after); `tsc --noEmit`; adversarial per-gate test review. No source or test file was modified.

## Machine checks (re-run independently)

- **Full key-less suite:** `Test Files 41 passed | 1 skipped (42)` — `Tests 613 passed | 16 skipped (629)`. Matches the phase doc's claim exactly (613+16/42, e2e skipped). Duration ~155 s.
- **Focused re-run** (`tests/phase-28.test.ts` + `tests/phase-16.test.ts` + `tests/tui/ingest-screen.test.tsx`): 47 passed / 47, 3 files.
- **`tsc --noEmit`:** exit 0.
- **`.env` hygiene:** existed (171 bytes), stashed for the key-less run, restored and re-verified at 171 bytes.

## Per-gate verdicts

| Gate | Verdict | Evidence |
|---|---|---|
| 28.1 Checkpoint skip | PASS | Test seeds 3 valid checkpoints via leg 1, then leg 2's recording stub shows `calls === []` while `result.extractions` still counts all 3 chunks (entities=4 each) from the stored JSON; exactly one resume line with the ratified wording; per-PDF checkpoint re-lands. Code: the guard (`readValidExtraction`) runs at `src/commands/ingest.ts:1313`, BEFORE the `extractChunkFn` seam (line 1333), so it binds stub and real path alike; skip branch (1318–1328) pushes the report entry and accumulates metrics from the stored JSON. |
| 28.2 Guard conservatism | PASS | Five variants (corrupt JSON / schema-invalid / envelope hash-mismatch / pages-mismatch / legacy no-envelope) each force re-extraction of the tampered chunk, and each re-written file is a valid envelope-carrying checkpoint. `readValidExtraction` (`src/state/extraction-checkpoints.ts`) returns null on ENOENT, parse failure, non-object, absent envelope, sha256/pages mismatch, and `validateExtractorResult` failure; it imports no LLM surface (fs/path + the deterministic validator only — the `07` §2.3 reuse law). |
| 28.3 Envelope write + first-run identity | PASS | Every extraction JSON has `_provenance` as its FIRST key with sha256/pages/sourceFile from the document-page frontmatter and a parseable timestamp (`src/commands/extract-chunk.ts:91-101` writes `{ _provenance, ...result }`; `ChunkExtraction.result` stays the bare `ExtractorResult`). No `Resuming` line on a healthy run; `.state/pdf-progress.json` AND the `.state/pdf-progress/` cache dir are gone after the run; two pinned fresh runs produce byte-equal `documents/`, `sources/`, `.state/extracted/`, `ingestion.json`, `rolling-memory.json`. See finding F5 on the compositional nature of the pre-phase byte-identity proof. |
| 28.4 Crash → chunk-level resume | PASS | Leg 1 dies on chunk 2 (throwing stub = worker-death shape) leaving chunk 1's checkpoint and no `ingestion.json` record; leg 2 calls exactly `['report-part-002','report-part-003']` — zero calls for the checkpointed chunk — with exactly one `Resuming report.pdf — 1/3 chunks already extracted, skipping...` line and the checkpoint landing. Correct final tree is anchored by amended gate 16.11's whole-tree byte-comparison against an uninterrupted run. |
| 28.5 Changed-PDF cleanup | PASS | A fresh-hash checkpoint (simulating a crashed changed-PDF attempt) survives the cleanup BYTE-FOR-BYTE and is consumed without re-extraction; all old-hash JSONs are re-extracted; a final sweep asserts every surviving extraction JSON's envelope sha256 === the new hash. Code (`ingest.ts:1221-1230`) deletes an old chunk JSON only when `readExtractionProvenanceSha256` returns anything ≠ the new hash (absent/corrupt/missing-envelope → null → deleted: conservative). |
| 28.6 Stage markers | PASS | After a mid-synthesis crash the entry survives with `extraction/materialize/curation === true`, the journal done-list, and the materialize cache beside it; the retry shows zero extraction calls, `materialize` spy never called, ZERO curation calls (a non-vacuous anchor — curation runs inside materialize, so the skip is proven even if the spy were inert), synthesis of only the un-done page, the amendment snapshot re-taken from disk (`snapshotAmendmentPages()` runs before the skip decision at `ingest.ts:1369`), and entry + cache removed when the `ingestion.json` checkpoint lands (`removePdfProgressEntry` at `ingest.ts:1438`). `extractedSetHash` covers every `.state/extracted/*.json`, sorted, basename + per-file hash (`pdf-progress.ts:221-240`); all writes funnel through `enqueueSerializedWrite`. |
| 28.7 Synthesis journal | PASS | The journal marks entity-5 done after the crash; the test then tampers the checkpoint JSON (entity-5 gains a mention — aggregate fingerprint changes, envelope stays valid) and the retry RE-SYNTHESIZES entity-5 (via the Phase 26 amendment path, asserted) alongside never-done entity-9, while the other 8 done pages skip. The fingerprint-mismatch override is structural: the journal is WRITE-only in `ingest.ts` (grep-verified — nothing reads `stages.synthesis` to force a skip); the per-page `synthesis-state.json` records remain the only skip law, exactly the rider's "records remain authoritative" clause. Journal is initialized per stage from `partitionStage`'s queue, appended AFTER each per-page record write (e.g. `ingest.ts:2324→2327`), closed at stage exhaustion (2957), and never touched on the null-label repair path (`journalSlug` null-guard, 1629). |
| 28.8 Crash telemetry | PASS | (a) `tailLines` filters `LLM Call \| ` lines BEFORE the last-N cut (40 cost lines + 2 real lines → tail is exactly the 2 real lines); records round-trip additive `fatalError`/`fatalStack` and stay absent on a hard crash (27.6 shape preserved). (b) Scripted-spawn conductor test: fatal event + cost-noise stderr → crash record carries the error/stack and a cost-free `stderrTail`. (c) REAL worker subprocess (`execFile` the `ingest-worker` subcommand against a corrupted `ingestion.json`): exit 1, terminal stdout line is the `fatal` event, and the SAME error text appears on stderr — the `src/cli.ts:241` `console.error(error)` mirror proven end-to-end. Screen test: the crash panel renders `Fatal error: …` above the tail (`ingest-screen.tsx:630-641`; ordering verified in source — see F6). |
| 28.9 Doc gates | PASS | Vision `04` §1 fine-grained-crash-resume rider (2026-09-07, user-ratified) present at line 23; Step 11 fine-grained extension present at line 217; Step 9 authority clause intact. Root `AGENTS.md`: the 2026-09-07 preference entry (line 118) and the dist-chain 1.0.31→1.0.32 entry (line 131) both present and matching the code. `scripts/launcher-entry.ts` VERSION = `1.0.32`. `BACKLOG.md`: B25 removed from the queue; the header note records the scheduling and the one remaining mention is B26's historical cross-reference ("former B25") — honest removal. `.state/phase-16-status.json` carries the deviation note for the superseded per-PDF-atomicity comment. Compliance log has the 2026-09-07 pre-check + closeout entries. Phase doc present with the amended-16.11 expectation (`leg2ExtractionCalls === []` — zero re-extraction, chunks resume) verified green in the suite. |

## Compliance findings

No contradiction with the vision docs found. Specifically checked:

- **Data is never skipped (rider law):** every guard failure path returns null → re-extract; the changed-PDF cleanup is envelope-aware but conservative (unreadable/stale → deleted); a fingerprint-mismatched page re-synthesizes despite a journal done-entry (28.7).
- **Existing law authoritative:** `ingestion.json` (Step 11) and `synthesis-state.json` (Step 9) write points untouched; the progress entry is removed at the PDF's checkpoint and the synthesis journal is observer-only (never read to force a skip). This is the safe reading of the rider's "records remain the authoritative durable skip law" and is documented in the status deviations.
- **Byte-identical normal operation (`01` §4):** resume line fires only when >0 valid checkpoints exist (recovery path); scaffolding (state file + cache dir) is removed at each PDF's checkpoint, and the whole-tree byte-equality of amended gate 16.11 (interrupted vs uninterrupted run) passed in my re-run.
- **Finalize boundary:** resume machinery lives inside the per-PDF loop; no changes to the deferred tail.
- **`07` §2.3/§5:** the checkpoint guard reuses `validateExtractorResult` (the reask loop's own deterministic validator); zero new LLM surface; `$0.00` spend claim consistent with all gates being stub-injected, script-spawned, or corrupted-state subprocesses.

## Adversarial findings (non-blocking)

- **F1 (spec drift, cosmetic):** the phase doc's refinement outcome 5 still shows the DRAFT resume-line wording (`Resuming <file> from chunk N/M (earlier chunks already extracted)…`) while the implemented/ratified wording is `Resuming <file> — N/M chunks already extracted, skipping...` (matches the root `AGENTS.md` 2026-09-07 entry — the binding law). The change is recorded honestly in `.state/phase-28-status.json` deviations ("Resume-line wording refined during gating"), but the phase doc body was not amended to the final wording.
- **F2 (test coverage gap, minor):** gate 28.1's fixture extraction carries zero relationships/claims, so the metrics-accumulation lines for the skip path (`ingest.ts:1324-1328` — `relationshipsExtracted`/`claimsExtracted`/`claimsByType` from the stored JSON) execute but are not pinned with non-zero values; only `result.extractions` counts are asserted. Verified correct by inspection (the lines mirror the fresh-extraction path exactly); a regression there would not fail any test.
- **F3 (test coverage gap, minor):** the conductor's CrashPanelState fatal-field spread (`ingest-conductor.ts:~426-428`) is never exercised end-to-end — 28.8b's crash auto-retries successfully so the panel never surfaces, and the screen test injects `CrashPanelState` directly. The crash-RECORD spread (`:~399-401`) IS asserted by 28.8b, and TypeScript pins the panel shape.
- **F4 (UAT wording nuance):** UAT 28.1 says "the chunk counter continues at the first missing chunk (not 1/M)"; the implementation's chunk loop still emits `Chunk N/M` lines from 1 for checkpointed chunks (document pages are rewritten unconditionally — deliberate, keeps the loop uniform), and the truthful position is carried by the `Resuming … — N/M chunks already extracted` line. The user accepted UAT 28.1 on 2026-09-07; noted for the record only.
- **F5 (proof-shape note):** gate 28.3's "byte-identical to pre-phase behavior" is proven compositionally (no resume lines + no scaffolding left + pinned two-run determinism + the pre-existing byte-asserting suites green), not against a literal pre-phase snapshot fixture — and cannot be, since the envelope is itself an intentional additive byte change to the extraction JSONs. Recorded in the status deviations; judged honest and sufficient.
- **F6 (assertion strength, cosmetic):** the ingest-screen Phase 28 test asserts the presence of both the `Fatal error:` row and the tail, not their relative order; the ordering (fatalError above the tail) is structural in `ingest-screen.tsx:630-641` and was verified by reading.

None of F1–F6 weakens a gate's claim materially; F2/F3 are the only ones worth a future test-hardening pass.

## Final verdict

**PASS.** Gates 28.1–28.9 pass on independent re-run (613 passed + 16 skipped / 42 files key-less; tsc clean), the implementation matches the 2026-09-07 rider and Step 11 extension with no vision contradictions, the phase-16 amendment and its deviation note are in place, and the doc/dist-chain entries are present and accurate. No blockers. The standing exception listed in the phase doc header (independent Verifier cold-check) is satisfied by this report.
