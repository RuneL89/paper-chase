## Plan: rename `rkkp-danibd-new` → `rkkp-danibd` and clean up workspace index

### 1. Rename the wiki folder
- `dist/wikis/rkkp-danibd-new` → `dist/wikis/rkkp-danibd`

### 2. Bulk replace the old slug inside the moved folder
- Replace `rkkp-danibd-new` with `rkkp-danibd` in all `.md` files (frontmatter `wiki:`, source `file:` PDF paths, `AGENTS.md`, `.state/proposed-agents.md`).
- Replace the slug in `.state/validation-report.json` and `.state/llm-calls.json`.

### 3. Update the wiki title to match the slug
- Root `index.md`: title `Rkkp Danibd New` → `Rkkp Danibd`, alias updated accordingly.

### 4. Clean up `dist/wikis/index-of-indexes.md`
- Change `rkkp-danibd-new/index.md` → `rkkp-danibd/index.md`.
- Remove `rkkp-adhd`, `rkkp-afdk`, and `rkkp-akdb` entries (deleted wikis).
- Update wikilink and prose to reflect only `rkkp-danibd`.

### 5. Refresh state and validation deterministically
- Run `chase ingest rkkp-danibd --no-extract --no-dox-llm`.
- This regenerates `.state/validation-report.json` and the workspace index without new LLM calls or re-synthesis.

### 6. Verify
- Confirm `validation-report.json` has `wikiSlug: rkkp-danibd` and no stale paths.
- Confirm `index-of-indexes.md` only lists `rkkp-danibd`.

### Note
- I assume the wiki title should also change to `Rkkp Danibd`. If you want to keep the current title `Rkkp Danibd New`, let me know before I implement.