# Plan: Push everything except DeepSeek/Zhipu/GLM providers

## Goal
Create a clean branch from the current `feat/multi-provider-and-429-stall` work that keeps all Phase-16 resilience (429/5xx stall ladder, audit log, JSON corrector, per-attempt deadlines, Retry-After clamp), per-step model routing, the folder-picker, icon tooling, and other non-provider work, while entirely removing DeepSeek, Zhipu, and the GLM model family from the codebase, tests, and docs.

## Branch strategy
Create a new branch `feat/429-stall-no-new-providers` from the current working tree. Do not rewrite the existing `feat/multi-provider-and-429-stall` branch in place so the original history remains available if needed. After the changes pass tests, this new branch can be merged/pushed to `main`.

## Source-code changes

### `src/llm/client.ts`
- Narrow `Provider` union to `'anthropic' | 'openai' | 'qwen' | \`custom:${string}\``.
- Remove `DEEPSEEK_API_URL` and `ZHIPU_API_URL` constants.
- Remove price-table entries: `deepseek-v4-pro`, `glm-4.7-flash`, `glm-4.7-flashx`, `glm-5.2`, `glm-5.3`.
- Remove `deepseek` and `zhipu` from `ModelRouting.apiKeys` type and from `setModelRouting` normalization.
- Remove provider-specific branches in `resolveApiKeyForProvider`, `getApiKeyStatus`, `displayProviderName`, `openAiCompatibleUrl`, and missing-key error messages.
- Simplify OpenAI-compatible dispatch conditions from `provider === 'openai' || ... || 'deepseek' || 'zhipu'` to `provider === 'openai' || provider === 'qwen'`.
- Keep the stall ladder, `transport-stalls.jsonl` audit log, `AbortSignal.timeout`, `parseRetryAfterSeconds`, and the `jsonCorrector` routing slot untouched.

### `src/tui/settings.ts`
- Remove `deepseek` and `zhipu` from `ApiKeys` interface and from `DEFAULT_SETTINGS.apiKeys`.
- Narrow `BuiltInProvider` to `'anthropic' | 'openai' | 'qwen'`.
- Delete `MODEL_CATALOG.deepseek` and `MODEL_CATALOG.zhipu` blocks.
- Delete `DEFAULT_MODEL_FOR_PROVIDER.deepseek/zhipu` and `CURATION_MODEL_FOR_PROVIDER.deepseek/zhipu`.
- Remove `deepseek`/`zhipu` cases from `isBuiltInProvider` and `normalizeApiKeys`.
- Keep `ModelSlot`, `ModelRouting`, `seedModelsForProvider`, `getModelCatalog`, and custom-provider support.

### `src/tui/settings-screen.tsx`
- Remove `'apiKeyDeepseek'` and `'apiKeyZhipu'` from `StaticSettingRow` and from `buildRowOrder`.
- Remove `'deepseek'` and `'zhipu'` from `providerList`.
- Update `PROVIDER_LABELS` and casts to the three remaining built-in providers.
- Remove `MODEL_CATALOG.deepseek` and `MODEL_CATALOG.zhipu` spreads in `MODEL_SHORT_NAMES`.
- Remove the `zhipu` block from `RECOMMENDATIONS` and update `recommendationFor` guard.
- Remove input-handler branches and `renderKeyRow` calls for the two API-key rows.
- Remove non-TTY fallback text rows for DeepSeek/Zhipu keys.
- Keep all model-row cycling, slot resolution, custom-provider rows, and test-connection logic.

### Untracked source files
The following uncommitted files are NOT provider-specific and will be kept:
- `src/llm/json-corrector.ts`
- `src/utils/folder-dialog.ts`
- `prompts/json-corrector.prompt.txt`
- `scripts/build-icon.ts`
- `scripts/set-icon.ts`
- `tests/folder-dialog.test.tsx`

## Test changes

### `tests/phase-11.test.ts`
- Delete constants `DEEPSEEK_PRO`, `GLM_FLASH`, `GLM_FLASHX`, `GLM_52`, `GLM_53`, `DEEPSEEK_URL`, `ZHIPU_URL`.
- Delete Gate 11.15 (DeepSeek provider) block.
- Delete Gate 11.17 (Zhipu/GLM provider) block.
- Rewrite Gate 11.16 to use Anthropic/OpenAI/Qwen in its mixed-provider examples instead of `deepseek`/`zhipu`.
- Rewrite Gate 11.18 `jsonCorrector` mixed-provider example to avoid removed providers.
- Strip `deepseek`/`zhipu` from all `apiKeys` expectations and type annotations.
- Recalculate row-count comments and DOWN-press loops after removing two API-key rows (Save index shifts from 17 → 15, Back 18 → 16).

### `tests/phase-16.test.ts`
- Generalize the Zhipu-named HTTP-500 mock comment to a generic provider 500-error shape.

### `tests/phase-24.test.ts`
- No changes needed.

## Documentation / DOX changes

### `AGENTS.md` (root)
- Delete the 2026-08-17 DeepSeek-provider user-preference entry and the 2026-08-19 Zhipu-provider entry.
- Rewrite the 2026-07-22 multi-provider entry to list only Anthropic/OpenAI/Qwen.
- Generalize the 2026-08-20 stall-ladder example that names GLM-4.7-FlashX.
- Add a new `dist/` version-bump note (1.0.24) documenting the provider-removal asset change.

### `src/AGENTS.md`
- Rewrite the `tui/` ownership bullet to remove DeepSeek/Zhipu Settings details.
- Rewrite the `llm/client.ts` ownership bullet to remove DeepSeek/Zhipu endpoint, env vars, price-table, and reasoning-model notes.

### `tests/AGENTS.md`
- Rewrite the `phase-11.test.ts` summary to drop gates 11.15/11.17 and all provider-specific language.

### `Implementation Plan/PHASE_11_polish.md`
- Delete the v1.10.0 Zhipu amendment.
- Rewrite the v1.9.0 amendment to describe only generic per-step provider/model routing.
- Delete Gate 11.15 and Gate 11.17 sections.
- Rewrite Gate 11.16 examples to use remaining providers.
- Update Default Provider cycle, provider catalog, and per-step example.
- Delete UAT 11.9.
- Update approval checklist gate/UAT counts.

### `Implementation Plan/PHASE_16_run_resilience.md`
- Generalize references to Zhipu GLM-4.7-Flash as the throttled free-tier motivation (keep the stall behavior; anonymize the example).

### `scripts/launcher-entry.ts`
- Bump `VERSION` to `1.0.24`.
- Add a version-history comment explaining that the bundle changed because DeepSeek/Zhipu/GLM were removed, so installed runtimes must re-extract.

## Build / dist handling
- Run `npm run build` to regenerate `dist/chase.mjs` and `dist/launcher.cjs` from the provider-free source.
- Run `npm run package:win` to rebuild `dist/paper-chase.exe` and apply the icon patch.
- Do NOT commit runtime wiki artifacts (`dist/wikis/rkkp-dhhd/`, `dist/wikis/cross-wiki/` regenerated markdown, etc.) as part of the provider-removal PR. If those tracked files are currently modified only because of a UAT run, they should be reset to `main`'s version (or the branch's pre-UAT version) before pushing.

## Verification
1. `npm run test` — all vitest suites must pass after the test updates.
2. `npm run build` — esbuild must succeed and the bundle must not contain `deepseek`/`zhipu` literals.
3. Optional: `npm run package:win` — verify the Windows exe builds and extracts.

## Open questions before implementation
1. **Untracked runtime artifacts in `dist/wikis/rkkp-dhhd/`**: Should these be deleted/ignored, or do you want them committed? They look like UAT output and should not go to `main`.
2. **Tracked `dist/wikis/` changes**: The current working tree shows modified tracked files under `dist/wikis/`. Should these be reset to the `main` state before the provider-removal PR, or do you want a fresh cross-wiki discovery run committed?
3. **Existing `feat/multi-provider-and-429-stall` branch**: Should it be preserved as-is, or do you want it force-pushed to match the new provider-free state once we are done?

Once you approve this plan and answer the open questions, I will create the new branch and start the edits.