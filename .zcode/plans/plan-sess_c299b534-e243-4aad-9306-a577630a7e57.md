## Update the humanizer skill to upstream v2.11.2

Replace `C:\Users\atavi\.agents\skills\humanizer\SKILL.md` (v2.1.1, 24 patterns) with the current upstream file from `blader/humanizer` (v2.11.2, 35 patterns + new voice-matching / false-positive / output-mode sections). This is the only file in that skill folder; no other copies exist on the system.

### Steps

1. **Download byte-exact** — `curl -fsSL https://raw.githubusercontent.com/blader/humanizer/main/SKILL.md -o "C:\Users\atavi\.agents\skills\humanizer\SKILL.md"` (direct download avoids any tool-mediated rewriting of the content).
2. **Verify** — Read the written file: confirm frontmatter (`name: humanizer`, `license: MIT`, `metadata.version: 2.11.2`), the 35 numbered patterns, and that the file ends cleanly at the `## Source` section. If the download fails, fall back to fetching via the web reader MCP tool and writing the content with the Write tool.
3. **Effect** — the new description/triggers load on the next session start; no restart-safety concerns since it's a plain markdown skill.

### Notes

- The upstream frontmatter drops the old `allowed-tools` key — the replacement follows upstream verbatim, which just removes a tool restriction (nothing breaks). If you'd rather keep the `allowed-tools` block, say so and I'll re-add it on top.
- No Wiki v5 project files are touched; per the 2026-07-19 preference this skill intentionally lives outside the project folder, so no DOX pass applies.