# Plan: Promote the optimization decisions to canon + phase the implementation (Phases 13–15)

**Canon mode:** Operate. The ratified decisions in `Project Vision/optimizations/optimizations.md` (L1, L1b, L2-guidance, L3, L3e, L5; L4 rejected) are marked "pending promotion" — per `optimizations/AGENTS.md` they take effect only after promotion into the canon docs. This plan: (0) closes Phase 12, (1) promotes the decisions into vision docs 01/04/05/07, (2) writes three phase docs + index updates, (3) DOX closeout. Implementation of the phases themselves happens afterward, on your word ("Start Phase 13").

---

## Step 0 — Close Phase 12 (Golden Rule precondition)

- Update `.state/phase-12-status.json`: status `verified-awaiting-uat` → `accepted`; record that UAT 12.1 was satisfied by the 2026-07-23 adhd-wiki production ingest (reask loop live end-to-end, run passed the old failure point and completed; per your answer just now), UAT 12.2/12.3 demonstrated by gate evidence as the phase doc allows.
- Log the acceptance to `.state/compliance-log.md`.

## Step 1 — Vision-alignment pass (promotion, per the ratified amendment maps)

Drafted and applied, then presented to you as a diff summary for review (your chosen flow). `02`/`03`/`06` verified as needing **no edit** (02 §4.7/§4.8 are already quality-based with the sparse flag; 03 describes the settled taxonomy; 06's claims/citations are regrouped, never altered) — stated in the compliance log.

**`01_PRODUCT_VISION_AND_ARCHITECTURE.md`**
- §4.1 layer table: annotate the curation pass — one topic-curation call + one entity-curation call per ingest (may run in parallel, ~$0.05–0.25), placed between Layer 3 aggregation and Layer 4 synthesis.
- §5 "Who Decides What": add rows — *topic merge/drop decisions: LLM (curation pass), application deterministic*; *entity identity merges: LLM (curation pass), application deterministic*.

**`04_orchestration_detailed.md`**
- §1 sentence + ASCII diagram: add the two per-ingest curation calls after aggregation; add the L5 note — entity/topic synthesis stages may process pages concurrently (bounded pool, fixed cap 4), output/report order deterministic; extraction, curation, DOX, workspace, updater stay sequential.
- Step 6: rewrite as **aggregate → curate → apply → write** (topic curation merge/drop/keep + entity curation merge-only, strict-identity); rolling-memory update explicitly after curation.
- §6: quality-failure enumeration gains "curation decision list fails deterministic validation" → bounded reask (Phase 12 pattern) → **keep-all fallback** among the enumerated deterministic fallbacks; plus the L1 note — output-token ceilings are safety ceilings sized above structural output (synthesis 32768, DOX 8192), not length controllers; truncation simply surfaces as the quality failure it causes.
- §9: curation prompts carry `{languageDirective}`; topic slugs/folders follow the output language (claim types become folder names per `05` §2.1).

**`05_page_types_specification.md`**
- §7: topic eligibility rule — a topic must be a theme/concept/issue a journalist would search for; meta-descriptors of the documents' rhetoric (statistical, temporal, methodological…) are not topics and are pruned at materialization; one theme under different wording/plural/form merges.
- §6: entity identity rule — one real-world thing = one page; name variants/abbreviations/translations/word-order merge at materialization into the canonical page, `aliases` accumulate variant titles; sub-units never merge into parents; colocated-but-distinct things stay separate.
- §2: add `sparse` to optional frontmatter fields (per `02` §4.8); note `aliases` also accumulates merged name variants.

**`07_validation_and_quality.md`**
- §1: layered-checks list gains curation decision-list validation; extend the no-Critic note — the curation pass is a *narrow bounded structured-output checker* (one decision list, deterministic validation, deterministic keep-all fallback, pennies per run), not the removed free-floating Critic.
- §2: validation order gains decision-list validation after aggregation, before topic/entity page writes.
- §5: enumerate the keep-all fallback triggers (validation exhaustion, transient exhaustion, HTTP 4xx → skip curation = pre-curation behavior, logged; self-healing next run because curation input includes on-disk topics/entities).

**`optimizations/optimizations.md`**
- §4 decision log: mark L1, L1b, L2, L3, L3e, L5 "Promoted to canon 2026-07-23" with target sections; L4 stays n/a; header note updated (ratified decisions promoted).

**Compliance log:** one promotion entry (changed docs/sections, result COMPLIANT — restores fidelity to 02 §4.6/§4.7/§4.8).

## Step 2 — Phase docs + plan-index updates

**`Implementation Plan/PHASE_13_output_caps_and_prompt_self_sizing.md`** (L1 + L1b + L2 label; ~2–3h; budget $0)
- `src/agents/synthesis.ts`: four 8192 literals → shared `SYNTHESIS_MAX_TOKENS = 32768`; `src/dox-writer.ts`: `DOX_WRITER_MAX_TOKENS` 2048→8192.
- Four synthesis prompts: remove every word-count line; insert the **verbatim-ratified** replacement block (topic variants adjust the completeness list). `prompts/dox-writer.prompt.txt`: add the ratified `## Pages` catalog-line quality rule ("each line must tell the reader something the page title alone does not").
- `sparse: true`: Materializer writes it for entity pages with ≤2 mentions and no significant claims/relationships (`02` §4.8); the ingest synthesis-replacement path re-imposes `sparse` deterministically next to `aliases` (UAT 6.3 precedent — verified at `src/commands/ingest.ts:756`).
- TUI Settings DOX-slot recommendation labels → mid-tier per L2 guidance (both providers).
- Gates (LLM-free): constants; prompts word-count-free + block verbatim; DOX rule present; sparse written/not-written; sparse survives synthesis replacement (stub); TUI labels; prompt diff confined to the amended blocks; full-suite regression green.

**`Implementation Plan/PHASE_14_topic_and_entity_curation.md`** (L3 + L3e; ~5–7h; budget $0)
- New `prompts/curation-topics.prompt.txt` + `prompts/curation-entities.prompt.txt` (with the `{languageDirective}` block per `prompts/AGENTS.md`); new `src/agents/curation.ts`: input builders (topics: slug/title/folder/claim-count/≤3 sample claims ~200 chars; entities: slug/title/type/folder/mention-count/significance/disambiguation/1–2 sample mention contexts — **both include existing on-disk pages**); strict JSON decision lists; deterministic validators (slug-exists, exactly-one-bucket, into-kept, no-self-merge, union-find chain resolution); reask via Phase 12 `runWithFeedbackRetry`; **keep-all fallback** (validation/transient exhaustion, HTTP 4xx); two-round lexical-stem bucketing above ~250–300 topics with per-round validation+fallback + global reconciliation round.
- Deterministic application: topic merge (claim union+dedupe, title from `into`) / drop (claims live on entity/document pages — preservation intact); entity merge (union evidence, relationship subject/object slug rewrite, aliases accumulation, canonical folder = into-folder); **exact-segment wikilink rewrite** across all content pages (bare + pipe forms; prefix-collision safety — `[[odense]]` must not corrupt `[[odense-bup-auditorium|…]]`); update-mode: existing topic merge/drop deletes page/folder deterministically + re-materializes `into`; manual-edit skip via hash mismatch → conflict log; **all-or-nothing** application.
- Materializer seam (verified): aggregation already completes in memory before page writes (`src/materializer.ts:206–402`) — split into aggregate → curate → apply → write; the Phase 8 `removedDuplicates` machinery is the precedent for deterministic page deletion.
- Settings: additive fifth routing slot `curation` (`ModelRouting.curation`, `resolveModel` callType `'curation'`, TUI `modelCuration` row + provider defaults Sonnet 5 / GPT-5.6 Terra + recommendation label; provider switch resets all five slots; `.paper-chase.json` additive).
- Observability: `.state/curation-report.json` (merges, drops, skips, fallback events), human-editable `.state/curation-overrides.json` (never-merge pairs honored by the checker), console warning + metrics counter on fallback; rolling memory updated post-curation.
- Gates (LLM-free, ~12): every validator rule class; reask→keep-all; all-or-nothing; topic merge/drop; entity merge + relationship rewrite; exact-segment link rewrite incl. prefix collisions and bare-link display-title insertion; union-find chains; manual-edit skip; overrides honored; on-disk inclusion + folder deletion + re-materialize; slot routing; two-round bucketing; `{languageDirective}` in both prompts.

**`Implementation Plan/PHASE_15_synthesis_concurrency.md`** (L5, user-narrowed scope; ~3–4h; budget $0)
- Bounded worker pool, **fixed cap 4**, around ONLY the entity and topic synthesis loops (`src/commands/ingest.ts:744,827`); single serialized writer funnel for `llm-calls.json` / `synthesis-report.json` / `conflicts.json`; reports collected and written in original page order; per-page strict→permissive→template + reask semantics unchanged; TUI aggregate progress ("142/338 pages, 4 workers"); extraction, curation, DOX, workspace, updater stay sequential; existing 429/backoff machinery handles pool pressure.
- Gates (LLM-free): in-flight count never exceeds 4 (instrumented stub); deterministic report order under completion reordering; no interleaved JSONL writes; per-page fallbacks unchanged; TUI progress string; sequential stages untouched.

**Index/prompt updates:** `IMPLEMENTATION_PLAN_MASTER_INDEX.md` (+3 directory rows, +3 $0 budget rows, +3 "What you have" rows, file listing); `MASTER_IMPLEMENTATION_PROMPT.md` §4 mapping (+3 rows: 13 → `04` §6 + `07` §5 + `02` §4.7/§4.8; 14 → `01` §4.1/§5, `04` §1/Step 6/§6/§9, `05` §6/§7, `07` §1/§2/§5; 15 → `04` §1).

## Step 3 — DOX closeout for this planning pass

- Root `AGENTS.md`: Child DOX Index Implementation Plan line gains Phases 13–15 (the 2026-07-23 L2 routing preference already exists — no change).
- `Implementation Plan/AGENTS.md`: ownership bullet gains the three new phase docs.
- `.state/compliance-log.md`: promotion entry + planning-pass entry.

## Execution order after approval

1. Steps 0–3 in one pass (docs only; no code, no commits unless you ask).
2. Present the vision-amendment diff summary for your review (your chosen flow).
3. You say **"Start Phase 13"** → canon loop per phase (Implementer → Verifier → Reporter → your UAT acceptance), 13 → 14 → 15 in order.
4. After Phase 15: **package UAT** — one full adhd-wiki re-ingest against the $144.95 / ~11 h baseline (projection ~$45–55 / ~2.5–3.5 h). Real-money step, only when you trigger it.