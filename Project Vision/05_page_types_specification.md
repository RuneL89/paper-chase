# LLM Wiki CLI — Page Types Specification (Detailed)

This document specifies the page types produced by the LLM Wiki CLI. It is written for implementers and contributors who need to know exactly what frontmatter, content, and naming conventions each page type requires. A reader with no prior context should be able to produce a valid page of each type after reading this document.

---

## 1. Overview

The LLM Wiki CLI generates a wiki of interlinked markdown pages. Each page has a `type` field in its YAML frontmatter. The default page types are:

| Type | Purpose | Created by |
|---|---|---|
| `index` | Contract / navigation page | DOX Writer (deterministic) |
| `document` | One chunk of a source PDF | Layer 1 (deterministic) |
| `source` | Provenance catalog entry for a PDF | Layer 1 (deterministic) |
| `topic` | Theme or concept across the corpus | Layer 3 (Materializer) |
| `entity` | Named real-world thing | Layer 3 (Materializer) |
| `raw` | Unparseable or scanned PDF page | Layer 1 (deterministic) |

The page-type taxonomy is **not hard-coded**. The LLM may create new types (e.g., `timeline`, `claim`, `transaction`, `case`) when the corpus demands them. New types must be documented in the folder-level `index.md` and in the wiki's `AGENTS.md`.

`entity`, `topic`, and `document` pages are optionally enhanced by the **Synthesis Writer** (Layer 4) in Phase 5+, which adds a Layer 1 synthesis of readable prose while preserving the Layer 2 structured or extracted detail.

---

## 2. Common Frontmatter Rules

Every page, regardless of type, must have:

```yaml
---
title: "Page Title"
type: "document"  # or another valid type
updated: "2026-07-16T10:00:00Z"
---
```

Optional but common fields:

- `created` — ISO 8601 timestamp of first creation.
- `wiki` — the wiki slug this page belongs to.
- `tags` — a list of tags for indexing and navigation.
- `confidence` — `high`, `medium`, or `low`, indicating how confident the LLM is in the extracted content.

The `type` field is free-form but must be declared in the folder-level `index.md` contract if it is not one of the six default types.

---

## 3. The `index` Page Type

`index` pages are contracts, not content. They tell the reader how the wiki or folder is organized and what to expect inside.

### 3.1 Frontmatter

```yaml
---
title: "Executives"
type: index
wiki: acme-reports
updated: "2026-07-16T10:00:00Z"
children:
  - john-smith.md
  - jane-doe.md
  - index.md
---
```

### 3.2 Content Structure

An `index` page typically contains:

- **Title and scope** — what this wiki or folder is about.
- **Catalog** — a list of folders or pages inside.
- **Navigation** — guidance on where to start.
- **Contract** — the rules that pages inside this folder must follow (page types, naming conventions, citation format).
- **Statistics** — counts of sources, pages, and page types.

### 3.3 Levels of Index

| Level | File | Purpose | Generated By |
|---|---|---|---|
| Workspace | `index-of-indexes.md` | Lists all wikis in the workspace. | Deterministic |
| Wiki | `wikis/<slug>/index.md` | Lists all folders and the wiki's scope. | DOX Writer |
| Folder | `wikis/<slug>/<folder>/index.md` | Lists pages in the folder and the folder's contract. | DOX Writer |
| Sub-folder | `wikis/<slug>/<folder>/<sub>/index.md` | Lists pages in the sub-folder. | DOX Writer |

### 3.4 Naming Convention

`index.md` inside the folder it governs. No slug needed.

---

## 4. The `document` Page Type

A `document` page represents one chunk of a source PDF. It is the primary container for the raw material of the corpus.

### 4.1 Frontmatter

```yaml
---
title: "Part 1: Annual Report 2024"
type: document
wiki: acme-reports
tags: ["annual-report", "2024", "financial"]
confidence: high
sources:
  - file: "wikis/acme-reports/raw/annual-report-2024.pdf"
    pages: "1-15"
    extracted: "2026-07-16T10:00:00Z"
updated: "2026-07-16T10:00:00Z"
---
```

### 4.2 Content Structure

A `document` page has two layers:

1. **LLM-written synthesis** at the top (optional, Phase 5+):
   - Summary of the chunk.
   - Key claims with citations.
   - Links to related entities, topics, and sources.

2. **Preserved extracted detail** below:
   - Full text, organized by headings.
   - Tables preserved verbatim as markdown tables.
   - Figure descriptions.

### 4.3 Naming Convention

For a source with slug `annual-report-2024` and chunk number `001`:

```
documents/annual-report-2024-part-001.md
```

For a small PDF that fits in one chunk:

```
documents/<source-slug>.md
```

---

## 5. The `source` Page Type

A `source` page is a deterministic provenance record for one PDF.

### 5.1 Frontmatter

```yaml
---
title: "Source: Annual Report 2024.pdf"
type: source
wiki: acme-reports
file: "wikis/acme-reports/raw/annual-report-2024.pdf"
sha256: "a1b2c3d4e5f6..."
pages: 120
ingested: "2026-07-16T10:00:00Z"
updated: "2026-07-16T10:00:00Z"
warnings:
  - "Pages 3, 48 appear to be scanned or image-only."
---
```

### 5.2 Content Structure

A `source` page contains:

- A brief description of the PDF.
- A table of metadata (file path, size, page count, hash, extraction timestamp).
- A list of warnings (e.g., scanned pages, malformed text).
- Links to the document pages derived from this source.
- Links to the source PDF file for direct access.

### 5.3 Naming Convention

```
sources/<source-slug>.md
```

The source slug is derived from the filename without extension.

---

## 6. The `entity` Page Type

An `entity` page is about a named real-world thing: a person, organization, company, location, product, or other named object.

### 6.1 Frontmatter

```yaml
---
title: "John Smith"
type: entity
wiki: acme-reports
tags: ["person", "executive"]
updated: "2026-07-16T10:00:00Z"
sources:
  - file: "wikis/acme-reports/raw/annual-report-2024.pdf"
    pages: "1-5, 47-48"
---
```

### 6.2 Content Structure (Two-Layer)

**Layer 1: Synthesis**

```markdown
John Smith is the CEO of Acme Corp, a position he has held since 2019 [^src1].
Prior to Acme, he served as CFO of Beta Industries [^src2].
```

**Layer 2: Preserved Detail**

```markdown
## Mentions

- Page 1: "John Smith, CEO of Acme Corp" [^src1]
- Page 47: "John Smith previously served as CFO of Beta Industries" [^src2]

## Relationships

- [[Acme Corp]] — CEO of [^src1]
- [[Beta Industries]] — Former CFO of [^src2]

## Sources

[^src1]: annual-report-2024.pdf, pages 1-5
[^src2]: annual-report-2024.pdf, pages 47-48
```

### 6.3 Naming Convention

```
entities/<subfolder>/<slug>.md
```

Example: `entities/people/executives/john-smith.md`

---

## 7. The `topic` Page Type

A `topic` page is about a theme, concept, or legal issue that appears across the corpus.

### 7.1 Frontmatter

```yaml
---
title: "Revenue Recognition"
type: topic
wiki: acme-reports
tags: ["financial", "accounting"]
updated: "2026-07-16T10:00:00Z"
sources:
  - file: "wikis/acme-reports/raw/annual-report-2024.pdf"
    pages: "12-15, 89-92"
---
```

### 7.2 Content Structure

**Layer 1: Synthesis**

2–4 paragraphs of readable prose that explain how the topic appears across the corpus, why it matters, and how it relates to key entities. The summary must include key claims with inline citations and links to related entities and topics.

**Layer 2: Preserved Detail**

A list of every claim related to this topic, organized by source and page, with links to the entities involved, plus a `## Sources` section defining every citation.

### 7.3 Naming Convention

```
topics/<subfolder>/<slug>.md
```

Example: `topics/financial/revenue-recognition.md`

---

## 8. The `raw` Page Type

A `raw` page is for unparseable or scanned PDF pages where `pdfjs-dist` could not extract text.

### 8.1 Frontmatter

```yaml
---
title: "Raw: Annual Report 2024 — Page 48"
type: raw
wiki: acme-reports
source: "wikis/acme-reports/raw/annual-report-2024.pdf"
page: 48
updated: "2026-07-16T10:00:00Z"
---
```

### 8.2 Content Structure

A note that the page could not be parsed, with a link to the source PDF for manual review.

### 8.3 Naming Convention

```
raw/<source-slug>-page-<number>.md
```

---

## 9. Custom Page Types

The LLM may introduce new page types when the corpus demands them. Examples:

- `timeline` — for chronological events.
- `transaction` — for financial transactions.
- `case` — for legal cases or investigations.
- `claim` — for specific assertions that need verification.

When a new type is introduced:
1. The Extractor includes it in the JSON output.
2. The Materializer creates the page with the new `type`.
3. The DOX Writer documents it in the folder-level `index.md`.
4. The system logs the new type in `.state/proposals/page-types.json`.
