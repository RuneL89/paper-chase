# src/ — DOX contract

## Purpose

All TypeScript source for Paper Chase v.1.0: CLI entry point, TUI (Ink), PDF extraction, LLM client, the Extractor agent (Layer 2), the Synthesis Writer (Layer 4), the deterministic Materializer (Layer 3), the Curation agent (Phase 14, L3 + L3e), the DOX Writer (Layer 5), the AGENTS.md Updater, schema validation, commands, state, and utilities.

## Ownership

* `cli.ts` — Commander entry point; named `program` export; `program.parse()` only runs when executed directly (guarded), never on import; Phase 11 rebrand: `program.name('chase')`, description "The paper chase, automated. Turn PDFs into citation-backed markdown wikis.", launched via `bin/chase.js`; the ingest summary line is the shared `formatIngestSummary(result)`; `ingest` accepts `--no-extract` (Layer-1-only opt-out; extraction is default-on since Phase 2) and `--no-dox-llm` (deterministic-DOX opt-out; the LLM DOX Writer is default-on for CLI runs since Phase 6); Phase 7: `init --output-language` and `ingest --input-language`/`--output-language` (ISO codes en, da, de, fr, es, no, sv); Phase 9: `ingest --update-agents` (opt-in AGENTS.md update proposal after the ingest, saved to `.state/proposed-agents.md` — never auto-applied); the parse guard is pkg-aware (2026-07-23 packaging): `process.pkg` short-circuits to direct execution and an empty `import.meta` (CJS bundle) falls back to argv[1]
* `tui/` — Ink TUI. Phase 11 (phase doc §2.3): the production menu is exactly five items — Create New Wiki, Add PDFs, Ingest PDFs, Settings, Exit; the development/review screens (test-screen, extractor-test-screen, validation-report-screen, compounding-log-screen, entity/topic/dox browsers, agents-review-screen, structural-changes-screen) and their now-dead helpers (`components/tree-browser.tsx`, `hooks/use-document-chunks.ts`, `utils/line-diff.ts`) are deleted — their `.state/` outputs keep being written by the pipeline and are reviewed as files. Phase 11 v1.6.0 (user directive 2026-07-23) RESTORED `agents-review-screen.tsx` (adapted: Reject is a no-op that keeps `.state/proposed-agents.md`; Accept still copies the proposal over AGENTS.md) and `utils/line-diff.ts` — the review screen is FLOW-ONLY, reachable only from the post-ingest `p` shortcut, never a menu item. `app.tsx` (screen router; Screen = menu|init|add-pdfs|ingest|settings|agents-review|exit — `agents-review` is the v1.6.0 flow-only screen, routed to from the Ingest screen's `onReviewAgents` with `wiki` from `flowWiki`, `onBack` returns to the menu; optional `workspace` prop threaded into every screen; optional test-only `ingestFn` threaded into the Ingest screen; `flowWiki` state carries the continuous-workflow wiki), `menu.tsx` (main menu + exported `resolveMenuSelection`; Phase 11 first-launch welcome splash shown only when neither `.paper-chase.json` nor the legacy `.llm-wiki-cli.json` exists), `init-screen.tsx` (Title + Workspace text inputs; slug derived from Title via `slugify`; Phase 7 Output Language dropdown below Workspace, English pre-selected, Left/Right cycles — Enter on the dropdown submits so the pre-Phase-7 Title → Tab → Tab → Enter flow is unchanged; Phase 11 `onCreated` callback routes a successful create straight to Add PDFs), `add-pdfs-screen.tsx` (Phase 11: `initialWiki` starts the screen in add mode skipping the selector; the success banner is `Copied N file(s) to wikis/<slug>/raw/.`; every successful add is followed by a `Start ingesting now? [Y/n]` prompt — Y/Enter invokes `onStartIngest`, n/Escape goes back), `ingest-screen.tsx` (passes `doxLlm: true` since Phase 6 — production ingests are LLM-driven; Phase 7 Input/Output Language dropdowns pre-selected from the wiki's `.state/language.json`, Tab moves focus between the wiki list and the dropdowns, inline slug-forking warning + explicit Enter/Escape confirm gate when the chosen input language differs from the last run on a wiki with extractions; Phase 9 'Propose AGENTS.md Updates' toggle (key `a`, pre-selected from the settings `updateAgents` field, passed to `ingest` as `updateAgents`); Phase 11: `initialWiki` pre-selects the wiki once the list loads, `Chunk X/Y` progress lines render with a plain-text `[██████████]` bar, the result banner is the shared `formatIngestSummary`, and the `onViewReport` prop is gone; Phase 11 v1.6.0: when a run completes with `agentsUpdateProposed`, the success state shows `AGENTS.md update proposed — press [P] to review the diff.` (non-TTY fallback never renders it) and `p`/`P` invokes the new `onReviewAgents(wiki)` prop — no proposal means no hint and `p` does nothing), `settings-screen.tsx` (Phase 11: "LLM Model Routing" section — a Provider row (v1.4.0: Anthropic ↔ OpenAI; Qwen extension 2026-08-04: Anthropic ↔ OpenAI ↔ Qwen, Left/Right-cycling, directly above Default Model; v1.8.0: custom providers appear as `custom:<id>` entries) plus one Left/Right-cycling dropdown per call type (Default, Extractor, Synthesis Writer, DOX Writer, Phase 14: Curation) with provider-aware inline recommendation labels (Phase 13: the DOX Writer recommendation is the mid-tier label — Anthropic `Sonnet — mid-tier; structural navigation, correctness re-imposed deterministically`, OpenAI `GPT-5.6 Terra — mid-tier; structural navigation, correctness re-imposed deterministically`, Qwen `Qwen 3.7 Max — mid-tier; structural navigation, correctness re-imposed deterministically`; Phase 14: the Curation recommendation is Anthropic `Sonnet — mid-tier judgment for merge/drop decisions`, OpenAI `GPT-5.6 Terra — mid-tier judgment for merge/drop decisions`, Qwen `Qwen 3.7 Max — mid-tier judgment for merge/drop decisions`); per-call-type dropdowns offer "Same as default" (persisted as null) plus the selected provider's catalog ids and a `Custom model...` sentinel (v1.7.0): cycling to `Custom model...` opens a raw model-id `TextInput`, submitting stores the custom string in the slot, a persisted custom id displays raw and can be cycled back to edit mode; v1.7.0: pressing `T` on a model row runs `testModelConnection` against the slot's resolved model and shows the result in a dedicated test-result area (success / HTTP error / network error / missing key), independent of the save status; switching the provider RESETS the five slots to the new provider's defaults via `seedModelsForProvider` so stale cross-provider ids can never persist; v1.8.0: the "Add Custom Provider" row below the model rows creates a new custom provider with OpenAI-compatible defaults (base URL, Authorization/Content-Type headers, request/response JSON templates, empty model list) and switches to it; when a custom provider is selected, its configuration rows appear inline (Base URL, API Key, Add Header + per-header rows, Request Template, Response Text/Input/Output paths, Add Model + per-model rows, Delete Custom Provider); v1.5.0: the "API Keys" section below the model rows — one row per provider showing only the masked status (`[configured ••••last4]` stored / `[from environment ••••last4]` env / `[not set]`, via `getApiKeyStatus`; the full key is NEVER rendered), Enter opens a masked `TextInput` (ink-text-input `mask`, empty draft — never pre-filled), a non-empty submit STAGES the key, an EMPTY submit stages a clear (null), Escape cancels, staged values persist only via [ Save ]; saves to `.paper-chase.json`), `settings.ts` (Phase 11: `TuiSettings { synthesis, updateAgents, models, apiKeys, customProviders }`; loads `.paper-chase.json` with a read-only fallback to legacy `.llm-wiki-cli.json` when the new file is absent; `saveSettings` always writes the new name; older files without a `models` block — or without `provider` inside it (v1.4.0) — load with the Anthropic defaults filled in; v1.5.0: `apiKeys { anthropic, openai }` (string | null, null = not stored) — a missing block loads as nulls, saved configs always carry it, no key-format validation; v1.4.0 exports `MODEL_CATALOG` (provider → `{id, label}[]`: Anthropic Haiku/Sonnet/Opus, OpenAI GPT-5.6 Luna/Terra/Sol; Qwen extension 2026-08-04: Qwen-Plus / Qwen 3.7 Max / Qwen 3.8 Max; v1.7.0: every catalog carries the `__custom__` sentinel), `DEFAULT_MODEL_FOR_PROVIDER` (cheapest tier per provider), and `seedModelsForProvider`; Phase 14: `CURATION_MODEL_FOR_PROVIDER` (mid-tier per provider: `claude-sonnet-5` / `gpt-5.6-terra` / `qwen3.7-max`) — `seedModelsForProvider` returns five slots including `curation`, and `normalizeModels` fills an absent `curation` with null (legacy configs byte-identical); v1.8.0: `CustomProviderConfig { id, name, baseUrl, apiKey, headers, requestTemplate, responseTemplate, models }` (OpenAI-compatible by default, fully configurable via `{{model}}`/`{{messages}}`/`{{maxTokens}}`/`{{temperature}}`/`{{apiKey}}` placeholders and dot/bracket response paths), `createCustomProvider`, `slugifyCustomProviderId`, `findCustomProvider`, `isBuiltInProvider`, `getModelCatalog`, `getDefaultModelForProvider`, `getCurationModelForProvider`, and `customProviders` persisted as a top-level block (legacy configs load as `[]`)), `components/` (header — text "Paper Chase v.1.0", footer, spinner, error-box, success-box), `hooks/use-wiki-list.ts`, `hooks/use-wiki-details.ts`, `hooks/use-raw-contents.ts`
* `extraction/pdf.ts` — frozen Phase 0 surface: `extractText(pdfPath, startPage?, endPage?)` extracts plain text from a PDF page range (omitted range = every page), preserving line breaks and joining pages with newlines; `getPageCount(pdfPath)` returns the page count. Both use the pdfjs-dist legacy build with bundled standard fonts and destroy the document when done. No engine configuration: pdfjs-dist is the only extraction backend. Packaging (2026-07-23): inside a pkg exe the standard fonts resolve from the snapshot assets and `configurePackagedWorker()` materializes the CJS pdf.js worker to the OS temp dir and require()s it so `globalThis.pdfjsWorker` registers — pdf.js's fake worker then skips its dynamic import(), which pkg's runtime does not support; dev/test behavior is byte-identical (the packaged branch is gated on `isPackaged()`)
* `extraction/markdown-tables.ts` — deterministic plaintext-table detection; rebuilds markdown tables from whitespace-collapsed extraction output (conservative guards, every word preserved)
* `llm/client.ts` — `callLLM(prompt, system?, options?)`; multi-provider (Phase 11 v1.4.0, user directive 2026-07-22; Qwen extension 2026-08-04; custom providers v1.8.0): the Anthropic Messages API (default), the OpenAI Chat Completions API, the Qwen/DashScope OpenAI-compatible API, or a user-defined custom provider, branched by `resolveProvider()`; Anthropic sends the byte-identical pre-extension shape (`max_tokens`, top-level `system`, optional `temperature`; `x-api-key` + `anthropic-version` headers), OpenAI posts to `https://api.openai.com/v1/chat/completions` with `authorization: Bearer $OPENAI_API_KEY`, Qwen posts to `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions` with `authorization: Bearer $DASHSCOPE_API_KEY`; OpenAI/Qwen use `max_completion_tokens` (never `max_tokens`), NEVER send `temperature` (GPT-5.6 reasoning models reject it — `options.temperature` is ignored), the system prompt as a leading `role: 'system'` message, and parse `choices[0].message.content` + `prompt_tokens`/`completion_tokens`; custom providers (v1.8.0) use a JSON request body template with `{{model}}`/`{{messages}}`/`{{maxTokens}}`/`{{temperature}}`/`{{apiKey}}` placeholders, configured headers (values may use `{{apiKey}}`), and dot/bracket JSON paths for response extraction; Phase 11 v1.5.0 (user directive 2026-07-23): the API key resolves per call, per provider — Settings-stored key from the routing config's additive `apiKeys` field (built-in) or the custom provider's `apiKey` field (custom) → `process.env.ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `DASHSCOPE_API_KEY` (the `.env` fallback loader populates process.env first) → the missing-key error, which now names all three sources ('…is not set. Add it in Settings, export it in your environment, or add it to a .env file in the project root.'); the resolved key is used exactly where the env key always was (`x-api-key` / `Bearer`) and is NEVER logged or rendered in full — `getApiKeyStatus(provider, storedKey)` powers the Settings screen's masked rows (`{ source: 'stored' | 'environment' | 'none', last4 }`; triggers the same one-time .env load; returns at most the last 4 characters of the resolved key; stored wins); logs `LLM Call | Tokens: i/o | Cost: $x` for every call; additive Phase 7 v1.1.0 `maxRetries` (bounded retry amendment, user-ratified 2026-07-20): transient failures (HTTP 429/5xx, network) retried up to N extra attempts, HTTP 4xx always throws immediately, default 0 = frozen no-retry behavior for every caller that does not opt in (pipeline callers pass 2 = 3 total attempts) — identical semantics for all providers; Phase 16 (vision `04` §1 pool transport tuning, user-ratified 2026-07-25): the backoff between retries is the exponential `transportRetryDelayMs` (5s/15s/45s — was linear 1s/2s; attempt counts and error classes unchanged), large-output calls (`maxTokens >= LARGE_CALL_MAX_TOKENS = 32768`) carry `LARGE_CALL_HEADERS_TIMEOUT_MS = 600_000` (smaller calls keep undici's default), `setTransportRetrySleeper(fn|null)` is the test-only hook that makes backoff assertions wall-clock-free, and `isTransientTransportError(err)` is the ONE exported classification behind the ingest per-page transport fallback (matches all providers' thrown shapes: 'API transport error after N attempt(s)' = network/timeout, 'API error (HTTP 429|5xx)' = transient after retries; 4xx and everything else classify false); additive Phase 2 `CallLLMOptions { maxTokens?, temperature? }` — defaults (1024, temperature omitted) preserve the frozen Phase 0 behavior exactly; additive Phase 5 `callType` and `logPath` options so each LLM call can be tagged and appended to `wikis/<slug>/.state/llm-calls.json` (the entry gains an additive `provider` field in v1.4.0); Phase 11 model routing: `ModelRouting { provider?, default, extractor, synthesis, dox, curation?, apiKeys?, customProviders? }` (per-call-type entries are a model id or null = "Same as default"; `provider?: 'anthropic' | 'openai' | 'qwen' | \`custom:${id}\`` absent = anthropic so legacy callers/configs are unchanged; Phase 14 `curation?: string | null` absent in legacy tables, normalized to null by `setModelRouting`; v1.5.0 `apiKeys?: { anthropic?, openai?, qwen? }` carries the Settings-stored keys, normalized to nulls by `setModelRouting`; v1.8.0 `customProviders?: CustomProviderConfig[]` carries the custom provider configs), `setModelRouting(routing | null)` (called once per ingest run from the workspace settings), `resolveProvider()`, `resolveModel(callType?, override?)` (order: explicit override → routing entry for the call type ('extractor' → extractor, the four synthesis call types → synthesis, 'dox-writer' → dox, Phase 14: 'curation' → curation, everything else → default) → routing default → `ANTHROPIC_MODEL` env → DEFAULT_MODEL; with no routing set the behavior is byte-identical to the pre-Phase-11 client; the env fallback stays anthropic-scoped — there is deliberately no `OPENAI_MODEL` env var), `resolveModelFromRouting(routing, callType?, override?)` (v1.7.0: the pure variant used by the TUI test helper), `resolveApiKeyForTest(provider, storedKey?, customProviders?)` (v1.7.0: Settings → env → `.env`; v1.8.0: custom provider key lookup), `testModelConnection(provider, model, apiKey, customProviders?)` (v1.7.0: a tiny no-retry `maxTokens: 1` call that returns `{ ok, message }` without logging or cost accounting; v1.8.0: supports custom providers), additive `CallLLMOptions.model` per-call override, and Sonnet ($3/$15) + Opus ($15/$75) + GPT-5.6 Luna ($1/$6) / Terra ($2.50/$15) / Sol ($5/$30) + Qwen-Plus ($0.5/$1) / Qwen 3.7 Max ($2/$5) / Qwen 3.8 Max ($3/$6) entries in the per-MTok price table (Qwen prices are PLACEHOLDER — TODO: verify against the DashScope console; custom provider models fall back to the default price entry); Phase 15: the `logPath` append (`appendLlmCallLog`) runs inside the shared serialized write queue (`utils/serialized-writes.ts`, keyed by path) so pooled synthesis workers appending concurrently never interleave JSONL lines — still best-effort, a logging failure never fails the call
* `llm/reask.ts` — Phase 12 (validation-feedback retry / "reask", vision `04` §6 feedback-retry carve-out, user-ratified 2026-07-23): the shared helper behind every content-defect retry in the pipeline. `runWithFeedbackRetry(runLlm, validate, { label, maxAttempts?, onRepair? })` runs attempt 1 via `runLlm(null, 1)` (the attempt-1 prompt is byte-identical to pre-Phase-12 at every site) and, on a validation failure, hands the validator's exact `errors` plus the invalid output to `buildCorrectionBlock` (`=== CORRECTION REQUIRED ===` + `REASK_CORRECTION_INSTRUCTION` + one `- <error>` line per error + the invalid output verbatim + `=== END CORRECTION ===`); the call site composes `${prompt}\n\n${feedback}` and logs attempts 2+ under the `<ctx>#attempt<N>` context. ≤3 total attempts (`REASK_MAX_ATTEMPTS`); exhaustion returns `{ output: null, attempts, lastErrors }`. THROWN errors (HTTP 4xx, transport) are never quality-retried — they propagate at the Extractor/synthesis sites and are caught ONCE per target by the DOX/workspace/updater callers. Repair accounting: `beginReaskRun()` resets the module-level counter at ingest start and `reaskRepairs()` reports it (surfaced as `metrics.feedbackRepairs`)
* `utils/hash.ts` — `sha256(filePath)` streaming helper
* `utils/slug.ts` — wiki-slug validation (kebab-case), source-slug derivation, and `slugify` (also used by the Extractor for deterministic slug normalization); Phase 7 additive: `slugify(name, language?)` transliterates with the input language's map first (vision `04` §9.3); omitted/'en' runs the byte-identical pre-Phase-7 ASCII-only path
* `utils/language.ts` — Phase 7 language model (vision `04` §9): `SUPPORTED_LANGUAGES` (en, da, de, fr, es, no, sv with English + native display names), `getLanguage` (throws on unsupported codes), `transliterate` (explicit per-language map BEFORE NFD diacritic stripping — da/no æ→ae ø→oe å→aa, de ä→ae ö→oe ü→ue ß→ss, sv å→a ä→a ö→o, en/fr/es map-less), `buildLanguageDirective(role, input, output)` (one paragraph per agent role; '' when en/en), and `applyLanguageDirective(prompt, directive)` (fills `{languageDirective}`; removes the whole `=== LANGUAGE ===` block when the directive is empty so en/en prompts stay byte-identical)
* `utils/aliases.ts` — Obsidian wikilink aliases (UAT 6.3 fix, user decision 2026-07-19 Option A): `aliasesForTitle(title, fileSlug)` returns `[title]` when the title differs from the file basename (case-insensitive) and `undefined` otherwise; `enforceAliasesInMarkdown(markdown, title, fileSlug)` re-imposes the field over a fully-rendered page (used for LLM-written synthesis output; pages without a frontmatter block are returned unchanged). Phase 14 additive: `combinedAliases(title, fileSlug, extraAliases?)` unions the base alias with curation-merged variant titles (trimmed; empties, the basename, the title itself, and case-insensitive duplicates skipped) and `enforceAliasesInMarkdown` takes the same optional extras — the entity-page writer and both ingest entity synthesis write points pass `EntityPageData.mergedAliases` through (vision `05` §2: every merged variant title resolves to the canonical page). Used by every page writer: entity/topic/source pages, the ingest document-page writer (always a no-op there: title == chunk id), and the DOX Writer's deterministic index frontmatter (both deterministic and LLM-enforced paths; index pages always carry their folder/wiki title as the alias). Since the 2026-07-20 pipe-form change, aliases are retained as Obsidian search/suggester aids only — link resolution no longer depends on them
* `utils/wikilinks.ts` — Obsidian-native wikilink forms (user directive 2026-07-20, compliance-log entry [2026-07-20 00:15]): `formatWikilink(target, display?)` builds `[[target|Display]]` or the bare `[[target]]` when the display is missing or equals the target exactly (Obsidian's own display==target behavior); `parseWikilinkTarget(inner)` splits a link's inner text on the FIRST `|` (targets never contain spaces/pipes in our wikis). Conventions: content pages `[[<basename>|<Title>]]`, folder indexes `[[<folder-path>/index|<Title>]]`, wiki root `[[index|<Wiki Title>]]`. Phase 14 additive: `rewriteWikilinkTargets(markdown, rewrites)` repoints links after curation merges — matching is by EXACT pre-pipe target segment only (prefix-collision-safe: rewriting `[[odense]]` never touches `[[odense-bup-auditorium|X]]` or `[[odense-2]]`); pipe-form `[[from|Display]]` → `[[into|Display]]`, bare `[[from]]` → `[[into|From Title]]`; an empty rewrite map returns the input byte-identically. The single home for link formatting/parsing — used by the entity/topic page writers, the DOX Writer (catalogs, prompt link targets, repair), the materializer's curation rewrite pass, and the link checker
* `utils/wikilink-repair.ts` — Phase 20 (B20, vision `02` §2): deterministic, conservative wikilink repair — `repairWikilinksInMarkdown(markdown, universe)` (pure, synchronous, never throws; no-repair input returns byte-identical) repairs an unresolved wikilink target via (a) UNIQUE prefix match against the wiki's basename slugs (`indikator-2` → `indikator-2-ekkokardiografi`), then (b) UNIQUE alias match (frontmatter `aliases` slugified with the universe's language), returning `{ markdown, repairs, unrepairable }`; zero/multiple matches stay broken and are reported with sorted candidates (never guessed); display text never rewritten; `isRepairableContentPage` scopes to entity/topic pages (index/sources/documents excluded); `repairWikilinksInWiki` is the one-time remediation core (rewrites changed pages, re-converges `.state/ingestion.json` `pageHashes` from disk for every modified page — no B19-class false flags) behind `scripts/repair-wikilinks.ts`, with `formatWikiRepairReport`
* `utils/paths.ts` — workspace/wiki path helpers; generated paths always use forward slashes
* `utils/app-root.ts` — application-root resolution for runtime assets (prompts/, templates/, pdfjs standard fonts, the CJS pdf.js worker): dev/tests walk up to the nearest package.json; pkg exes (`isPackaged()` via `process.pkg`) resolve one level above the bundle's snapshot dist/ dir. All prompt/template/font/worker path resolution goes through `appRoot()` — never ad-hoc `import.meta.url` walks (added 2026-07-23 for the Windows exe packaging)
* `utils/file-dialog.ts` — native graphical PDF picker (user decision 2026-07-17 10:55): `pickPdfFiles()` spawns `powershell.exe -NoProfile -NonInteractive -Command <script>` (`shell: false`, args array only) showing a topmost System.Windows.Forms OpenFileDialog (PDF filter, multi-select, 10-minute timeout); resolves with the picked paths, `null` on cancel, throws descriptively on spawn/non-zero-exit/timeout; `parseDialogOutput(stdout)` is the pure `\r\n`-safe parser exported for tests
* `utils/line-diff.ts` — minimal line-based diff (restored in Phase 11 v1.6.0 from the Phase 9 implementation, byte-for-byte): `diffLines(before, after)` (classic LCS over lines; op list with added/removed counts) and `diffHunks(diff, contextLines?)` (display hunks with `+ `/`- `/`  ` prefixes and `...` separators); dependency-free, used by the flow-only AGENTS.md review screen
* `utils/worker-pool.ts` — Phase 15 (vision `04` §1 concurrency note): `runPool(items, worker, { concurrency, staggerMs?, sleepFn? })` — a bounded worker pool: exactly `concurrency` workers pull indexes off a shared counter (in-flight NEVER exceeds the cap), results land in INPUT order regardless of completion order, and a rejected item rejects the pool after the in-flight workers settle (items not yet started never start). Phase 16 (vision `04` §1 pool transport tuning): `staggerMs` spreads pickups deterministically through a slot chain (pickup #n starts ~n x staggerMs after the pool begins — a stage never fires 4 large requests at the same instant; default 0 = the plain Phase 15 behavior; `sleepFn` is the test seam for observing the stagger without wall-clock waiting). No dependencies; used ONLY by the synthesis stages in `commands/ingest.ts` (entity, topic, and — since Phase 22 gate 22.10 — composite)
* `utils/serialized-writes.ts` — Phase 15: `enqueueSerializedWrite(key, task)` — the single shared serialized write queue (a per-key promise chain): each task runs after the previous task for the same key settles; a task's rejection propagates to its own caller but never breaks the chain. Serializes the `llm-calls.json` appends (the `llm/client.ts` logPath seam) and the `conflicts.json` read-modify-writes (`state/conflicts.ts`) under pool concurrency — one shared mechanism, not two
* `commands/init.ts` — Phase 1 `init(slug, { title?, workspace?, outputLanguage? })`: creates `wikis/<slug>/` structure and generates AGENTS.md from `templates/AGENTS.md` (all `{{WIKI_TITLE}}`/`{{SLUG}}`/`{{OUTPUT_LANGUAGE}}` replaced; title defaults to the slug, output language to English); Phase 7 additive: persists `.state/language.json` (`outputLanguage`, `lastInputLanguage: 'en'`)
* `commands/ingest.ts` — `ingest(slug, { workspace?, pagesPerChunk?, extract?, synthesis?, doxLlm?, onProgress?, extractChunkFn?, synthesizeEntityFn?, synthesizeEntityPermissiveFn?, synthesizeTopicFn?, synthesizeTopicPermissiveFn?, writeDoxIndexFn?, writeWorkspaceIndexFn?, outputLanguage? })`: hash-skip unchanged PDFs, page-by-page extraction, whole-page chunking (default 5), document pages via gray-matter, source pages, state; Layer 2 since Phase 2: per-chunk Extractor run (default-on, `extract?: boolean` opt-out) saving `.state/extracted/<chunk-id>.json`, additive `IngestResult.extractions` counts; Phase 5: after materialization, optionally runs the Synthesis Writer per entity and topic page in that order (opt-in `synthesis?: boolean`, default false), writing synthesized pages only when the preservation check passes; Phase 7 v1.1.0: each synthesis mode is retried up to 3 total attempts on preservation failure (`trySynthesisMode`, language-agnostic) before the chain moves on, and attempt counts are recorded in the synthesis report; Phase 6 pipeline order (phase doc §3.5): synthesis → `validateWiki()` (content validation, additive `IngestResult.validation`) → `writeDoxContracts()` → `validateWiki()` again (final validation covering the DOX pages, additive `IngestResult.finalValidation`; the persisted `validation-report.json` reflects the final pass) → `writeWorkspaceIndex()` (2026-07-20 amendment: regenerates `wikis/index-of-indexes.md` from every wiki's freshly-written root contract, at the end of every ingest); `doxLlm?: boolean` (default false) and the test-only `writeDoxIndexFn`/`writeWorkspaceIndexFn` pass straight through to the DOX Writer; `outputLanguage?` (English default) feeds the workspace pass prose language; Phase 7 (vision `04` §9): additive `inputLanguage?`/`outputLanguage?` options — output resolves flag → `.state/language.json` → 'en', input resolves flag → `lastInputLanguage` → 'en'; the resolved pair threads into the Extractor (via the default extractDocumentChunk wrapper), the four synthesis functions, and `writeDoxContracts` (the workspace pass receives the output language's English name); when the resolved input differs from `lastInputLanguage` and `.state/extracted/` holds JSON, a visible slug-forking warning is logged before processing (vision `04` §9.3); after the run `lastInputLanguage` is persisted (a per-run output override never overwrites the stored output language); additive `IngestResult.languages`; with `extract: false` there is a single DOX pass and no validations; `extractChunkFn` and the `synthesize*Fn` injections are optional test-only helpers for LLM-free runs; Phase 9: additive `updateAgents?: boolean` (default false) runs the AGENTS.md Updater after the workspace pass — proposal written to `.state/proposed-agents.md`, original AGENTS.md never touched, additive `IngestResult.agentsUpdateProposed`; test-only `proposeAgentsUpdateFn` injection keeps gates LLM-free; Phase 8: incremental ingestion + compounding — unchanged PDFs skip with `Skipping <file> (unchanged).`, changed PDFs are fully re-processed (old document pages removed along with their stale `.state/extracted/<chunk>.json` files; a warning is logged when the current input language differs from the source's recorded extraction language), PDFs recorded in state but missing from `raw/` log a warning and their derived pages are KEPT (checked on both the normal and empty-`raw/` paths); each source records its input `language` in ingestion state; after every run `state.pageHashes` is re-hashed from disk post-synthesis (a working hash map is threaded between per-PDF materialize calls within one run so pages written earlier in the run are not false-flagged), conflict-skipped pages log `Skipping update of <path> (manually edited). Conflict logged.`, and `.state/metrics.json` is written (run timestamp, new PDFs, new/updated entities via rolling-memory diff with conflict-skipped slugs excluded from updates, conflicts delta, `totalCost` from `sumLlmCostSince`); 2026-07-21 UAT fix loop: materialize ALSO runs when every PDF is hash-skipped (guarded on existing `.state/extracted` JSON) so an already-forked wiki is repaired by one more ingest — deleted fork duplicates drop their working-hash entries and log `Removed duplicate page <path> (entity now lives at <canonical path>).`; crash-safe finalization — the wiki language state and a preliminary `metrics.json` (totalCost excluding the not-yet-run DOX/workspace calls) are written immediately after the post-synthesis re-hash block, BEFORE validation/DOX/workspace, and both metrics writes are try/catch-hardened (`Warning: could not record (preliminary )ingestion metrics: <msg>` via progress, the run still completes); additive test-only `writeWorkspaceProseFn` pass-through to `writeWorkspaceIndex` (the 2026-07-21b prose call is otherwise unstubbable at ingest level); Phase 11: per-call LLM model routing is wired at the top of `ingest()` — `loadSettings(workspace)` + `setModelRouting({ ...models, apiKeys })` in a best-effort try/catch (the single integration point covering both the CLI and the TUI; a settings failure keeps whatever routing was already in effect; v1.5.0: the Settings-stored API keys ride along on the routing config); additive `IngestResult.synthesisRan` flags whether the Synthesis Writer stage ran; exported `formatIngestSummary(result)` is the shared CLI/TUI end-of-run banner (`Ingest complete: X ingested, Y skipped.` plus a Synthesis segment only when `synthesisRan` and a Validation segment only when a validation pass ran); the Phase 11 metrics fields (phase doc §2.6 — chunk/relationship/claim/page/folder/conflict-split/token/wall-clock counters) are populated by `buildRunMetrics` at BOTH the preliminary and final write points (`sumLlmUsageSince` supplies cost + tokens), and the final write is followed by a compact `Metrics: N chunks, M entities (X new), R relationships, C claims, $cost, Ts` progress line; Phase 12 (reask): `beginReaskRun()` resets the repair counter at run start, `trySynthesisMode` retries preservation failures via `runWithFeedbackRetry` (the checker's exact failure strings as the correction block; thrown errors propagate — never quality-retried; the four `synthesize*Fn` seams take trailing `feedback`/`attempt` params), the final metrics write records `feedbackRepairs: reaskRepairs()`, and when repairs reach ≥5 or exceed 25% of the run's logged LLM calls (`countLlmCallsSince`) a prompt-quality warning is emitted before `Done!` — `Warning: N of M LLM calls this run needed validator-feedback repair — the underlying prompt may need attention.`; Phase 13: both entity synthesis write points (strict and permissive) re-impose the `sparse` frontmatter field over the model-written page via `enforceSparseInMarkdown(page, entityPage.sparse === true)` composed after `enforceAliasesInMarkdown` (the UAT 6.3 deterministic-re-imposition precedent) — a model-emitted `sparse` on a non-sparse entity is removed and a missing one on a sparse entity is added; topic write points are untouched (entity-only rule, vision `02` §4.8); Phase 14 (curation): `runMaterialize` passes `curation: synthesis === true` plus the run's language pair into every materialize call (the enablement rule — curation follows the synthesis flag; no CLI flag, no TUI toggle), the test-only `curateTopicsFn`/`curateEntitiesFn` seams pass straight through (the `writeDoxIndexFn` precedent), `result.curation.fallbacks.length` accumulates into the additive `curationFallbacks` metrics counter, `rewrittenLinks` hashes and `removedPages` deletions fold into the working page-hash map between per-PDF materialize calls, `manualEditSkips` surface as progress lines, and both entity synthesis write points pass `entityPage.mergedAliases` as the extras argument of `enforceAliasesInMarkdown` (merged variant titles survive the model-written page); Phase 15 (vision `04` §1 concurrency note, user-ratified L5 scope): the entity- and topic-synthesis per-page loops run through `runPool(…, { concurrency: SYNTHESIS_POOL_SIZE })` with the FIXED exported constant `SYNTHESIS_POOL_SIZE = 4` (deliberately NOT a Settings field); each pool task is the pre-Phase-15 per-page body verbatim (strict → permissive → structured template with the Phase 12 reask loop inside — outcomes byte-equivalent to the sequential loop) and RETURNS its synthesis-report entry, which `appendSynthesisReportEntries` writes ONCE per stage in original page order after the pool completes; progress is the aggregate `Synthesis: N/M pages complete (4 workers)` line re-emitted per completion (identical TUI/CLI; the per-page preservation WARNING lines are unchanged); extraction, curation, DOX, workspace, and the updater stay sequential; Phase 16 (vision `04` §6 + Step 9 + Step 11 + §1, user-ratified 2026-07-25): (a) PER-PAGE TRANSPORT FALLBACK at the two synthesis pool loops ONLY — an error classifying `isTransientTransportError` (429/5xx/network/timeout after the client's bounded retries) is caught for THAT page: structured template, loud warning `Transport failure for <slug> after retries — template fallback.`, report `finalMode: 'transport-fallback'`, additive `metrics.transportFailures` increment (the 'transport' outcome kind never touches the preservation-conflict counters); HTTP 4xx NEVER falls back — it rethrows and aborts; (b) OUTAGE DETECTOR per stage (`TRANSPORT_OUTAGE_CONSECUTIVE_LIMIT = 5`, `TRANSPORT_OUTAGE_RATE = 0.1` — 5 consecutive transport-failed pages OR more than 10% of the stage's ATTEMPTED pages aborts the run with the transport error; the consecutive counter resets on any completed chain, quality templates included, because the LLM demonstrably answered; below both thresholds the run completes with a run-level summary warning); (c) SYNTHESIS RESUME — `.state/synthesis-state.json` records checkpoint per page as it completes via `recordSynthesisPage` (serialized queue; every outcome recorded: pass modes skip-eligible, `structured-template`/`transport-fallback` retried); each stage partitions pages through `partitionStage` (skip-eligible record + matching `pageDataHash(page, language)` + not rewritten by this run's materialize), skipped pages make zero LLM calls and contribute reconstructed report entries so the ordered stage report is complete (counters `result.synthesisSkipped`/`synthesisTopicsSkipped`, progress line `Synthesis: N page(s) skipped (unchanged data).`); (d) PER-PDF CHECKPOINT — `writeIngestionState` runs inside the per-PDF loop right after `state.sources[sourceSlug] = ...` with `state.pageHashes = workingPageHashes`, exactly the record the end-of-run write produces; (e) the pool calls pass `staggerMs: options.poolStaggerMs ?? SYNTHESIS_POOL_STAGGER_MS` (250 — a fixed constant like the pool cap; `poolStaggerMs` is the test seam); Phase 17 (B1 + B2, vision `05` §2 + `06` §2-§3/§7): the four synthesis write points (strict/permissive entity at ~1032/1069, strict/permissive topic at ~1219/1248) compose the existing aliases/sparse enforcers with `enforceFrontmatterInMarkdown` / `enforceTopicFrontmatterInMarkdown` (the COMPLETE deterministic frontmatter written OVER the model's — created when the model omitted the block; `updated` is the real write time, fixing B2) and `enforceSourcesSectionInMarkdown` / `enforceTopicSourcesSectionInMarkdown` (the `## Sources` definitions rebuilt basename-form from `buildCitationMap`; in-prose `[^srcN]` markers byte-identical); enforcement runs ONLY at these write points, so skip-eligible pages under the Phase 16 resume rule are never touched (byte-stability contract, gate 17.12); Phase 18 (B18): `preservationFeedbackErrors` feeds the checker's `extraMarkers` into the reask correction block (`Off-map citation marker (remove it or replace it with a key from the CITATION KEYS list): …`); Phase 19 (B19): the end-of-run re-hash moved into a `finally` around the synthesis stages (`rehashWrittenPagesFromDisk`) so even an ABORTED run converges what it wrote — the convicted leak: the per-PDF checkpoint persists pre-synthesis template hashes and an abort between a synthesis write and the re-hash left recorded(template) != disk(synthesized) — plus the working folds are carried into persisted state even when no per-PDF checkpoint ran (`state.pageHashes = workingPageHashes` before the pre-synthesis `writeIngestionState`), and `MaterializeResult.convergedPages` surface as progress lines; Phase 20 (B20): the synthesis stage builds the slug universe ONCE per run (`buildSlugUniverse(slug, workspace, { language: input })`) and the four write points compose `repairPageLinks` (a small closure over `repairWikilinksInMarkdown`) as the outermost enforcement — conservative unique-prefix/alias repair with per-page `Link repair <label>: N repaired[, M unrepairable].` progress lines; Phase 22 gate 22.10: the COMPOSITE synthesis stage is wired as the third pooled stage (after entity and topic) — `lastMaterializeResult.compositePages` flows through the same strict → permissive → structured-template chain (`trySynthesisMode` reask, `checkCompositePreservation`), the same pool (cap 4 + stagger), the same per-page transport fallback + outage detector + `recordSynthesisPage` checkpoint (the CompositePageData fingerprint drives skip-eligibility and the reconstructed skip entries), report entries with `pageType: 'composite'` in the existing finalMode vocabulary, the test-only `synthesizeCompositeFn`/`synthesizeCompositePermissiveFn` seams (the `synthesizeEntityFn` precedent), and the composite write-point enforcers (`enforceCompositeFrontmatterInMarkdown` + `enforceSourcesSectionInMarkdown` over `buildCompositeCitationMap`, `repairPageLinks` outermost — the sparse enforcer is NOT applicable); additive counters `synthesizedComposites`/`synthesizedCompositesPermissive`/`compositeConflicts`/`synthesisCompositesSkipped` (folded into `formatIngestSummary`); the `partitionStage`/`reconstructedSkipEntry` helpers accept CompositePageData; `pageType: 'composite'` also added to the conflicts-entry union; Phase 23 (§2.3, backlog B21): the COMPARISON synthesis stage is wired as the FOURTH pooled stage (after composite) — `lastMaterializeResult.comparisonPages` flows through the identical strict → permissive → structured-template chain (`trySynthesisMode` reask, `checkComparisonPreservation` with the row-value `droppedRowValues` joining the reask feedback as `Dropped table row value (restore this exact value): …`), the same pool/detector/checkpoint (the ComparisonPageData fingerprint drives skip-eligibility and the reconstructed skip entries), report entries with `pageType: 'comparison'`, the test-only `synthesizeComparisonFn`/`synthesizeComparisonPermissiveFn` seams, and the comparison write-point enforcers (`enforceComparisonFrontmatterInMarkdown` + `enforceComparisonBridgeInMarkdown` — the deterministic bridge re-imposed — + `enforceSourcesSectionInMarkdown` over `buildComparisonCitationMap`, `repairPageLinks` outermost; the sparse enforcer NOT applicable); additive counters `synthesizedComparisons`/`synthesizedComparisonsPermissive`/`comparisonConflicts`/`synthesisComparisonsSkipped` (folded into `formatIngestSummary`); `partitionStage`/`reconstructedSkipEntry` widened to ComparisonPageData; `pageType: 'comparison'` added to the report + conflicts unions
* `commands/extract-chunk.ts` — Phase 2 `extractDocumentChunk(wikiDir, chunkId, language?)`: the single shared extraction path used by `ingest` (the Phase 11-deleted TUI Test Extractor screen was the only other caller); reads the document page (chunk text, page range, source file from frontmatter), the wiki constitution, and rolling memory (read-only); writes `.state/extracted/<chunk-id>.json`; Phase 7 additive `language` passes the run's input/output pair to the Extractor
* `commands/add-pdf.ts` — user-directed Phase 1 extension (2026-07-17): `addPdfToWiki(wikiDir, sourcePath)` copies a PDF into `<wikiDir>/raw/` (strips surrounding quotes from drag-drop pastes, validates existence + `.pdf`, keeps the file name); throws typed `AddPdfError` with user-displayable messages; no LLM, no new deps
* `state/ingestion-state.ts` — read/write `wikis/<slug>/.state/ingestion.json` (phase doc §2.3 shape); Phase 8 additive: per-source `language` (input language at last extraction) and top-level `pageHashes` (wiki-relative page path → SHA-256 of content at the end of the last ingestion, recorded AFTER all programmatic writes so the tool's own writes are never flagged as manual edits)
* `state/rolling-memory.ts` — Phase 2 read-only loader and Phase 3 writer for `.state/rolling-memory.json` (vision `04` §5 shape: `folderStructure` → folders, `entities[].slug` → slugs; absence → empty lists, malformed → descriptive throw); Phase 8 additive: `readFullRollingMemory` (tolerant full reader incl. `mentionCount`; null when absent)
* `state/conflicts.ts` — Phase 5: logs entity/topic/document preservation-check failures to `wikis/<slug>/.state/conflicts.json` (Phase 22 gate 22.10: the entry `pageType` union gains `'composite'`; Phase 23: gains `'comparison'`, and a comparison check's `droppedRowValues` ride the `dropped.claims` channel — the closest existing vocabulary for dropped content items); Phase 8 additive: `logManualEditConflict` appends `{timestamp, type: 'manual-edit', page, reason: 'Page was manually edited since last ingestion. Skipping update.'}` when a page hash differs from the recorded hash; 2026-07-21 additive: optional `reason` override (used by the Materializer's fork reconciliation when a manually-edited DUPLICATE page is kept — default string byte-identical; Phase 14: also used for curation merge/drop vetoes, with a reason naming the concern, the from/into slugs, and the skip); Phase 15: both append paths (`logConflict`, `logManualEditConflict`) run inside the shared per-path serialized write queue (`utils/serialized-writes.ts`) so pool workers logging conflicts concurrently never lose or interleave entries
* `state/metrics.ts` — Phase 8: `writeMetrics`/`readMetrics` for `wikis/<slug>/.state/metrics.json` (null when absent/malformed) and `sumLlmCostSince` (sums cost from `llm-calls.json` JSON lines with timestamp >= run start, best-effort); Phase 11 (phase doc §2.6): `IngestionMetrics` is extended additively — the Phase 8 fields (`run`, `newPdfs`, `newEntities`, `updatedEntities`, `conflicts`, `totalCost`) are unchanged so existing readers keep working, and the new fields add `chunksProcessed`/`chunksSkipped`/`chunksFailed`, `entitiesNew`/`entitiesUpdated`, `relationshipsExtracted`, `claimsExtracted`/`claimsByType`, `pagesByType`, `foldersCreated`, `brokenLinks`/`orphanedPages` (final validation pass), `conflictsManualEdit`/`conflictsPreservation`, `totalTokens`, and `wallClockMs`; additive `sumLlmUsageSince` returns cost AND input/output token sums (cost-only `sumLlmCostSince` is now a thin wrapper over it); Phase 12 additive: `feedbackRepairs` field (validator-feedback repairs in the run, sourced from `reaskRepairs()` in `llm/reask.ts`) and `countLlmCallsSince(dir, sinceIso)` (counts `llm-calls.json` JSON-lines entries with `timestamp >= sinceIso`; feeds the ingest repair-rate warning); Phase 14 additive: required `curationFallbacks` field (keep-all fallback events across both curation concerns in the run, sourced from `MaterializeResult.curation.fallbacks`); Phase 16 additive: required `transportFailures` field (per-page transport fallbacks across both synthesis stages in the run — pages that landed on the structured template after exhausted transient transport retries)
* `state/language.ts` — Phase 7: read/write `wikis/<slug>/.state/language.json` (`WikiLanguageState { outputLanguage, lastInputLanguage }`); absent file → `{ en, en }` (pre-Phase-7 wikis), unknown codes fall back to 'en' per field, malformed JSON throws descriptively
* `state/synthesis-report.ts` — Phase 5: `logSynthesisReport(wikiDir, entry)` writes a `synthesis-report.json` entry for each entity, topic, and document page showing the strict/permissive/structured-template fallback chain; Phase 7 v1.1.0 additive `attempts` field per mode (bounded retry counts); Phase 15: `appendSynthesisReportEntries(wikiDir, entries)` appends a batch in one read-modify-write — the pooled synthesis stages COLLECT their entries in memory and append once per stage in original page order (deterministic, diff-friendly regardless of completion order); `logSynthesisReport` is now a one-entry wrapper over it; Phase 16: additive `finalMode: 'transport-fallback'` — a page that landed on the structured template because a transient transport error was still throwing after the bounded retries (the per-page transport fallback); Phase 22 gate 22.10: `pageType` gains `'composite'` for the composite stage's entries; Phase 23: gains `'comparison'` for the comparison stage's entries
* `state/synthesis-state.ts` — Phase 16 (vision `04` Step 9 synthesis resume + Step 11 per-page checkpointing, user-ratified 2026-07-25): `.state/synthesis-state.json` — `{ pages: { "<wiki-rel path>": { mode, dataHash, synthesizedAt } } }`, the content-addressed completion memory of the Synthesis Writer. `pageDataHash(pageData, language)` is the ONE exported fingerprint helper (SHA-256 over the canonical stable-JSON aggregate — title + folder + structured data + the run's language pair; `slugToTitle` is EXCLUDED as global cross-page context) shared by the Materializer's preservation check and the ingest skip rule so the two can never drift; `isSkipEligible(record)` (strict-synthesis/permissive-synthesis only — template fallbacks are always retried); `synthesisPagePath(pageData)`; `readSynthesisState` (absent → empty, malformed → descriptive throw, the readIngestionState house style); `recordSynthesisPage(wikiDir, path, record)` — the per-page checkpoint: read-modify-write through the Phase 15 serialized write queue (pool workers checkpoint concurrently), stale entries (page file gone) pruned at write time, page keys written sorted so the file is deterministic regardless of pool completion order. Phase 22: `pageDataHash`/`synthesisPagePath` accept `CompositePageData` — the composite fingerprint is a hash over { members, unioned evidence, language } (gate 22.8). Phase 23: both accept `ComparisonPageData` — the comparison fingerprint is a hash over { subject, dated table sections, bridge, language } (`slugToTitle` excluded as always)
* `state/structural-changes.ts` — Phase 9: read/append `wikis/<slug>/.state/proposals/structural-changes.json` (vision `03` §5 shape: `{changes: [{timestamp, type: new-folder|new-page-type|entity-reclassification, path, reason, affectedEntities?}]}`; additive `knownPageTypes` tracker so a page type is logged exactly once); absent file → empty log, malformed JSON throws descriptively
* `state/curation-overrides.ts` — Phase 14 (phase doc §2.7): `readCurationOverrides(wikiDir)` reads `.state/curation-overrides.json` (`{"neverMerge": [["slug-a","slug-b"]]}` — human-curated pairs the curation calls may never merge). Absent → the file is CREATED with an empty list (so a journalist discovers the knob); malformed JSON or wrong shape → a console warning and an empty override set (never crashes the run). Pairs are symmetric; validation vetoes a matching merge edge into keep for BOTH concerns and records the veto. Phase 21: unchanged, and neverMerge still wins over the auto-apply tier, confirm-deny, and sticky records (gate 21.8)
* `state/curation-decisions.ts` — Phase 21 (§2.3, backlog B5): `.state/curation-decisions.json` — the STICKY identity-decision record `{ decisions: [{ concern, action: merge|drop|cluster, from: [...], into?, signal|model, decidedAt, runId }], splits: [slug] }`. `readCurationDecisions` creates the file empty when absent (the journalist discovers the `splits` knob; malformed → warning + empty, never crashes); `appendCurationDecisions(wikiDir, records)` appends newly-applied decisions deduped by (concern, action, from-set, into), backs malformed content up to `curation-decisions.corrupt.json` before rebuilding, and CONSUMES split slugs touching a newly applied record (the re-decision sticks — a permanent veto belongs in `curation-overrides.json` neverMerge). The materializer pre-applies the active records before candidate construction; a `splits` entry un-applies its record (reversal logged, pair returns to candidates) and vetoes the auto tier via `vetoSlugs`. Phase 22: the `cluster` action is live — members re-derive as `[into, ...from]` and the additive optional `class` (1-5) + `rationale` fields carry the rollup metadata (validated on read)
* `state/curation-report.ts` — Phase 14 (phase doc §2.7): `writeCurationReport(wikiDir, report)` writes `.state/curation-report.json` at the end of every materialize run whose curation stage ran — per-concern applied merges/drops, attempt counts, fallback events with cause (`validation-exhaustion` | `transport-exhaustion` | `http-4xx`), neverMerge vetoes, manual-edit skips, deleted pages, and rewritten-link paths+hashes. Not written when the stage was skipped (disabled / no extraction data / no candidates). Phase 21 additive fields (optional, legacy shape frozen): per-concern `fromSticky`, `decidedThisRun` (merges with signal + evidence, drops, denials), `proposedPairs`, `autoApplied`, top-level `splitReversals`. Phase 22 additive fields (optional, same discipline): `CurationReportCluster { members, class, into, signal?, evidence?, rationale? }` on `fromSticky.clusters`, `decidedThisRun.clusters` + `clusterDenials`, and `proposedClusters`; `manualEditSkips.action` gains `'cluster'`
* `pages/source-page.ts` — deterministic `sources/<source-slug>.md` provenance pages (phase doc §2.4)
* `pages/entity-page.ts` — deterministic `entities/<subfolder>/<slug>.md` writer with YAML frontmatter, mentions, relationships, claims, and `[^srcN]` citations (phase doc §2.1); relationship/claim entity links render in Obsidian-native pipe form `[[slug|Title]]` via `utils/wikilinks.ts` (bare `[[slug]]` fallback for unknown slugs); Phase 13 (vision `02` §4.8 + `05` §2): the single sparse rule `isSparseEntity({mentions, relationships, claims})` (≤2 mentions AND no claims AND no relationships) lives here; the writer emits `sparse: true` frontmatter (immediately after `aliases`, before `wiki`/`sources`) when the rule holds and never emits `sparse: false`; `enforceSparseInMarkdown(markdown, sparse)` re-imposes/removes the field over a fully-rendered page (the UAT 6.3 `enforceAliasesInMarkdown` precedent, but entity-only so it lives here rather than in `utils/aliases.ts`); `EntityPageData.sparse?` carries the aggregate-derived flag to the ingest synthesis write points; Phase 14 (vision `05` §2): `EntityPageData.mergedAliases?` carries every curation-merged variant title, and the writer computes `aliases:` via `combinedAliases(title, slug, mergedAliases)` so `[[<variant title>]]` resolves to the canonical page; Phase 17 (B10/B1/B2, vision `02` §4.3 B + `05` §2 + `06` §2-§3/§7): `EntityPageData.incomingRelationships?` (subject, predicate, evidence, page, source, pages — the object is the page's own entity) carries the bidirectional mirror, rendered in `## Relationships` after the outgoing lines as `- [[subject|Title]] — Predicate (incoming) — "evidence" [^srcN]` with the evidence verbatim; `buildCitationMap` and the frontmatter `sources` aggregation cover incoming records AFTER mentions/outgoing/claims so no-incoming pages keep pre-Phase-17 key numbering; the sparse rule reads the OUTGOING `relationships` only (ratified 2026-07-28: incoming does not clear sparse); the field is omitted (undefined) when empty so the Phase 16 aggregate fingerprint is unchanged for pages with no incoming edges; `enforceFrontmatterInMarkdown(markdown, pageData)` builds the COMPLETE deterministic frontmatter (title, type, aliases via combinedAliases, sparse, wiki, real-write-time `updated`, full aggregated sources, tags — one shared `buildEntityFrontmatter` helper with the writer) and writes it OVER the model's frontmatter (fabricated fields dropped), CREATING the block when absent (the body is preserved byte-for-byte; unparseable model frontmatter → returned unchanged); `enforceSourcesSectionInMarkdown(markdown, citationMap)` rebuilds the `## Sources` definitions deterministically in basename + page-range form, replacing the model's definitions (in-prose `[^srcN]` markers byte-identical; the section is replaced up to the next `##` heading or appended when missing)
* `pages/composite-page.ts` — Phase 22 (§2.2–§2.3, the five-class rollup amendment — vision `02` §4.6 / `05` §6 amended 2026-07-29, user-ratified; backlog B22): the COMPOSITE page kind — 2-4 logically-mapped entities (one ratified rollup class) pooled into ONE rich article while identities stay in the graph. `CompositePageData` carries the `into` slug/folder, the ratified `class` (1-5), a `members` block (slug, title, type, optional role — class-3 derives indicator/concept — significance, disambiguation, aliases), and the unioned MEMBER-TAGGED evidence (`memberEvidence`: per-member mentions/relationships/incoming/claims/timeline/contexts — every item keeps its member association). `writeCompositePage` renders the deterministic shell: complete frontmatter (`type: composite`, class, members block, aliases union via `combinedAliases`, wiki, real `updated`, aggregated sources over the union, member-type tags; `sparse` NEVER emitted — composites are rich by construction), the `## Members` section (name, type, role, significance per member), the standard Layer 2 sections with `### <Member Title>` evidence groups, and basename `## Sources`; a fellow member renders as plain title text (never a self-link), external slugs as pipe-form wikilinks. `buildCompositeCitationMap` is the ONE union citation map behind the writer, the preservation check, and the synthesis `citationMap` slot; `enforceCompositeFrontmatterInMarkdown` is the Phase-17-precedent re-imposition over model output (created when absent; body byte-identical). The Phase 16 fingerprint over this data is a hash over { members, unioned evidence, language } — member-set changes flip it exactly once, unrelated pages never touch it
* `pages/topic-page.ts` — deterministic `topics/<subfolder>/<slug>.md` writer grouped by claim type (phase doc §2.2); Phase 5 additive: optional `entities` and `context` fields for topic synthesis; claim entity links use the same pipe form as the entity-page writer; Phase 17 (B1/B2): the topic equivalents of the entity enforcers — `enforceTopicFrontmatterInMarkdown(markdown, pageData)` (complete deterministic frontmatter via the shared-with-writer `buildTopicFrontmatter`; created when absent; never carries `sparse`) and `enforceTopicSourcesSectionInMarkdown(markdown, citationMap)` (delegates to the entity-page implementation)
* `pages/comparison-page.ts` — Phase 23 (§2.2, backlog B21 — comparison-table articles; vision `05` §9 + `03` §3.1 extended): the COMPARISON page kind — ONE article per comparison-table SUBJECT at `comparisons/<slug>.md`. Identity keys on the canonical subject-entity slug when resolvable through the curated aggregate (a renamed/renumbered table reconciles onto ONE page through the entity's canonical identity; `slugify(table title)` is only the fallback when the subject is unknown — never the drifting title alone). `ComparisonPageData` carries the subject, the dated `ComparisonTableSection[]` (source, chunk pages, table page, caption, dimensions, entities, the extractor-reconstructed markdown — structure the extractor's, VALUES the PDF's — and summary), and the deterministic `ComparisonBridgeEntry[]` (exactly the claims sharing the table's entities, from live curated topics). `writeComparisonPage` renders the deterministic shell: complete frontmatter (`type: comparison`, title, aliases union over every distinct table caption via `combinedAliases`, wiki, real `updated`, aggregated sources, `comparison` tag; `sparse` NEVER emitted), one dated `## Table: <source-basename>, p. <page>` section per source table with the markdown preserved verbatim as that source printed it (cross-PDF drift EMBRACED, never force-merged), per-table entity links (known slugs pipe-form; the page's own subject PLAIN — the composite fellow-member precedent; unknown slugs plain, never broken links), the deterministic `## Related comparisons in prose` bridge (claim text + live topic link + shared-entity links, honest empty form `- No prose claims in the corpus reference this comparison's subjects yet.`), and basename `## Sources`. `buildComparisonCitationMap` is the ONE map behind writer/preservation/synthesis (table sections cite `file|<table page>`, bridge claims cite `file|<chunk pages>`); `comparisonRowValues` parses a markdown table into per-row subject + numbers (the gate-23.5 vocabulary); `enforceComparisonFrontmatterInMarkdown` and `enforceComparisonBridgeInMarkdown` are the Phase-17-precedent re-impositions over model output (frontmatter complete/created; the deterministic bridge replaced or inserted before `## Sources`)
* `pages/document-page.ts` — Phase 5: `DocumentPageData` interface for document-page synthesis (chunk title, source, page range, extracted text, entity slugs, and claims)
* `materializer.ts` — deterministic Layer 3: reads `.state/extracted/*.json`, aggregates entities/topics, writes pages, updates rolling memory (phase doc §2.3); Phase 5 additive: returns `MaterializeResult` with `EntityPageData`, `TopicPageData`, and `DocumentPageData` for each page so the Synthesis Writer can use them without re-parsing markdown; Phase 9 additive: logs structural changes (vision `03` §5) before saving rolling memory — new folders (diff vs previous rolling memory) and new entity page types (diff vs the log's `knownPageTypes`) are appended to `.state/proposals/structural-changes.json` exactly once each; Phase 8 additive: conflict detection — optional `MaterializeOptions.pageHashes` (wiki-relative path → recorded SHA-256); before overwriting an entity/topic page whose recorded hash differs from the current on-disk hash the page is treated as manually edited: the write is SKIPPED, a manual-edit entry is appended to `.state/conflicts.json`, the page is excluded from `entityPages`/`topicPages` (so the Synthesis Writer never overwrites manual edits either), and the path is reported in `MaterializeResult.conflicts`; successful writes are reported in `MaterializeResult.writtenPages` (`{path, hash}`) so the ingest command can persist the post-run hashes. Note: re-deriving every page from the full extraction set IS the Phase 8 update-mode merge (mentions accumulate across chunks with a composite dedupe key that preserves same-text-different-page; relationships/claims dedupe by content keys; Sources accumulate all contributing files). **Folder-resolution rule (2026-07-21 UAT fork fix, vision `03` §3.2 + `04` §3.2 Step 6):** "first folder assignment wins" applies ACROSS runs — a slug already in the PREVIOUS rolling memory keeps its recorded folder as canonical (extraction folder fields for that slug are ignored for page placement, the divergent folder is never added to `folderStructure` on that entity's account, and rolling memory never moves an existing entity); within a run the first extraction's folder wins for new slugs. **Fork reconciliation:** after the entity writes, any page for the same slug at a NON-canonical folder under `entities/` is a fork duplicate — an unmodified one (on-disk hash matches `pageHashes`) is DELETED and reported in `MaterializeResult.removedDuplicates`; an edited or untracked one is KEPT and logged as a manual-edit conflict (with a duplicate-specific reason) for journalist review. Phase 13 additive: `MaterializeResult.entityPages` data carries the aggregate-derived `sparse` boolean (`isSparseEntity` over the deduped mentions/relationships/claims), recomputed from the full extraction set every run so the flag appears and disappears as the evidence grows (Phase 8 update mode). **Phase 14 curation stage (phase doc §2.2–§2.5, vision `04` §3.2 Step 6):** the pipeline is aggregate → curate → apply → write — aggregation completes fully in memory, then (only when `MaterializeOptions.curation === true` AND extraction data exists AND at least one candidate exists) the two curation calls run in parallel over candidates built from the aggregate PLUS the on-disk sets (update runs re-curate everything — the self-healing property); validated decision lists are applied all-or-nothing BEFORE any page write: topic merges union claims (identical claim TEXTS deduped, title from `into`), topic drops remove only the grouping (claims stay on entity/document pages), entity merges union mentions/relationships/claims/timeline (existing content-key dedupe), rewrite relationship subject/object + claim/timeline entity slug references globally, and accumulate every variant title into the survivor's `mergedAliases` (canonical folder = into-folder); merged-away/dropped on-disk pages are deleted deterministically with their now-empty folder chains pruned up to (never past) the section root; a from-page whose recorded `pageHashes` entry is missing or mismatched is VETOED out of its merge/drop (logged as a manual-edit conflict with a curation reason, reported in `manualEditSkips`, treated as keep for the rest of the application); a `null` decision list (the keep-all fallback) writes every candidate as pre-Phase-14 with a console warning per fallback; after the page writes, an exact-segment wikilink rewrite pass repoints `[[from|…]]`/`[[from]]` across all pre-existing entity/topic/document pages (never DOX indexes, which regenerate). The stage reports in `MaterializeResult.curation` (`CurationSummary`: applied merges/drops, fallbacks, attempts, vetoes, manual-edit skips, removedPages, rewrittenLinks) and writes `.state/curation-report.json` AFTER rolling memory is saved (memory therefore always reflects the curated set); when the stage is skipped the result has no `curation` field and materialize() is byte-identical to pre-Phase-14 (the gate-14.14 regression guard). Test seams `curateTopicsFn`/`curateEntitiesFn` mirror the `writeDoxIndexFn` precedent. **Phase 21 (§2.1–§2.3, backlog B5):** the stage is now sticky → detect → auto-apply → propose → model — (1) `.state/curation-decisions.json` is PRE-APPLIED before candidate construction (`collapseStickyMerges` union-find seeded from active records; sticky drops removed; `splits` slugs and neverMerge pairs un-apply their records into `splitReversals`), so the model only judges unstuck candidates and a merged-away page can never reappear (gate 21.6); (2) `detectPreMergePairs` (`agents/pre-merge.ts`) runs per concern over the candidates (entity candidates now carry `aliases` from merged variant titles ∪ on-disk frontmatter) with the document pages' text as the abbreviation corpus; (3) the AUTO tier (transliteration + alias) applies with no LLM call through the same merge-application closures as model merges, minus manual-edit vetoes; (4) proposed pairs leave the open candidate list and ride the curation calls' `proposedPairs` option (confirm-deny); (5) every applied decision of the run is appended to the decisions file with its signal (`appendCurationDecisions`, deduped, runId = the report's `run` timestamp; split slugs touching a new record are consumed). ONE closure pair (`applyEntityMergeToMaps`/`applyTopicMergeToMaps`) implements the merge application for all three tiers; the manual-edit veto filter guards every tier's from-pages. `CurationSummary` gains `fromSticky`/`autoApplied`/`proposedPairs`/`denials`/`splitReversals` (legacy fields keep their decided-this-run shape), and the report distinguishes `fromSticky` vs `decidedThisRun` with per-merge signal + evidence. **Phase 16 synthesis-resume preservation (vision `04` Step 9):** before any entity/topic page write, the skip-eligibility check runs against `.state/synthesis-state.json` — a strict/permissive record whose `dataHash` matches the current aggregate fingerprint (via the same exported `pageDataHash` the ingest skip rule uses) preserves the finished page BYTE-FOR-BYTE: no write, the page still lands in `entityPages`/`topicPages` (the ingest skip rule needs the data), and the path + current on-disk hash are reported in `MaterializeResult.preservedPages` so the recorded page hashes converge to the preserved content. The check runs BEFORE the manual-edit conflict path (preservation never overwrites anything, so a human edit to such a page is kept too) and requires the file to exist (a deleted skip-eligible page is rewritten as the template and re-synthesized — self-healing). With no records on disk every check falls through to the byte-identical pre-Phase-16 path. **Phase 17 (B10, vision `02` §4.3 B):** after the subject-attach loop, a mirror loop attaches an INCOMING record (subject, predicate, evidence, page, source, pages) to the OBJECT entity's aggregate — same skip-on-unknown guard, self-loops attach once as outgoing only; entity curation unions incoming records into survivors and the slug-remap pass rewrites their subjects; at page assembly incoming records are deduped (same content-key rule as outgoing) and any record that a curation merge collapsed into a same-page self-loop is dropped so the loop renders once; `EntityPageData.incomingRelationships` is omitted when empty so no-incoming pages keep their pre-Phase-17 aggregate fingerprint (skip-eligible pages stay byte-stable). **Phase 19 (B19, vision `07` §3):** `checkPageConflict` now takes the deterministic render of the current aggregate and returns `write | converge | conflict` — disk != recorded but disk == current render means the page is provably tool-written (stale recorded hash) and CONVERGES (reported in `MaterializeResult.convergedPages`, a convergence note logged per page, the write proceeds so the fold records the disk hash, never a conflicts.json entry); anything else conflicts exactly as before — human-edit protection is not weakened (curation veto filter and fork reconciliation untouched). **Phase 22 (§2.1–§2.2, the five-class rollup amendment; backlog B22):** the stage gains CLUSTER decisions on the same machinery — sticky cluster records (`action: 'cluster'`, members re-derived as `[into, ...from]`) pre-apply before candidate construction (members excluded from candidates, gate 22.6); the deterministic families propose class-5 region name-form families and class-3 indicator↔concept pairs (`detection.proposedClusters`, members leave the open list and ride the entities call's `proposedClusters` like pairs); judgment classes 2/4 arrive as open clusters in the validated `decisions.clusters`. Applying a cluster (`applyEntityClusterToMaps`) pulls every member OUT of entityMap (member pages are NEVER written; their on-disk pages delete like merged-away pages) into `clusterMap`, remaps member-slug references to the `into` slug via the shared `entitySlugRemap`/`mergeRewrites` passes (member-targeted wikilinks across pre-existing pages rewrite to the composite), and the composite page is written at the `into` member's folder/slug with the unioned member-tagged evidence (intra-cluster relationship mirrors dedupe like the entity self-loop rule; the frontmatter aliases union every member title). The manual-edit veto is all-or-nothing per cluster; a reversed cluster (`splits`/neverMerge) deletes the composite page only when the on-disk page IS `type: composite` (member pages rebuilt, gate 22.7); re-applying an already-pooled `into` is a defensive no-op so a duplicate application can never clobber pooled evidence with empty on-disk-meta aggregates (production never reaches it — members leave the candidate set). `MaterializeResult.compositePages` carries the structured data (additive; empty pre-Phase-22); `CurationSummary` gains `entityClusters`/`proposedClusters`/`clusterDenials`/`fromSticky.entityClusters`; rolling memory tracks composites at their `into` slug; recording stores class + rationale on the cluster record. **Phase 23 (§2.2, backlog B21):** comparison-page assembly rides the same machinery — extracted `tables` are collected during aggregation with their chunk's source provenance, then assembled AFTER curation (subject-entity identity resolves through the canonical, post-remap slug set incl. composite `into` slugs via `slugToTitle`): one page per subject, each source's table deduped (source|page|title|markdown) and sorted (source, page) into dated sections; the deterministic prose bridge is computed from the CURATED topic claims (exactly the claims sharing the table's entities); pages write at `comparisons/<slug>.md` with the same Phase 16 resume preservation (fingerprint over the ComparisonPageData aggregate) and Phase 8/19 conflict/convergence postures as the other loops; `MaterializeResult.comparisonPages` additive (empty pre-Phase-23); `comparisons` joins the folder structure + the structural-change log (with the page count and the tables' entities) and the curation wikilink-rewrite scope
* `dox-writer.ts` — the DOX Writer, Layer 5 (Phase 6): scans `wikis/<slug>/` (excluding `raw/` and `.state/`), builds a tree of folders and markdown files, and writes `index.md` navigation contracts for every folder plus the wiki root. Two modes: deterministic (default, `doxLlm` absent/false — generic template descriptions, no LLM calls, byte-stable behavior) and LLM-driven (`doxLlm: true` — one LLM call per folder plus the root via `prompts/dox-writer.prompt.txt`, `callType: 'dox-writer'`, `maxTokens: 8192` (Phase 13 `DOX_WRITER_MAX_TOKENS`, up from 2048 — the same vision `04` §6 safety-ceiling doctrine), folder context includes children, statistics, full contents of the folder's OWN direct pages, the wiki AGENTS.md, and rolling memory). Bottom-up synthesis (user-directed 2026-07-19 refinement): folders are processed deepest-first post-order at every level, and every folder with child folders — the root included — also receives `childIndexes` (the freshly-written full `index.md` of each direct child folder, written earlier in the same run) so parents synthesize a summary-of-summaries; the root's top-folder indexes flow through the same `childIndexes` mechanism. Catalog rule: the `## Pages` section catalogs direct pages AND child folders; a folder with sub-folders but zero direct pages still catalogs its child folders instead of emitting "No pages in this folder." 2026-07-25 navigation fix (user-ratified; BACKLOG B3 + the root-hub gap found in the same `dist/wikis/rkkp-adhd` audit): the wiki ROOT also carries a complete `## Pages` catalog of its four top-area indexes alongside the curated `## Start Here` (in the prompt, the wiki-level example, and the deterministic `buildFolderBody`), and catalog completeness is deterministically enforced — `missingCatalogLinks` verifies every supplied catalog target (direct pages + child-folder indexes; the root's four top-area indexes) appears as a wikilink target in the body and rejects self-referential index links (a catalog entry pointing at the folder's own `/index`), so a human or agent can reach every page by following only the parent/child index chain; violations are content defects that join the missing-section errors of the Phase 12 reask loop before the deterministic fallback (which always catalogs everything). All catalog/navigation links use the Obsidian-native pipe form (user directive 2026-07-20): pages `[[<basename>|<Title>]]`, child/parent/sibling folder indexes `[[<folder-path>/index|<Title>]]`, wiki-root parent `[[index|<Wiki Title>]]` — implemented via `utils/wikilinks.ts`. Wikilink safeguard (2026-07-19 refinement, pipe-form rework 2026-07-20): the prompt supplies the exact resolvable pipe-form link for every catalog/navigation target (`linkText` computed deterministically; additive `parentLinkText`/`siblingLinkTexts` context fields), and the LLM body is post-processed before writing — every `[[...]]` is tested with semantics mirrored from `validation/link-checker.ts` (path map + slug map + folder-index/root fallbacks); pipe-form links with resolvable targets are kept verbatim, resolvable-but-bare legacy links are NORMALIZED to the canonical pipe form, unresolvable links that uniquely match a page title (case-insensitive) are rewritten to that page's canonical pipe form, and anything else is de-linked (brackets stripped, display text kept); one summary warn line per folder at most. Deterministic enforcement over the LLM output (vision `03` §6): the LLM's frontmatter is always discarded (title, type, wiki, updated, children are re-imposed) and its `## Statistics` section is replaced with the deterministic counts; Phase 12 (reask): the folder/root LLM paths run through `runDoxLlmWithRetries` → `runWithFeedbackRetry` — unparseable output or missing required sections (`missingRequiredSections`; folder: `## Pages` + `## Navigation`, root: `## Start Here` + `## Pages` since the 2026-07-25 navigation fix, all levels: `# title` + `## Statistics`) or an incomplete/self-referential catalog (`missingCatalogLinks`, same fix) is a content defect retried ≤3 total attempts with the exact missing-section/missing-catalog-link list fed back as a correction block before the per-target deterministic fallback, while a THROWN error (HTTP 4xx — never retried — or exhausted transient retries) is caught ONCE per target and falls straight to the deterministic contract, so a missing key still never crashes an ingest. `writeDoxIndexFn` (test-only) replaces the LLM call; `logPath` defaults to `<wiki>/.state/llm-calls.json`. Never creates folders, moves pages, writes content pages, or decides taxonomy. Phase 7 additive: `WriteDoxOptions.language { input, output }` flows into `DoxIndexContext.language` and fills the folder prompt's `{languageDirective}` (vision `04` §9 — index prose in the output language); deterministic mode is unaffected. **Workspace pass (2026-07-20 amendment; per-wiki segments 2026-07-21):** `writeWorkspaceIndex({ workspace?, wikiSlug, doxLlm?, writeWorkspaceIndexFn?, outputLanguage?, logPath? })` re-composes `wikis/index-of-indexes.md` at the end of every ingest from PER-WIKI SEGMENTS — the topmost parent in the same bottom-up chain. The pass writes ONLY the triggering wiki's own contribution: one LLM call (`prompts/dox-writer-workspace.prompt.txt`, a per-wiki entry prompt whose only content input is the triggering wiki's freshly-written root index.md) produces a 1–3 sentence description in the run's output language, used for both the wiki's prose segment (`<!-- segment:<slug> -->` markers) and its `## Wikis` catalog line. Every other wiki's segments are preserved BYTE-FOR-BYTE (parsed by `parseWorkspaceSegments`; pre-segment files are migrated by seeding prose from existing catalog descriptions); wikis removed from disk lose their segments; missing segments get a fixed placeholder. Deterministic over ALL wikis every run: children (`<slug>/index.md` per wiki), statistics (wiki count + corpus totals), the fixed English framing line, and frontmatter (no `wiki` field). Fallback is per-wiki: LLM failure → the first prose paragraph of the triggering wiki's root index; Phase 12 (reask): an EMPTY entry response is a content defect retried ≤3 total attempts via `runWorkspaceEntryWithRetries` → `runWithFeedbackRetry` (feedback: 'the response was empty; return the complete requested text'), while a thrown error is caught ONCE and also yields the deterministic description. The LLM writes prose only — links are added deterministically, so no wikilink repair is needed at this level. **Prose model (2026-07-21b amendment):** the page's top prose is a coherent cross-wiki synthesis (`prompts/dox-writer-workspace.prompt.txt`, ALL wikis' root contracts) regenerated ONLY when the wiki set changed since the file's frontmatter children (add/remove) or no `<!-- workspace-prose -->` block exists — routine ingests preserve it byte-for-byte; the `## Wikis` catalog stays per-wiki owned (`prompts/dox-writer-workspace-entry.prompt.txt`, one line per wiki in that wiki's own language). Fallbacks are per-concern: `deterministicWorkspaceProse` for the prose, root-index first paragraph for the catalog line. Phase 22 (§2.4): `type: composite` pages catalog with their member names — `scanFolder` reads the frontmatter `members` (`FileNode.compositeMembers`) and the deterministic folder body appends `— composite of <A, B>` to the composite's catalog line (the composite title itself already carries the member names). Phase 23 (§2.4, gate 23.6): the `comparisons/` top-level folder joins the bottom-up chain — generic scanning/index-writing covers `comparisons/index.md` automatically, and the wiki ROOT's deterministic re-imposition includes it when (and only when) the folder has content: `buildChildrenList` appends `comparisons/index.md` to the root children, the deterministic root body's `## Pages` catalogs `[[comparisons/index|Comparisons]]`, and the root statistics gain `Comparison pages: N` — all conditional, so table-less wikis stay byte-identical (no ghost entries, UAT 23.2)
* `validation/link-checker.ts` — Phase 4: deterministic wikilink check; reports broken links and orphaned pages (excludes `index.md`, `sources/*.md`, and the wiki constitution `AGENTS.md`); Phase 5 additive: folder indexes resolve as `[[Folder Name]]` so DOX navigation links work; 2026-07-20 pipe-aware semantics: the part before the first `|` is the resolution target, matched by exact vault-relative path (without `.md`, suffix tolerated) first, then by the slugified-basename map with the folder-index/wiki-root fallbacks — legacy bare forms (`[[Page Title]]`, `[[slug]]`, `[[Folder Name]]`) stay resolvable for older wikis; Phase 17 (B12b, vision `02` §2 + `07` §2.5): the parse loop also tallies OUTGOING wikilink counts per page (every `[[...]]` occurrence, resolvable or not) and the result gains `islands: string[]` — `entities/`/`topics/` pages (per `wikiRelative`) with zero outgoing wikilinks, `index.md` exempt, detection-only in the same posture as orphans (`orphaned` semantics unchanged); Phase 23 (§2.4, gate 23.6): comparison pages are content pages for link/orphan/island purposes — `comparisons/` joins the island scope; Phase 20 (B20): the walk + resolution maps are the exported SHARED SLUG UNIVERSE — `SlugUniversePage` (adds frontmatter `aliases` via gray-matter), `buildSlugUniverseForDir`/`buildSlugUniverse` (the latter takes an optional `{ language }` for alias slugification — the checker never sets it, so its semantics are frozen byte-identical) and `resolveWikilinkTarget(universe, target)`; `checkLinks` consumes them with unchanged behavior
* `validation/citation-checker.ts` — Phase 4: deterministic `[^srcN]` citation check; requires a matching definition in the page and a source PDF in `raw/`; Phase 17 (§2.6, vision `07` §2.5): report-only `missingFrontmatterSource` — every distinct in-prose `[^srcN]` key (definition lines excluded by lookahead) on entity/topic pages must be covered by the page's frontmatter `sources`, matched by source-file basename between the definition and the frontmatter `file` entries; absent/unparseable frontmatter covers nothing
* `validation/schema-validator.ts` — Phase 4: deterministic YAML frontmatter check (`title`, `type`, `updated`) using `gray-matter`; excludes the wiki constitution `AGENTS.md`. Phase 22 (§2.4): `composite` joins the known types, and `type: composite` requires a `members` list of 2-4 entries (each with a slug) plus a `class` field (integer 1-5). Phase 23 (§2.2): `comparison` joins the known types (no extra required fields — the complete deterministic frontmatter covers title/type/updated)
* `validation/index.ts` — Phase 4: `validateWiki()` aggregator and `logValidation()` console reporter; additive Phase 5 file logging: `logValidation(summary, wikiDir?)` also writes `wikis/<slug>/.state/validation-report.json`, and `writeValidationReport(wikiDir, summary)` writes the same file without console output; Phase 17: the link summary line gains the islands count and per-island warnings (`Island page (no outgoing links): <path>`, same posture as orphans), and the citation summary/warnings count `missingFrontmatterSource` alongside invalid + missingSource
* `agents/extractor.ts` — Phase 2: the Extractor (Layer 2). `extractChunk(chunkText, pageRange, sourceFile, agentsMd, existingFolders, existingEntities, options?)` → extended `ExtractorResult` (entities/relationships/claims + `timeline`, `context`, per-entity `significance`, optional `disambiguation` — gates 2.9-2.12 extension); typed `ExtractorError` (raw response on invalid JSON, issue list on schema failure); fence-tolerant JSON parse; deterministic `slugify()` normalization of all slugs before validation; extraction calls use `maxTokens: 32768, temperature: 0` (raised from 16384 on 2026-07-24, user-ratified, after a dense chunk's extraction JSON structurally exceeded the old ceiling — the reask cannot repair truncation); Phase 12 (reask): invalid JSON and schema-validation failures are content defects retried ≤3 total attempts via `llm/reask.ts` with the validator's exact errors (the parse message or the issue list) fed back as a correction block appended to the unchanged prompt — exhaustion throws the `ExtractorError` fail-loud (the ingest aborts), and thrown transport/HTTP errors (incl. 4xx) are never quality-retried and propagate immediately; Phase 7 additive: `options.language { input, output }` fills the prompt's `{languageDirective}` (vision `04` §9) and `normalizeExtractorSlugs(data, language?)` transliterates with the input language before slugifying — entity slugs, relationship/claim/timeline slug references, `entity.folder` segments, and `claim.type` (the topic-slug source; gate 7.9, UAT 7.2 fix) — absent → en/en → byte-identical behavior; Phase 23 additive (§2.1, backlog B21): the optional `tables` output (comparison tables — `subject`, `title`, `page`, `rowDimension`, `colDimension`, `entities`, `markdown`, `summary`) rides the same normalization (`table.subject` + `table.entities` slugified with the input language) and the agent always fills the array (`[]` when the model omitted it) — pass-through only, no new pipeline behavior
* `validation/extractor-schema.ts` — Phase 2: non-throwing `validateExtractorResult(data, pageRange?)` → `{ valid, issues }` per phase doc §2.4 + extended fields; folder prefix `^entities/|^topics/`, no `..`, ≤4 segments, page-range checks (skipped for unparseable ranges). Phase 23 (§2.1, gate 23.1): the result gains an additive `warnings` channel (never affects `valid`, never feeds the reask loop); the optional `tables` array is validated when present — `title`/`page` (in range)/`markdown` non-empty required, `subject`/dimensions/`summary` free-text, and a table's `entities` naming a slug the chunk did not extract WARNS but passes (tables may mention unextracted names); an absent `tables` field stays valid (pre-Phase-23 JSON compatibility)
* `validation/preservation-check.ts` — Phase 5: `checkPreservation` (entity) and `checkTopicPreservation` (topic) verify synthesis did not drop any mention context, relationship evidence, claim text, or existing source citation; Phase 17 (B10): incoming relationship evidence is required exactly like outgoing (dropped incoming evidence is named in `droppedRelationships` — data-driven, no structural change beyond the new field); Phase 18 (B18): both result interfaces gain `extraMarkers: string[]` — off-map `[^srcN]` keys found in the written page (key + first line, first-appearance order, definition lines included; allowed set = the deterministic map keys plus `originalData.citations` for entities) — and `passed` is false when non-empty; markers are NEVER stripped/renumbered (reask-only, phase doc §2.3). Phase 22 (§2.4): `checkCompositePreservation` — the same verbatim-substring mechanism iterated PER MEMBER over the unioned composite evidence (every member's mentions, outgoing + incoming relationship evidence, claims, and the union citation map keys via `buildCompositeCitationMap`; off-map markers included) — one dropped item of any member fails the page. Phase 23 (§2.3/§2.4, gate 23.5): `checkComparisonPreservation` — ROW-VALUE preservation, NOT byte-substring (the table markdown structure is the extractor's reconstruction; the VALUES are the PDF's): for every data row of every dated table section, the row subject (first cell) and each of the row's numbers (`comparisonRowValues` from `pages/comparison-page.ts`) must appear in the emitted `## Table:` section (scoped to the section naming the source + page, whole-page fallback when the heading was renamed); a dropped row, a dropped value, or an ALTERED number is a content defect named in `droppedRowValues` (`<source>, p. <page> row "<subject>": value "<number>"`) for the reask loop; citations + off-map markers follow the entity/composite checks exactly
* `agents/synthesis.ts` — Phase 5: the Synthesis Writer (Layer 4). `writeEntitySynthesis` and `writeTopicSynthesis` read their type-specific prompts (`synthesis.prompt.txt`, `synthesis-topic.prompt.txt`), inject page data + wiki constitution, and call `callLLM(prompt)` with the shared Phase 13 `SYNTHESIS_MAX_TOKENS = 32768` output-token safety ceiling (one module constant behind all four synthesis call sites; vision `04` §6 — a ceiling, never a length controller; the model never sees the value); output is a two-layer markdown article with synthesis at the top and preserved detail sections below. Additive permissive fallbacks: `writePermissiveEntitySynthesis` and `writePermissiveTopicSynthesis` read the corresponding permissive prompts and allow the model to summarize in Layer 1 while preserving the exact structured data in Layer 2; used by `ingest.ts` when strict synthesis fails preservation. Document pages are not synthesized because full verbatim extracted-text preservation is impractical for real chunks; they keep their deterministic Phase 1 format. Phase 7 additive: all four functions take an optional `language { input, output }` fourth parameter and fill `{languageDirective}` via the shared prompt builder (vision `04` §9 — Layer 1 in the output language, Layer 2 verbatim); Phase 12 additive: all four take further optional trailing `feedback`/`attempt` parameters — when `feedback` is present it is appended to the filled prompt as the correction block and the log context becomes `<ctx>#attempt<N>`; the retry loop itself lives in `ingest.ts` (`trySynthesisMode` → `runWithFeedbackRetry`): a preservation-check failure is a content defect retried ≤3 total attempts with the checker's exact failure strings fed back before the chain moves on (supersedes the blind same-prompt retry of Phase 7 v1.1.0; attempt counts still recorded in the synthesis report), while thrown transport/HTTP errors propagate and are never quality-retried; Phase 17 (B10/B12a, vision `02` §4.3 B + §2): `formatRelationships(relationships, incomingRelationships?)` presents both directions with clear direction labels (`Outgoing (this entity is the SUBJECT of these relationships):` / `Incoming (this entity is the OBJECT of these relationships):`; byte-identical to pre-Phase-17 when there are no incoming records), and `buildEntitySynthesisValues` gains the `relatedEntities` slot — `buildRelatedEntities` (deterministic, deduplicated, sorted `{slug, title}` union of every relationship subject/object both directions and every claim co-entity, minus the page itself; titles via `slugToTitle` with the slug as fallback) rendered by `formatRelatedEntities` as `- <slug> — <Title>` lines (documented empty form `(none)`) — the two entity-synthesis prompts require wikilink targets to come from this list; Phase 18 (B18, vision `06` §1-§3/§7): `formatCitationMap(citationMap)` renders the deterministic map as `[^srcN]: <basename>, pages <range>` lines in assignment order (empty form `(none)`), and BOTH value builders gain the `citationMap` slot via `buildCitationMap` — all four synthesis prompts now teach the model the legal keys (the `=== CITATION KEYS ===` section); the topic prompt's pre-existing file-sorted `{sources}` block is kept byte-identical, with CITATION KEYS stated as authoritative. **Phase 22 (§2.3 + gate 22.10):** the composite writers `writeCompositeSynthesis`/`writePermissiveCompositeSynthesis` (prompts `composite.prompt.txt`/`composite-permissive.prompt.txt`, callTypes synthesis/permissive-synthesis, same ceiling + feedback/attempt contract) with the composite value builders — `formatCompositeMembers` (the members slot), `formatCompositeMemberEvidence` (per-member evidence groups in the standard Layer 2 shapes), `buildCompositeRelatedEntities` (the cluster-wide legal wikilink targets: union across all members' evidence minus the composite and its own members), and the `citationMap` slot via `buildCompositeCitationMap` over the unioned evidence; `sparse` never applies. Gate 22.10: the ingest synthesis stage IS wired for composites (the third pooled stage in `commands/ingest.ts`, mirroring the entity/topic stages exactly). **Phase 23 (§2.3, backlog B21):** the comparison writers `writeComparisonSynthesis`/`writePermissiveComparisonSynthesis` (prompt `comparison.prompt.txt` — ONE file per the phase doc's file list; its Layer-2 contract is already the row-value preservation contract, so the permissive leg reuses it, distinct `permissive-synthesis` callType kept) with the comparison value builders — `formatComparisonTables` (one dated block per source table), `buildComparisonRelatedEntities` (table + bridge entities minus the page's own subject), and the `citationMap` slot via `buildComparisonCitationMap`; `sparse` never applies. The ingest synthesis stage IS wired for comparisons (the fourth pooled stage in `commands/ingest.ts`, mirroring the 22.10 composite stage exactly)
* `agents/agents-updater.ts` — Phase 9: the AGENTS.md Updater. `proposeAgentsUpdate(wikiSlug, { workspace?, callLLMFn?, logPath? })` reads the current wiki constitution, the completed wiki structure (`collectWikiStructure` — folders + page types from frontmatter, excluding `raw/`/`.state/`), and the structural change log; fills `prompts/agents-updater.prompt.txt` and makes ONE LLM call (`callType: 'agents-updater'`, `maxTokens: 8192`, `maxRetries: 2` for transient failures). Phase 12 (reask): content-defect failures — a proposal missing required sections `## Folder Structure`/`## Page Types`/`## Language` or under 200 chars (`validateProposal`) — are retried ≤3 total attempts via `runWithFeedbackRetry` with the validator's exact errors fed back as a correction block, then the deterministic fallback (current AGENTS.md + a `## Proposed Additions (deterministic fallback)` section listing the new folders/page types); a THROWN error (HTTP 4xx, transport) is caught ONCE and falls straight to the same deterministic fallback — never quality-retried. Deterministic enforcement: the proposal's `## Language` section is always re-imposed VERBATIM from the current constitution (Phase 7 binding); code fences are stripped. The proposal is saved to `.state/proposed-agents.md` — the original AGENTS.md is NEVER overwritten (gate 9.4; applying is a human action by manual copy — the Phase 9 TUI review screen was deleted in Phase 11). `callLLMFn` (test-only) keeps gates LLM-free
* `agents/curation.ts` — Phase 14: the Curation agent (L3 + L3e, vision `04` §3.2 Step 6 + `05` §7/§6). `curateTopics(candidates, options)` (merge same-theme duplicates, drop meta-descriptor non-topics, keep the rest) and `curateEntities(candidates, options)` (MERGE-ONLY under strict identity; the `unsure` bucket folds into keep) run inside the materializer between aggregation and page writes. Input builders emit every aggregated candidate PLUS the on-disk set with ≤3 sample claims / ≤2 sample mentions (each truncated to ~200 chars) and an `onDisk` flag; the two calls run in parallel via `Promise.all` with `callType: 'curation'` and the Phase 13 `SYNTHESIS_MAX_TOKENS = 32768` ceiling. Deterministic validation (`validateTopicDecisions`/`validateEntityDecisions`, exported for tests) rejects: unknown slugs, a slug in two buckets, an input slug missing from every bucket, an `into` that is dropped, self-merges, and merge chains with no unique survivor — union-find collapses legitimate chains (A→B, B→C) BEFORE bucket accounting, and `.state/curation-overrides.json` neverMerge pairs are vetoed into keep before collapsing. Content defects re-ask ≤3 total attempts via `runWithFeedbackRetry` with the exact offending entries fed back; every failure mode (validation exhaustion, exhausted transient retries, immediate HTTP 4xx) lands on the keep-all fallback — `decisions: null` on the single-call path, the materializer writes all candidates as pre-Phase-14 (no data loss, self-healing because the input includes the on-disk set). Scaling (`CURATION_SINGLE_CALL_LIMIT` = 250 candidates OR, since Phase 16, `estimateDecisionListTokens(candidates) >= CURATION_SIZE_TRIGGER_TOKENS` = 24576 — the decision-list size estimate approaching the 32768 ceiling triggers bucketing even below 250, because truncation is a failure the reask cannot repair): deterministic lexical-stem bucketing (`bucketStem` — lowercase, per-segment trailing plural 's' stripped, trailing pure-digit segments dropped) with buckets closing on the count limit OR the size estimate, one validated call per bucket, then one global reconciliation call over the survivors; each round has its own keep-all fallback folded in as keep decisions, so a failed round leaves the other rounds' results intact and the final set never grows. **Phase 16 slim schema (vision `04` Step 6 + `07` §2.3):** neither prompt lists a `keep` bucket — kept candidates are the deterministic complement (input minus merges/drops/`unsure`), derived by validation; the old missing-from-every-bucket class survives only as a legacy-'keep' CONTRADICTION check (a `keep` list that is still emitted must be exactly consistent with the raw buckets; double-listing/unknown/self-merge/into-dropped/no-unique-survivor rules unchanged); `callLLMFn` (test-only) keeps every gate LLM-free; every call is logged to `.state/llm-calls.json`. **Phase 21 (§2.2, backlog B5):** `CurateCallOptions.proposedPairs` (from `agents/pre-merge.ts`) fill the prompts' `=== PROPOSED PAIRS ===` section (`formatProposedPairsBlock`; `stripProposedPairsBlock` removes it byte-identically when empty) — the model CONFIRMS/DENIES each pair with a few-words justification via the additive `"pairs"` output array, validated deterministically (judgments must copy a proposed pair exactly, each at most once; proposed-pair slugs are rejected from the merge/drop/keep/unsure buckets; unjudged pairs are denials; confirmed pairs join the merge edges with neverMerge vetoes intact). Verdicts land in the additive `CurationOutcome.pairVerdicts`; proposed-pair members leave the open candidate list (open discovery runs over unproposed candidates only) and are judged in exactly ONE call (the single call, or the reconciliation on the two-round path — signal-aware grouping in `bucketCandidatesSized` never splits a pair across buckets, byte-identical to Phase 16 when no pair edges exist). **Phase 22 (§2.1):** the entity call also gains `proposedClusters` (deterministic class-3/5 composite proposals — `formatProposedClustersBlock`/`stripProposedClustersBlock`, byte-identical stripping when empty) judged confirm/deny via the additive `"clusters"` output, which also carries OPEN cluster decisions (judgment classes 2 brand↔generic, 4 facility↔city); deterministic validation enforces the ratified rules with exact error lists (members exist, class ∈ 1-5, 2 ≤ members ≤ 4, class-3 exactly 2 members — one indicator slug + one concept via `isIndicatorSlug`, class-5 same slug-stem via `regionSlugStem`, `into` ∈ members with deterministic member order into-first-then-sorted, neverMerge pairs reject, no member in two clusters, cluster members stay out of merge/drop/keep/unsure/pairs, a confirmed pair never overlaps an applied cluster; topics never cluster) and confirmed clusters land in `decisions.clusters` with denials in `clusterVerdicts`; computeSurvivors removes clustered members and composeDecisions carries clusters across rounds
* `agents/pre-merge.ts` — Phase 21 (§2.1, backlog B5; evidence: the 2026-07-28/29 rkkp curation reports): the DETERMINISTIC pre-merge signal engine, a pure LLM-free `detectPreMergePairs(candidates, {language?, corpusText?, neverMerge?, vetoSlugs?})` over one concern's candidates returning `{ autoApply, proposed, vetoed }` pairs `{from, into, signal, evidence}`. Six signal families: transliteration-identical slugs (the input language's digraphs collapsed — da/no oe/ae/aa→o/a/a; AUTO tier), exact alias match (a candidate's title is exactly another's frontmatter alias and slugifies to its slug; AUTO tier), corpus `Full Name (ABBR)` mining (capitalization-bounded, subsequence-checked), edit-distance ≤ 2 on long slugs (digit-only-diff + low-prefix-overlap guards), token subsequence (≥2 tokens shorter side, org-unit-word/digit extras blocked) + initials alignment (letter-only initials, a name-token↔initial alignment required — morten-moller ✗ peter-moller and topic-a ✗ topic-b never fire), formulaic families (region name-forms X/X-region/region-X, indicator number↔name indikator-N-*), and a checked-in da↔en domain glossary (~30 clinical/registry groups). Two tiers (gate 21.2): AUTO-APPLY is exactly transliteration + alias (`AUTO_APPLY_SIGNALS`); everything else is PROPOSE-only to the §2.2 confirm-deny call. neverMerge pairs and `vetoSlugs` (the splits escape hatch) move auto-tier pairs to `vetoed` — never applied (gate 21.8). Precision contract (gate 21.1): zero false positives on colocated-but-distinct controls; `curationPairKey` is the shared symmetric pair key. **Phase 22 (§2.1, the five-class rollup amendment):** the two formulaic families no longer propose MERGES — region name-form families (one core, 2-4 forms; larger families propose nothing, never a partial cluster) propose CLASS-5 clusters and the indicator number-name↔bare-concept leg proposes CLASS-3 clusters, all in the additive `proposedClusters` output (`ProposedCluster { members (into first), class, into, signal, evidence }`; only the strict-identity indicator legs stay merge pairs; a merge edge wholly inside a proposed cluster's member set is dropped so a pair is never double-covered; a neverMerge pair inside a cluster proposal vetoes it whole); `isIndicatorSlug`/`regionSlugStem` are the exported class-rule helpers the curation validator reuses
* `agents/` — Phase 2 landed `extractor.ts`; Phase 5 adds `synthesis.ts` (the Synthesis Writer, Layer 4); Phase 9 adds `agents-updater.ts`; Phase 14 adds `curation.ts` (the Curation agent, L3 + L3e); Phase 21 adds `pre-merge.ts` (the deterministic pre-merge signal engine); the Materializer (Phase 3) is deterministic code in `src/materializer.ts`, not an agent in this folder; the DOX Writer (Phase 6) lives in `src/dox-writer.ts`

## Local Contracts

* Phase 0 public surface is frozen per `Implementation Plan/PHASE_00_infrastructure.md` §7: `extractText`, `callLLM`, `sha256`, and the TUI framework must not change signature/behavior after Phase 0 approval; later phases extend, they do not break (Phase 1's `getPageCount` and Phase 2's `CallLLMOptions` are additive extensions with identical defaults)
* `agents/` holds only the agents whose phase has run (Phase 2: the Extractor; Phase 5: the Synthesis Writer; Phase 9: the AGENTS.md Updater; Phase 14: the Curation agent); the Materializer is deterministic code in `src/materializer.ts`; DOX Writer and any rolling-memory writes may not land outside their phases
* LLM calls in the pipeline (all via `callLLM`, all logged): the Extractor (Layer 2, per chunk), the Synthesis Writer (Layer 4, opt-in Phase 5), the Curation agent (Phase 14, two calls per materialize — topics + entities in parallel — when the ingest run's synthesis flag is on AND extraction data exists; no CLI flag, no TUI toggle), the DOX Writer (Layer 5, Phase 6 `doxLlm` mode, per folder plus the wiki root, plus one workspace-pass call per ingest for `wikis/index-of-indexes.md`), and the AGENTS.md Updater (Phase 9, opt-in `updateAgents`, one call per ingest); every other concern (folder creation, page writing, memory updates, curation decision application) is deterministic code
* Four-class retry policy (vision `04` §6, feedback-retry carve-out user-ratified 2026-07-23, implemented in Phase 12; per-page transport fallback + outage detector user-ratified 2026-07-25, implemented in Phase 16; binding for every pipeline LLM call): (1) **deterministic** failures — HTTP 4xx — are NEVER retried (`callLLM` throws immediately) and NEVER fall back per page (a config problem must not silently template a wiki); (2) **transient** transport failures (429/5xx/network/timeouts) retry inside `callLLM` with exponential backoff (`transportRetryDelayMs` 5s/15s/45s; `maxRetries`; large-output calls carry the 600s headers timeout) — and when they are STILL throwing after those retries at a per-page synthesis stage, the Phase 16 per-page transport fallback catches them for THAT page (structured template, loud warning, report `finalMode: 'transport-fallback'`, `metrics.transportFailures++`), while the per-stage outage detector aborts the run with the transport error at 5 consecutive transport-failed pages or more than 10% of the stage's attempted pages; every other stage unchanged (Extractor thrown errors abort; DOX/workspace/updater catch once per target); (3) **content-defect** failures — invalid Extractor JSON, Extractor schema issues, Phase 14: invalid curation decision lists — re-ask ≤3 total attempts via `llm/reask.ts` with the validator's exact errors fed back, and Extractor exhaustion aborts the ingest fail-loud (Phase 14: curation exhaustion lands on the keep-all fallback instead — no data loss); (4) **quality** failures — synthesis preservation, DOX/workspace missing-section/unparseable/empty output, updater missing-section/too-short proposals — use the same feedback-retry loop before their deterministic fallbacks, and a THROWN error at those sites is caught ONCE per target (never quality-retried). Every repair is logged with the `<ctx>#attempt<N>` context, counted in `metrics.feedbackRepairs`, and a prompt-quality warning fires at ≥5 repairs in a run or >25% of the run's LLM calls
* Output-token ceilings (vision `04` §6 + `07` §5, user-ratified 2026-07-23, implemented in Phase 13; binding): every `maxTokens` value is a SAFETY CEILING sized above the largest legitimate output — it is never a length controller (the model never sees the value; a low ceiling yields truncated output, never shorter output). Length control lives in the prompts (the ratified self-sizing block), not in the caps. Synthesis-family calls share one constant (`SYNTHESIS_MAX_TOKENS = 32768` in `agents/synthesis.ts`, no per-language split; Phase 14: the two curation calls use the same ceiling — decision lists for ~300 topics run 5–8K output); the DOX Writer uses `DOX_WRITER_MAX_TOKENS = 8192`; the Extractor uses `EXTRACTION_MAX_TOKENS = 32768` (frozen at 16384 by L1, raised 2026-07-24 after a live run proved a dense chunk's JSON structurally exceeds 16384 — the reask cannot repair truncation, only the ceiling can)
* Curation contract (Phase 14, phase doc §2.2–§2.5; vision `04` §3.2 Step 6 + §6, vision `07` §2.3/§5; Phase 16 slim schema + sizing, vision `04` Step 6, user-ratified 2026-07-25): curation runs iff the ingest run's synthesis flag is on AND extraction data exists — otherwise `materialize()` is byte-identical to pre-Phase-14 (the regression guard; no new CLI flag, no TUI toggle). Decisions are validated DETERMINISTICALLY (every candidate slug in exactly one place — kept candidates are the DERIVED complement since Phase 16: input minus merges/drops/`unsure`, never model-listed; a legacy `keep` list is accepted only when exactly consistent, rejected as contradictory otherwise; every `into` kept, no self-merges, union-find chain resolution) and applied ALL-OR-NOTHING; entity curation is merge-only (never drop — asymmetry: a false merge is far worse than a false keep), entity `unsure` folds into keep. The two-round bucketing triggers on candidate count (>250) OR decision-list size estimate (≥ `CURATION_SIZE_TRIGGER_TOKENS` = 24576, a conservative 75% of the 32768 ceiling — verbose lists can overflow below 250, and truncation is unreaskable). Every failure mode (validation exhaustion after ≤3 reasks with the exact offending entries fed back, transient transport exhaustion after the bounded retries, immediate HTTP 4xx) lands on the keep-all fallback: candidates written as-is, a console warning, a `.state/curation-report.json` entry, and the additive `metrics.curationFallbacks` counter. A manually-edited (or untracked) from-page is vetoed out of its merge/drop and logged as a conflict, never auto-merged. `.state/curation-overrides.json` `neverMerge` pairs are forced into keep for both concerns (the file is created empty when absent; malformed content is ignored with a warning). `.state/extracted/*.json` is NEVER touched (reversibility); rolling memory is written AFTER curation and reflects it
* Phase 21 curation-overhaul contract (§2.1–§2.3, backlog B5; binding): identity decisions are STICKY — every applied merge/drop is recorded in `.state/curation-decisions.json` and pre-applied deterministically before candidate construction (union-find seeded; drops removed), so the model only judges unstuck candidates and re-litigation is impossible (gates 21.4/21.6). The AUTO-APPLY tier is EXACTLY the two near-zero-risk deterministic signals — slug-identical-after-transliteration and alias-exact — everything else the pre-merge engine finds is PROPOSE-only into the confirm-deny path (gate 21.2). Confirm-deny proposals are bounded and validated like any other decision list; confirmed pairs apply identically to model merges; denials are recorded; unjudged pairs are denials. Proposed-pair members leave the open candidate list and are judged in exactly one call (signal-aware grouping; the keep-all fallback and neverMerge contracts are NOT weakened — neverMerge wins over auto-apply, confirm, and sticky). Merges are one-way by default; the ONLY reversal is the hand-edited `splits: [slug]` escape hatch in the decisions file (un-applies the record, logs the reversal, returns the pair to candidates, and the slug is consumed when the pair is re-decided — a permanent veto belongs in `curation-overrides.json`)
* Concurrency boundary (Phase 15, vision `04` §1 concurrency note + optimizations.md L5 user-narrowed scope; binding): the ONLY pooled stages are the synthesis stages — entity, topic, (since Phase 22 gate 22.10) composite, and (since Phase 23) comparison — a bounded worker pool (`utils/worker-pool.ts`) at the FIXED cap `SYNTHESIS_POOL_SIZE = 4` (an exported `commands/ingest.ts` constant, deliberately NOT a Settings field). Per-page semantics are byte-equivalent to the sequential loop (same strict → permissive → template chain, same reask attempts/`#attemptN` contexts, same counts and fallbacks). Determinism under the pool: pool results and synthesis-report entries land in ORIGINAL PAGE ORDER (collected in memory, written once per stage via `appendSynthesisReportEntries`), `llm-calls.json` appends and `conflicts.json` read-modify-writes are serialized through the ONE shared per-path queue (`utils/serialized-writes.ts`), and progress is the aggregate `Synthesis: N/M pages complete (4 workers)` counter (no per-page spam; per-page WARNING lines kept). Phase 16 additive: pickups are staggered by the FIXED `SYNTHESIS_POOL_STAGGER_MS = 250` (same not-a-Settings-field rule; a dispatch delay only — no per-page semantics change; `poolStaggerMs` ingest option is the test seam), per-page `.state/synthesis-state.json` checkpoints ride the same serialized queue, and the per-page transport fallback + outage detector (see the retry contract) run INSIDE the pool tasks without altering the rejection semantics. Extraction (chunks share rolling-memory context), the curation calls, the DOX Writer (bottom-up level dependencies), the workspace pass, and the AGENTS.md Updater remain SEQUENTIAL — pooling any of them is a contract violation
* Run-resume contract (Phase 16, vision `04` Step 9 + Step 11, user-ratified 2026-07-25; binding): resume is a PURE OPTIMIZATION — an uninterrupted run is byte-identical with or without the machinery (gate 16.11 proves it by byte-comparison of the page tree and reports). `.state/synthesis-state.json` records are written per page as synthesis stages complete (serialized queue; sorted keys; stale entries pruned at write time); ONLY `strict-synthesis`/`permissive-synthesis` records with a matching `pageDataHash` fingerprint are skip-eligible — template fallbacks (`structured-template`, `transport-fallback`) are always retried; the Materializer preserves skip-eligible pages byte-for-byte (never rewrites them) while any aggregate change (new evidence, a curation merge) changes the fingerprint and re-synthesizes normally. Per-PDF checkpoints persist each PDF's ingestion record the moment its own processing completes — exactly the record the end-of-run write produces, which stays the final complete record
* Page types are declared in frontmatter; known built-in types are `entity`, `topic`, `document`, `source`, `raw`, `index` (added in Phase 6), `composite` (added in Phase 22 — 2-4 entities of one ratified rollup class pooled into one article; requires `members` (2-4) + `class` (1-5); member pages are never written), and `comparison` (added in Phase 23 — one article per comparison-table subject at `comparisons/<slug>.md`, each source's table preserved verbatim in its own dated section)
* Wikilink form contract (user directive 2026-07-20, superseding the bare title-form convention; compliance-log entry [2026-07-20 00:15]): every wikilink the CLI writes uses Obsidian's native target-plus-display pipe form — content pages `[[<basename-without-.md>|<Title>]]` (basename only, no folder path), folder indexes `[[<folder-path>/index|<Title>]]`, wiki root `[[index|<Wiki Title>]]`, bare `[[target]]` only when display equals the target exactly. The single implementation is `utils/wikilinks.ts` (`formatWikilink`/`parseWikilinkTarget`); the link checker resolves the pre-pipe target by exact vault-relative path first, then the slugified-basename map, keeping legacy bare forms resolvable for older wikis
* Frontmatter `aliases` rule (UAT 6.3 fix, 2026-07-19): every page writer adds `aliases: [<title>]` when the page title differs from the file basename (case-insensitive) and omits it otherwise; the single implementation is `utils/aliases.ts` and LLM-written synthesis output gets the field re-imposed deterministically at write time (the model's frontmatter is not trusted for it). Since the 2026-07-20 pipe-form change, aliases are kept as Obsidian search/suggester aids — they are no longer the link-resolution mechanism
* Frontmatter `sparse` rule (Phase 13, vision `02` §4.8 + `05` §2): an entity page carries `sparse: true` when its aggregate has ≤2 mentions AND no claims AND no relationships; the field is ABSENT otherwise — `sparse: false` is never written. The single rule implementation is `isSparseEntity` in `pages/entity-page.ts` (entity-only; topic pages never carry the field); the deterministic writer derives it from the page data, and LLM-written entity synthesis output gets the field re-imposed/removed deterministically at both ingest write points via `enforceSparseInMarkdown` (the model's frontmatter is not trusted for it — same precedent as `aliases`)
* LLM providers (Anthropic since the user decision 2026-07-17; OpenAI added as an optional second provider by the Phase 11 v1.4.0 user directive 2026-07-22; Qwen added as an optional third provider 2026-08-04): the routing table's `provider` field picks the API — 'anthropic' (default; legacy configs without `provider`), 'openai', or 'qwen'; keys resolve per call (Phase 11 v1.5.0): Settings-stored key (`.paper-chase.json` `apiKeys`, gitignored — never commit, masked to source + last 4 in the TUI, never logged) → `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `DASHSCOPE_API_KEY` env → project-root `.env` fallback; model resolved per call by `resolveModel` (Phase 11): explicit per-call override → the `.paper-chase.json` routing entry for the call type (Phase 14: the curation calls route through the additive `curation` entry — absent in legacy configs, normalized to null = "Same as default") → the routing default → `ANTHROPIC_MODEL` env (anthropic-scoped; no `OPENAI_MODEL` or `QWEN_MODEL` env var exists) → the built-in default `claude-haiku-4-5-20251001`; with no routing set the env-then-default behavior is byte-identical to pre-Phase-11; anthropic prices overridable via env
* Language rule (Phase 7, vision `04` §9.2, binding): Layer 1 prose (synthesis, DOX index descriptions) is written in the wiki's output language; Layer 2 evidence (mentions, relationship evidence, claim text, quotes) stays verbatim in the source language — never translated or reworded; slugs are transliterated with the input language's map before slugifying; when both languages are English, filled prompts and slugify output are byte-identical to pre-Phase-7
* Every LLM call must log tokens and cost; product LLM spend is tracked per phase in `.state/phase-N-status.json`
* TUI conventions (Ink 7): every `useInput` is gated `isActive: isRawModeSupported === true`; components must render correctly in non-TTY contexts (static fallbacks); Escape = back, Enter = select
* Native-picker contract (user decision 2026-07-17 10:55, binding for the Add PDFs screen): the PRIMARY way to add PDFs is the OS-native graphical file picker (`utils/file-dialog.ts`); typed/pasted path entry is a demoted FALLBACK only, kept for environments where the dialog cannot run. Cancelling the dialog is neutral ("No files selected."), never an error; input is gated while the dialog/copy is in flight
* Windows shellout rule: spawn with an args array and `shell: false`, never string-concatenated shell commands (see `utils/file-dialog.ts`); the one sanctioned exception is spawning `.cmd` shims like npm, which requires `shell: true` on Windows (EINVAL guard — the Phase 11-deleted `tui/test-screen.tsx` was the original case; the remaining cases are the `cli.ts` `test` command and the `bin/chase.js` npx fallback, and spaced-path-sensitive launchers avoid a shell entirely by spawning `process.execPath` with the local `node_modules/tsx/dist/cli.mjs`)
* ESM only (`"type": "module"`); strict TypeScript; no new dependencies without recording the reason in the phase status file

## Work Guidance

* Follow the phase document for the phase being implemented; check compliance against the mapped vision documents before writing code (root AGENTS.md + `Implementation Plan/AGENTS.md`)
* Windows/Git Bash environment: no UNIX-only shellouts in committed code without a Windows fallback

## Verification

* `npm test` (vitest) must be green; `npx tsc --noEmit` must be clean
* Each phase's gates in its phase document are the acceptance tests for code here

## Child DOX Index

No child folders with their own contracts yet.
