# tests/ — DOX contract

## Purpose

Vitest suites for LLM Wiki CLI v2.0. Each phase's technical gates are encoded as tests here; `npm test` is the phase acceptance check.

## Ownership

* `infrastructure.test.ts` — Phase 0 gates 0.1–0.5 (extraction, page ranges, hashing, LLM cost logging, CLI commands)
* `tui/menu.test.tsx` — Phase 0 gates 0.6–0.8 (TUI render, menu options, navigation); gate 0.7's "menu shows all 5 options" superseded 2026-07-17 by the user-directed 6-item menu (`add-pdfs` added; deviation logged in `.state/phase-1-status.json`), then grown to 7 items in Phase 2 ('Test Extractor' after 'Ingest PDFs', phase doc §5.2), then 9 items in Phase 3 ('Browse Entities' and 'Browse Topics' added after 'Add PDFs'), then 10 items in Phase 4 ('View Validation Report' added after 'Add PDFs'), then 11 items in Phase 6 ('Browse DOX Contracts' added after 'Browse Topics'), then 14 items in Phases 8+9 ('View Ingestion Log' after 'View Validation Report'; 'Review AGENTS.md Updates' and 'View Structural Changes' after 'Browse DOX Contracts', phase doc §5.3); the settings-footer assertion matches an unwrapped substring because the footer wraps at 80 columns
* `tui/test-screen-spawn.test.tsx` — Phase 0 regression test (UAT 0.2 fix, commit 86633e8): `test` command spawns npm via shell on Windows (EINVAL guard)
* `phase-01.test.ts` — Phase 1 gates 1.1–1.9 (init structure, document pages, frontmatter, source-page hash, idempotency, state) plus supplementary tests for chunking, placeholders, warnings, stale-chunk cleanup; runs hermetically against a temp workspace via the `workspace` option; all `ingest` calls pass `extract: false` (Phase 2 deviation: Phase 1 gates are Layer-1 tests and must stay key-less and $0)
* `phase-02.test.ts` — Phase 2 gates 2.1–2.12 (Extractor JSON schema, known entities, deterministic slugs, valid folders, claims with pages, `.state/extracted/` persistence, empty input, rolling memory, timeline, context, significance, disambiguation); the 12 gates are LIVE LLM tests (`test.skipIf(!process.env.ANTHROPIC_API_KEY)`, 120s timeouts; the file loads the project-root `.env` so skipIf mirrors the client's key resolution; gate 2.6 hermetic temp workspace) plus deterministic LLM-free suites (schema validator cases, slug normalization, JSON parse/ExtractorError, rolling-memory reader, ingest `extract: false`)
* `phase-03.test.ts` — Phase 3 gates 3.1–3.8 (Materializer creates entity pages, valid frontmatter, all mentions, citations, dynamic folders, topic pages, rolling memory, re-ingest updates); deterministic and LLM-free via fake `.state/extracted/*.json` fixtures in temp workspaces, plus a supplementary test that ingest with `extract: false` preserves materialized pages; UAT 6.3 aliases-fix tests (2026-07-19): `aliasesForTitle`/`enforceAliasesInMarkdown` unit tests and per-writer alias rule tests (entity/topic/source pages carry `aliases: [<title>]` on title/basename mismatch, omit it on a case-insensitive match; ingest-written document pages never carry the field); 2026-07-20 pipe-form change: gates 3.2b/3.6b assert entity/topic pages emit `[[slug|Title]]` pipe-form wikilinks (not bare titles or slugs), plus `formatWikilink`/`parseWikilinkTarget` unit tests (pipe/bare/first-pipe split/display with special characters/trimming)
* `phase-04.test.ts` — Phase 4 gates 4.1–4.6 (link checker, broken links, citation checker, schema validator, orphaned pages, validation logged); deterministic and LLM-free via temp workspaces and an injected `extractChunkFn` for the ingest integration test; supplementary tests for `logValidation` console output and `validation-report.json` file output; 2026-07-20 pipe-aware checker tests: pipe-form links resolve via their pre-pipe target (basename and alias-display forms), folder-index/root links resolve by exact vault-relative path (`.md` suffix tolerated), legacy bare forms (`[[Page Title]]`, `[[slug]]`, `[[Folder Name]]`, `[[Wiki Title]]`) remain resolvable, and pipe links with unresolvable targets are reported broken
* `tui/extractor-test-screen.test.tsx` — Phase 2 §5 TUI tests: wiki/chunk selectors, run flow, results panel (counts, entities, save path), scrollable JSON viewer, Escape chain, non-TTY fallback; `extractChunkFn` stub-injected so tests never call the LLM
* `tui/entity-browser.test.tsx` — Phase 3 §5.1 TUI tests: entity folder tree render, folder expansion, file viewer, Escape chain, non-TTY static fallback
* `tui/topic-browser.test.tsx` — Phase 3 §5.2 TUI tests: topic folder tree render, folder expansion, file viewer, Escape chain, non-TTY static fallback
* `phase-05.test.ts` — Phase 5 gates 5.1–5.9 (Synthesis Writer returns readable markdown for entity/topic pages, includes all mentions/relationships/claims, preservation check catches drops and passes for good output, synthesis does not run by default) plus supplementary tests for strict synthesis success, strict→permissive fallback on preservation failure, final fallback to structured template when both fail, synthesis report recording the fallback chain, LLM call log entries tagged with callType/context, and ingest integration covering topic synthesis; deterministic and LLM-free via mocked `callLLM` or self-skip live tests when `ANTHROPIC_API_KEY` is present; Phase 7 v1.1.0: the two synthesis-report shape assertions include the additive `attempts` field (retry counts) — amendment-sanctioned, recorded as a deviation in the phase-7 status file; UAT 6.3 aliases-fix test (2026-07-19): synthesized entity/topic pages carry deterministic `aliases` frontmatter (re-imposed at write time over the model-written page; added for 'John Smith'/'john-smith', omitted for the case-insensitive 'Financial'/'financial' match); 2026-07-20 pipe-form change: the `buildCompletePage` stub fixture renders relationship links as `[[slug|Title]]` (fixture-only — the preservation check compares data strings, not link renderings)
* `phase-06.test.ts` — Phase 6 gates 6.1–6.8 (every folder has `index.md`, `index.md` lists children, wiki-level `index.md` links to top four folders, valid YAML frontmatter, accurate statistics, re-ingest regeneration, DOX pages pass final validation via `result.finalValidation` alongside the preserved `result.validation`, folder descriptions are content-based via an injected `writeDoxIndexFn` stub whose hallucinated frontmatter/statistics prove deterministic enforcement re-imposes children and counts) plus supplementary tests (throwing `writeDoxIndexFn` → run completes and the folder's `index.md` equals the deterministic contract) and the 2026-07-19 bottom-up/wikilink refinement tests (parent contexts carry the exact freshly-written child index markdown with children processed before parents; a zero-page parent gets the stub's synthesis with `## Pages` cataloguing child folders and no fallback; unresolvable stub wikilinks are rewritten on unique title match or de-linked otherwise, with `checkLinks` reporting zero broken links on index pages); deterministic and LLM-free via temp workspaces, an injected `extractChunkFn` for the ingest integration tests, and the injected `writeDoxIndexFn` for the `doxLlm: true` tests; UAT 6.3 aliases-fix tests (2026-07-19): a materialized wiki + deterministic DOX contracts produce index pages with folder/wiki-title `aliases` (root `Test Wiki`, folder `Entities`/`Executives`; entity pages aliased, document pages not), and the LLM-mode path re-imposes the aliases over hallucinated stub frontmatter; 2026-07-20 pipe-form change: deterministic catalogs/navigation emit pipe-form links (new test: root Start Here `[[entities/index|Entities]]`, page catalog `[[john-smith|John Smith]]`, folder links `[[entities/people/index|People]]`, root parent `[[index|Test Wiki]]`, plus a no-legacy-bare-titles check and a final `checkLinks` pass), the repair test now expects the canonical pipe form `[[golden-master|Source: Golden Master]]` on unique title match and `[[index|Test Wiki]]` normalization of the resolvable-bare `[[Test Wiki]]`, and the LLM stubs emit pipe forms (the zero-page-parent test's no-warnings assertion is preserved); 2026-07-20 workspace-index amendment + 2026-07-21 per-wiki-segments amendment: gates 6.9–6.11 (existence/frontmatter/children/segment markers, statistics, per-wiki deterministic fallback byte-equal to the deterministic run) plus the reworked supplementary tests (entry context carries ONLY the triggering wiki's root contract, description lands in both prose segment and catalog line, non-triggering wiki gets the placeholder; init-only wikis skipped, zero ingested wikis writes nothing) and gates 6.12–6.16 (per-wiki catalog-line ownership across ingests, mixed-language lines survive, per-concern fallback + removal drops lines, prose regenerates only on wiki-set change, prose synthesizes ALL root contracts) — LLM-free via injected `writeWorkspaceIndexFn` stubs and the exported `parseWorkspaceSegments`
* `tui/dox-browser.test.tsx` — Phase 6 §5.1 TUI tests: DOX contract tree render, index.md viewer, content page viewer, Escape chain, non-TTY static fallback; stubs disk operations in temp workspaces so tests remain LLM-free
* `tui/phase-01-screens.test.tsx` — Phase 1 §5.4 TUI tests (init form fields, wiki list) plus end-to-end form/ingest flows on a fake-TTY harness; uses temp workspaces via the screens' optional `workspace`/`defaultWorkspace` props
* `tui/add-pdfs-screen.test.tsx` — user-directed Phase 1 extension tests (2026-07-17): Add PDFs screen render, wiki list, live raw/ contents, interactive select-and-paste flow (kept working after the 10:55 redesign — typing auto-focuses the fallback manual input), missing-file error path, Escape contract (input → selector → menu), non-TTY static fallback (renders both add controls), plus `addPdfToWiki` unit tests (byte-identical copy of the golden master via `sha256`, quote/whitespace stripping, missing file, non-PDF rejection, paths with spaces); hermetic temp workspaces. The 2026-07-17 10:55 refinement adds native-picker tests with an injected `pickFiles` stub (never the real dialog): Browse button is the primary default-focused control, batch add summarizes "Added N file(s)", cancel is neutral ("No files selected."), picker failure points at manual entry, mixed results count successes and report per-file failures
* `file-dialog.test.ts` — user-directed Phase 1 refinement tests (2026-07-17 10:55): pure `parseDialogOutput` unit tests (single/multiple paths, `\r\n` and LF handling, empty output → `[]`, paths with spaces, blank-line skipping). `pickPdfFiles` itself spawns the real Windows OpenFileDialog and is exercised by user UAT only — automated tests must never spawn the dialog
* `phase-07.test.ts` — Phase 7 gates 7.1–7.9 (transliteration maps per language, byte-identical English default incl. LANGUAGE-block removal in all six prompt templates, input-language slug normalization, language directive reaching extractor/synthesis/dox prompts, language state round-trip via init+ingest, end-to-end Danish ingest materializing transliterated pages with verbatim Danish titles, cross-language synthesis passing preservation with verbatim Danish Layer 2, slug-forking warning on input-language change, folder-segment and claim-type transliteration — gate 7.9 added after UAT 7.2 found `entities/companies/møbler/` on disk; gates 7.10–7.12 added by the v1.1.0 bounded-retry amendment: callLLM transient-only retry (undici mocked via vi.mock — its native exports cannot be spied on), synthesis-chain quality retries with attempt counts, DOX folder/root/workspace retries before deterministic fallback) plus supplementary tests (no-warning cases, en/en prompt purity); deterministic and LLM-free via temp workspaces, a mocked `callLLM` (gates 7.4/7.6/7.7 drive the REAL extractor/synthesis/DOX prompt paths and capture filled prompts) and JSON-writing `extractChunkFn` stubs (pre-normalized fixtures — the stub bypasses slug normalization by design). Gate 7.6 deviation: the Danish title is asserted via parsed frontmatter because the entity-page writer only quotes YAML-sensitive titles
* `phase-08.test.ts` — Phase 8 gates 8.1–8.6 (new PDF adds new entities, new PDF updates existing entity pages incl. new source, unchanged PDFs skipped with "Skipping {filename} (unchanged)." via the onProgress callback, rolling memory reflects both PDFs, manual-edit conflict detected + logged to the wiki's `.state/conflicts.json` and the update skipped, no duplicate entity pages per slug across PDFs) plus supplementary tests (tool writes never flagged as manual edits via conflict-hash tracking, removed PDF warns and keeps derived pages, changed PDF replaces stale extraction JSON, changed PDF re-processed under the current input language warns on language drift, `metrics.json` records new PDFs / new+updated entities / conflicts / cost for the Ingestion Log screen) plus the 2026-07-21 UAT fix-loop regressions: cross-run folder-fork prevention (second run re-deriving a different folder keeps ONE page at the FIRST folder with both runs merged; rolling memory never moves the entity), fork reconciliation on an already-forked wiki (unmodified orphan deleted with the `Removed duplicate page ...` progress line and its hash entry dropped; manually-edited orphan KEPT and logged as a manual-edit conflict), and crash-safe finalization (`metrics.json` + `language.json` observed persisted BEFORE the DOX stage via a throwing `writeDoxIndexFn`/workspace stub — fallback behavior unchanged; a forced metrics-write failure warns twice and never fails the ingest); deterministic and LLM-free via temp workspaces and an injected `extractChunkFn` stub (same restructure pattern as phases 4/6/7 — deviations recorded in `.state/phase-8-status.json`); no dedicated `compounding-log-screen` test exists (screen is UAT-verified)
* `phase-09.test.ts` — Phase 9 gates 9.1–9.5 (updater proposes valid AGENTS.md, proposal includes new folders, proposal saved to `.state/proposed-agents.md`, original AGENTS.md never overwritten, updater opt-in only) plus supplementary tests (filled prompt carries constitution/structure/discoveries, ingest `updateAgents: true` full-wiring via the real proposer with injected `callLLMFn`, invalid proposals retried ≤3 attempts before the deterministic fallback, `## Language` section re-imposed verbatim over vandalized LLM output, first materialize logs new folders + new page types with reasons/affected entities, re-materialize logs nothing new, absent log reads empty); deterministic and LLM-free via temp workspaces, an injected `extractChunkFn`, and injected `callLLMFn`/`proposeAgentsUpdateFn` stubs. Gate 9.1 deviation (recorded in `.state/phase-9-status.json`): the phase doc's literal section name "Ingest Workflow" does not exist in the Phase 0 constitution template — the gate asserts the template's actual required sections (`## Folder Structure`, `## Page Types`, `## Ingest Instructions for the LLM`, `## Language`)
* `tui/agents-review-screen.test.tsx` — Phase 9 §5.1 TUI tests: proposed-changes summary (new folders, new page types, diff stats), apply copies the proposal over AGENTS.md (UAT 9.3), discard deletes it, scrollable full diff (waits on the diff-view-only `Full Diff (` marker — the summary's `[V] View Full Diff` label also contains 'Full Diff'), no-proposal guidance, non-TTY static fallback; hermetic temp workspaces, no LLM
* `tui/structural-changes-screen.test.tsx` — Phase 9 §5.2 TUI tests: change log render (timestamps, new folders, reasons, affected entities — UAT 9.4), empty state, non-TTY static fallback; hermetic temp workspaces, no LLM
* `phase-10.test.ts` — Phase 10 gates 10.1–10.7 (pdfjs byte-identical default vs. pre-refactor snapshots in `tests/snapshots/`, core extraction + ingest `pages: "N-M"` provenance under both engines, cross-engine page semantics, `resolvePdfEngine` precedence PLUS an end-to-end flag-vs-env conflict ingest test proving the explicit `pdfEngine` flag beats `PDF_ENGINE` through the whole pipeline — verifier-found fix, missing-JRE actionable error via PATH-override probe + test-only forced probe result, ab-corpus manifest/sha256 integrity, comparison-harness report sections) plus supplementary batch-surface and §3.5 renderTablesAsMarkdown Option A evidence tests; LLM-free; JRE-gated tests use a shared top-level `javaAvailable` probe + `test.skipIf(...)` with 120s timeouts (Java 21 Temurin is installed on the dev machine, so they run for real); gate 10.7 spawns the harness as a subprocess (node + tsx CLI, `shell: false`, `VITEST` stripped from the child env) because scripts/AGENTS.md forbids importing scripts from tests; `tests/snapshots/` holds the immutable pre-refactor pdfjs outputs (exact bytes, recorded BEFORE the pdf.ts split — never regenerate)

## Local Contracts

* Every test must trace to a numbered gate or requirement in a phase document; when a gate's literal test code cannot run on this platform, the restructured test must still verify the gate's pass criterion and the deviation must be recorded in `.state/phase-N-status.json`
* Tests must not make live LLM calls by default; live-call tests use `test.skipIf(!process.env.ANTHROPIC_API_KEY)` and are run deliberately with the key loaded
* Tests must not modify `test-pdfs/golden-master.pdf` (see `test-pdfs/AGENTS.md`)
* JRE-gated tests (opendataloader engine) use a shared `javaAvailable` probe + `test.skipIf(...)` with generous (120s) timeouts for JVM startup; the skip must be visible in the output, and on machines with Java they must actually run and pass
* Ink 7 non-TTY harness conventions: capture frames via fake stdout and assert after `unmount()`; drive input via fake-TTY stdin (PassThrough with `isTTY`/`setRawMode` stubs); render `TestScreen` with `autoRun=false` so tests never spawn `npm test` recursively

## Work Guidance

* Platform is Windows/Git Bash: shellout assertions need fallbacks (e.g. `shasum` → `certutil` → `node:crypto`)

## Verification

* `npm test` green in both modes: without a key — 197 passed + 13 self-skipped (12 live Phase 2 gates + Phase 0 gate 0.4); with the key (`.env` at project root) — 217 passed + 1 skipped (gate 0.4 keeps its Phase 0 exported-var-only behavior, verified separately 5/5). Counts verified 2026-07-21 after the workspace prose amendment added gates 6.15–6.16 to `phase-06.test.ts` (prose regenerates only on wiki-set change; prose synthesizes ALL root contracts). UPDATED 2026-07-21 (Phase 10, key present in env): 23 files, 265 passed + 1 skipped — includes the Phase 10 suite (16 tests after the verifier-mandated Gate 10.4 end-to-end conflict test, all JRE-gated tests running for real under Temurin 21) and the Phase 8/9 suites landed in parallel the same day. UPDATED 2026-07-21 (Phase 8 UAT fix loop, key present): 23 files, 270 passed + 1 skipped — the five new fork-reconciliation/crash-safe-finalization regressions in `phase-08.test.ts` (16/16 in that file)

## Child DOX Index

* `tui/` — TUI component tests (covered by this file's contracts; no separate child doc yet)
