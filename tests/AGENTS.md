# tests/ — DOX contract

## Purpose

Vitest suites for Paper Chase v.1.0. Each phase's technical gates are encoded as tests here; `npm test` is the phase acceptance check.

## Ownership

* `infrastructure.test.ts` — Phase 0 gates 0.1–0.5 (extraction, page ranges, hashing, LLM cost logging, CLI commands)
* `tui/menu.test.tsx` — Phase 0 gates 0.6–0.8 (TUI render, menu options, navigation); gate 0.7's "menu shows all 5 options" superseded 2026-07-17 by the user-directed 6-item menu (`add-pdfs` added; deviation logged in `.state/phase-1-status.json`), then grown to 7 items in Phase 2 ('Test Extractor' after 'Ingest PDFs', phase doc §5.2), then 9 items in Phase 3 ('Browse Entities' and 'Browse Topics' added after 'Add PDFs'), then 10 items in Phase 4 ('View Validation Report' added after 'Add PDFs'), then 11 items in Phase 6 ('Browse DOX Contracts' added after 'Browse Topics'), then 14 items in Phases 8+9 ('View Ingestion Log' after 'View Validation Report'; 'Review AGENTS.md Updates' and 'View Structural Changes' after 'Browse DOX Contracts', phase doc §5.3), then PRUNED back to exactly 5 items in Phase 11 (phase doc §2.3: Create New Wiki, Add PDFs, Ingest PDFs, Settings, Exit — the development/review screens were deleted); the suite also covers the Phase 11 first-launch welcome splash (shown only when neither `.paper-chase.json` nor the legacy `.llm-wiki-cli.json` exists); the settings-footer assertion matches an unwrapped substring because the footer wraps at 80 columns
* `phase-01.test.ts` — Phase 1 gates 1.1–1.9 (init structure, document pages, frontmatter, source-page hash, idempotency, state) plus supplementary tests for chunking, placeholders, warnings, stale-chunk cleanup; runs hermetically against a temp workspace via the `workspace` option; all `ingest` calls pass `extract: false` (Phase 2 deviation: Phase 1 gates are Layer-1 tests and must stay key-less and $0)
* `phase-02.test.ts` — Phase 2 gates 2.1–2.12 (Extractor JSON schema, known entities, deterministic slugs, valid folders, claims with pages, `.state/extracted/` persistence, empty input, rolling memory, timeline, context, significance, disambiguation); the 12 gates are LIVE LLM tests (`test.skipIf(!process.env.ANTHROPIC_API_KEY)`, 120s timeouts; the file loads the project-root `.env` so skipIf mirrors the client's key resolution; gate 2.6 hermetic temp workspace) plus deterministic LLM-free suites (schema validator cases, slug normalization, JSON parse/ExtractorError, rolling-memory reader, ingest `extract: false`)
* `phase-03.test.ts` — Phase 3 gates 3.1–3.8 (Materializer creates entity pages, valid frontmatter, all mentions, citations, dynamic folders, topic pages, rolling memory, re-ingest updates); deterministic and LLM-free via fake `.state/extracted/*.json` fixtures in temp workspaces, plus a supplementary test that ingest with `extract: false` preserves materialized pages; UAT 6.3 aliases-fix tests (2026-07-19): `aliasesForTitle`/`enforceAliasesInMarkdown` unit tests and per-writer alias rule tests (entity/topic/source pages carry `aliases: [<title>]` on title/basename mismatch, omit it on a case-insensitive match; ingest-written document pages never carry the field); 2026-07-20 pipe-form change: gates 3.2b/3.6b assert entity/topic pages emit `[[slug|Title]]` pipe-form wikilinks (not bare titles or slugs), plus `formatWikilink`/`parseWikilinkTarget` unit tests (pipe/bare/first-pipe split/display with special characters/trimming)
* `phase-04.test.ts` — Phase 4 gates 4.1–4.6 (link checker, broken links, citation checker, schema validator, orphaned pages, validation logged); deterministic and LLM-free via temp workspaces and an injected `extractChunkFn` for the ingest integration test; supplementary tests for `logValidation` console output and `validation-report.json` file output; 2026-07-20 pipe-aware checker tests: pipe-form links resolve via their pre-pipe target (basename and alias-display forms), folder-index/root links resolve by exact vault-relative path (`.md` suffix tolerated), legacy bare forms (`[[Page Title]]`, `[[slug]]`, `[[Folder Name]]`, `[[Wiki Title]]`) remain resolvable, and pipe links with unresolvable targets are reported broken
* `phase-05.test.ts` — Phase 5 gates 5.1–5.9 (Synthesis Writer returns readable markdown for entity/topic pages, includes all mentions/relationships/claims, preservation check catches drops and passes for good output, synthesis does not run by default) plus supplementary tests for strict synthesis success, strict→permissive fallback on preservation failure, final fallback to structured template when both fail, synthesis report recording the fallback chain, LLM call log entries tagged with callType/context, and ingest integration covering topic synthesis; deterministic and LLM-free via mocked `callLLM` or self-skip live tests when `ANTHROPIC_API_KEY` is present; Phase 7 v1.1.0: the two synthesis-report shape assertions include the additive `attempts` field (retry counts) — amendment-sanctioned, recorded as a deviation in the phase-7 status file; UAT 6.3 aliases-fix test (2026-07-19): synthesized entity/topic pages carry deterministic `aliases` frontmatter (re-imposed at write time over the model-written page; added for 'John Smith'/'john-smith', omitted for the case-insensitive 'Financial'/'financial' match); 2026-07-20 pipe-form change: the `buildCompletePage` stub fixture renders relationship links as `[[slug|Title]]` (fixture-only — the preservation check compares data strings, not link renderings)
* `phase-06.test.ts` — Phase 6 gates 6.1–6.8 (every folder has `index.md`, `index.md` lists children, wiki-level `index.md` links to top four folders, valid YAML frontmatter, accurate statistics, re-ingest regeneration, DOX pages pass final validation via `result.finalValidation` alongside the preserved `result.validation`, folder descriptions are content-based via an injected `writeDoxIndexFn` stub whose hallucinated frontmatter/statistics prove deterministic enforcement re-imposes children and counts) plus supplementary tests (throwing `writeDoxIndexFn` → run completes and the folder's `index.md` equals the deterministic contract) and the 2026-07-19 bottom-up/wikilink refinement tests (parent contexts carry the exact freshly-written child index markdown with children processed before parents; a zero-page parent gets the stub's synthesis with `## Pages` cataloguing child folders and no fallback; unresolvable stub wikilinks are rewritten on unique title match or de-linked otherwise, with `checkLinks` reporting zero broken links on index pages); deterministic and LLM-free via temp workspaces, an injected `extractChunkFn` for the ingest integration tests, and the injected `writeDoxIndexFn` for the `doxLlm: true` tests; UAT 6.3 aliases-fix tests (2026-07-19): a materialized wiki + deterministic DOX contracts produce index pages with folder/wiki-title `aliases` (root `Test Wiki`, folder `Entities`/`Executives`; entity pages aliased, document pages not), and the LLM-mode path re-imposes the aliases over hallucinated stub frontmatter; 2026-07-20 pipe-form change: deterministic catalogs/navigation emit pipe-form links (new test: root Start Here `[[entities/index|Entities]]`, page catalog `[[john-smith|John Smith]]`, folder links `[[entities/people/index|People]]`, root parent `[[index|Test Wiki]]`, plus a no-legacy-bare-titles check and a final `checkLinks` pass), the repair test now expects the canonical pipe form `[[golden-master|Source: Golden Master]]` on unique title match and `[[index|Test Wiki]]` normalization of the resolvable-bare `[[Test Wiki]]`, and the LLM stubs emit pipe forms (the zero-page-parent test's no-warnings assertion is preserved); 2026-07-20 workspace-index amendment + 2026-07-21 per-wiki-segments amendment: gates 6.9–6.11 (existence/frontmatter/children/segment markers, statistics, per-wiki deterministic fallback byte-equal to the deterministic run) plus the reworked supplementary tests (entry context carries ONLY the triggering wiki's root contract, description lands in both prose segment and catalog line, non-triggering wiki gets the placeholder; init-only wikis skipped, zero ingested wikis writes nothing) and gates 6.12–6.16 (per-wiki catalog-line ownership across ingests, mixed-language lines survive, per-concern fallback + removal drops lines, prose regenerates only on wiki-set change, prose synthesizes ALL root contracts) — LLM-free via injected `writeWorkspaceIndexFn` stubs and the exported `parseWorkspaceSegments`; 2026-07-25 navigation fix (BACKLOG B3 + root-hub gap, user-reported on `dist/wikis/rkkp-adhd`): gates 6.17a–6.17d (root LLM index catalogs all four top-area indexes in `## Pages` alongside Start Here; a folder index missing a page catalog link is re-asked with the exact target named; a self-referential catalog link is rejected and named; exhausted retries write the fully-catalogued deterministic body at every level) — the `richStubIndex` helper builds context-aware complete catalogs because the new `missingCatalogLinks` enforcement rejects curated subsets, and the zero-page-parent stub is scoped to `entities/people` for the same reason
* `tui/phase-01-screens.test.tsx` — Phase 1 §5.4 TUI tests (init form fields, wiki list) plus end-to-end form/ingest flows on a fake-TTY harness; uses temp workspaces via the screens' optional `workspace`/`defaultWorkspace` props. 2026-08-24 folder-picker extension: +7 tests with an injected `pickFolder` stub (never the real dialog) — the always-on target breadcrumb renders from screen open with the resolved absolute `wikis\` path and the `<title-slug>` placeholder until Title forms a valid slug, the slug fills in live as Title is typed, Enter on `[ Browse... ]` opens the picker pre-seeded with the current workspace value and the picked parent folder fills the field with the breadcrumb following, cancel is neutral, a failure shows the manual-fallback guidance, the field stays usable after a failure, and input is gated while the picker is in flight; the create-wiki key walk gains one Tab for the Browse stop (Title → Workspace → Browse → Language → Enter)
* `tui/add-pdfs-screen.test.tsx` — user-directed Phase 1 extension tests (2026-07-17): Add PDFs screen render, wiki list, live raw/ contents, interactive select-and-paste flow (kept working after the 10:55 redesign — typing auto-focuses the fallback manual input), missing-file error path, Escape contract (input → selector → menu), non-TTY static fallback (renders both add controls), plus `addPdfToWiki` unit tests (byte-identical copy of the golden master via `sha256`, quote/whitespace stripping, missing file, non-PDF rejection, paths with spaces); hermetic temp workspaces. The 2026-07-17 10:55 refinement adds native-picker tests with an injected `pickFiles` stub (never the real dialog): Browse button is the primary default-focused control, batch add summarizes "Added N file(s)", cancel is neutral ("No files selected."), picker failure points at manual entry, mixed results count successes and report per-file failures. 2026-08-28: `onStartIngest`/`initialWiki` usages adapt to the `WikiRef { workspace, slug }` payload (assertions on `.slug`; the continuous-workflow test passes an `initialWiki` object) — assertion intents unchanged
* `file-dialog.test.ts` — user-directed Phase 1 refinement tests (2026-07-17 10:55): pure `parseDialogOutput` unit tests (single/multiple paths, `\r\n` and LF handling, empty output → `[]`, paths with spaces, blank-line skipping). `pickPdfFiles` itself spawns the real Windows OpenFileDialog and is exercised by user UAT only — automated tests must never spawn the dialog
* `folder-dialog.test.ts` — user-directed extension tests (2026-08-24, the Create New Wiki folder picker): pure `parseSelectedPath` unit tests (single path, `\r\n`/LF handling, empty output → null, paths with spaces, blank-line skipping, first non-empty line wins). `pickFolder` itself spawns the real Windows FolderBrowserDialog and is exercised by user UAT only — automated tests must never spawn the dialog
* `phase-07.test.ts` — Phase 7 gates 7.1–7.9 (transliteration maps per language, byte-identical English default incl. LANGUAGE-block removal in all six prompt templates, input-language slug normalization, language directive reaching extractor/synthesis/dox prompts, language state round-trip via init+ingest, end-to-end Danish ingest materializing transliterated pages with verbatim Danish titles, cross-language synthesis passing preservation with verbatim Danish Layer 2, slug-forking warning on input-language change, folder-segment and claim-type transliteration — gate 7.9 added after UAT 7.2 found `entities/companies/møbler/` on disk; gates 7.10–7.12 added by the v1.1.0 bounded-retry amendment: callLLM transient-only retry (undici mocked via vi.mock — its native exports cannot be spied on), synthesis-chain quality retries with attempt counts, DOX folder/root/workspace retries before deterministic fallback; Phase 12 semantic update (2026-07-23): gate 7.12 Cases 2–3 re-cast to the feedback-retry policy — a THROWN error is a deterministic transport failure caught ONCE per target (people/entities/root = 3 calls; workspace entry = 1 call) and never quality-retried, while Case 1's content-defect garbage output still reasks ≤3 attempts) plus supplementary tests (no-warning cases, en/en prompt purity); deterministic and LLM-free via temp workspaces, a mocked `callLLM` (gates 7.4/7.6/7.7 drive the REAL extractor/synthesis/DOX prompt paths and capture filled prompts) and JSON-writing `extractChunkFn` stubs (pre-normalized fixtures — the stub bypasses slug normalization by design). Gate 7.6 deviation: the Danish title is asserted via parsed frontmatter because the entity-page writer only quotes YAML-sensitive titles
* `phase-08.test.ts` — Phase 8 gates 8.1–8.6 (new PDF adds new entities, new PDF updates existing entity pages incl. new source, unchanged PDFs skipped with "Skipping {filename} (unchanged)." via the onProgress callback, rolling memory reflects both PDFs, manual-edit conflict detected + logged to the wiki's `.state/conflicts.json` and the update skipped, no duplicate entity pages per slug across PDFs) plus supplementary tests (tool writes never flagged as manual edits via conflict-hash tracking, removed PDF warns and keeps derived pages, changed PDF replaces stale extraction JSON, changed PDF re-processed under the current input language warns on language drift, `metrics.json` records new PDFs / new+updated entities / conflicts / cost for the Ingestion Log screen) plus the 2026-07-21 UAT fix-loop regressions: cross-run folder-fork prevention (second run re-deriving a different folder keeps ONE page at the FIRST folder with both runs merged; rolling memory never moves the entity), fork reconciliation on an already-forked wiki (unmodified orphan deleted with the `Removed duplicate page ...` progress line and its hash entry dropped; manually-edited orphan KEPT and logged as a manual-edit conflict), and crash-safe finalization (`metrics.json` + `language.json` observed persisted BEFORE the DOX stage via a throwing `writeDoxIndexFn`/workspace stub — fallback behavior unchanged; a forced metrics-write failure warns twice and never fails the ingest); deterministic and LLM-free via temp workspaces and an injected `extractChunkFn` stub (same restructure pattern as phases 4/6/7 — deviations recorded in `.state/phase-8-status.json`); no dedicated `compounding-log-screen` test ever existed (the screen was UAT-verified only and was deleted in Phase 11)
* `phase-09.test.ts` — Phase 9 gates 9.1–9.5 (updater proposes valid AGENTS.md, proposal includes new folders, proposal saved to `.state/proposed-agents.md`, original AGENTS.md never overwritten, updater opt-in only) plus supplementary tests (filled prompt carries constitution/structure/discoveries, ingest `updateAgents: true` full-wiring via the real proposer with injected `callLLMFn`, invalid proposals retried ≤3 attempts before the deterministic fallback, `## Language` section re-imposed verbatim over vandalized LLM output, first materialize logs new folders + new page types with reasons/affected entities, re-materialize logs nothing new, absent log reads empty); deterministic and LLM-free via temp workspaces, an injected `extractChunkFn`, and injected `callLLMFn`/`proposeAgentsUpdateFn` stubs. Gate 9.1 deviation (recorded in `.state/phase-9-status.json`): the phase doc's literal section name "Ingest Workflow" does not exist in the Phase 0 constitution template — the gate asserts the template's actual required sections (`## Folder Structure`, `## Page Types`, `## Ingest Instructions for the LLM`, `## Language`)
* `tui/agents-review-screen.test.tsx` — restored in Phase 11 v1.6.0 from the Phase 9 suite (deleted in the Phase 11 menu cleanup) and adapted to the user directive 2026-07-23: the Review AGENTS.md Updates screen (now FLOW-ONLY, reachable via the post-ingest `p` shortcut) renders the proposed-changes summary with inline diff and [A]/[R]/[V]; Accept copies the proposal over AGENTS.md (proposal file kept); Reject is a NO-OP — AGENTS.md and `.state/proposed-agents.md` stay byte-identical and the done message reads 'Rejected proposed AGENTS.md updates for <wiki>. No changes made.' (supersedes the old reject-deletes-the-proposal test); full-diff view, no-proposal guidance, and the non-TTY static fallback are unchanged. Hermetic temp workspaces; no LLM calls.
* `tui/ingest-screen.test.tsx` — Phase 16 v1.0.3 (user directive 2026-08-22): the ingest screen renders the client's 429/5xx stall as a live progress line — a stubbed `ingestFn` emits the exact wiring line (`Rate limited by provider (HTTP 429) — waiting 60s before retry (attempt 2/6)...`; 5xx stalls read `Provider error (HTTP 500) — ...`) through `onProgress` and the frame shows it, so a multi-minute stall wait is visible instead of a frozen screen. Hermetic temp workspace; the ingest itself is an injected stub — no LLM calls. 2026-08-28: `initialWiki` becomes a `WikiRef { workspace, slug }` object (the workspace-registry fix).
* `tui/workspace-bootstrap.test.ts` — 2026-08-28 (user-reported bug fix): the workspace bootstrap unit suite (8 tests, hermetic temp boot dirs) — `loadWorkspaceRegistry` empty defaults, a pre-registry pointer back-seeds the registry, dedupe on load with the pointer winning; `registerWorkspace` persists the pointer + dedupe-adds to the registry, migrates the launch folder's settings into the workspace's own config ONCE (inherited models/keys/toggles, pointer/registry fields stripped, an existing config never overwritten), moves a re-registered workspace to the end, clears the pointer when the workspace IS the launch folder; `workspace`/`workspaces` round-trip through save/load with garbage entries tolerated.
* `tui/app-workspace.test.tsx` — 2026-08-28 (user-reported bug fix): the workspace-follow regression suite (4 tests). (a) THE bug: an App seeded with workspace A creates a wiki in workspace B via the injected `pickFolder` stub (threaded through App, the `ingestFn` precedent; the picker promise resolves on a controlled handshake because ink writes only the final frame on unmount for non-TTY stdout) and the PDF added afterwards must land in B's `raw/` — never A's — with `onWorkspaceRegistered` hearing B exactly once. (b) the InitScreen `onCreated` contract carries `{ slug, workspace }`. (c) aggregation: Add PDFs and Ingest list the wikis of EVERY registered workspace with `slug (workspace)` labels, while a single-workspace frame stays byte-identical (no suffix). (d) the wiki list is a LIVE scan — a wiki folder deleted on disk is gone from the selector on the next screen entry while other workspaces' wikis stay.
* `phase-11.test.ts` — Phase 11 gates 11.1–11.12 (phase doc §2/§5, v1.6.0), 11.13 (v1.7.0 custom model + test connection), 11.14 (v1.8.0 custom providers), 11.16 (v1.9.0 per-step provider + model routing), and 11.18 (Phase 16 v1.0.5 JSON Corrector Model slot): 11.1 the Settings screen persists composite `{ provider, model }` model routing to `.paper-chase.json`; 11.2 `resolveModel` precedence (override → routing entry → routing default → `ANTHROPIC_MODEL` → DEFAULT_MODEL; the four synthesis callTypes route to synthesis, 'dox-writer' to dox, null entries fall to default) plus a `callLLM` request-body check with mocked undici asserting the routed model; 11.3 the menu is exactly the five production items and nine deleted dev/review items are absent; 11.4 the continuous workflow — App's `workspace` prop renders hermetically, Init → Add PDFs with `initialWiki`, the `Copied N file(s) to wikis/<slug>/raw/.` banner, and the `Start ingesting now? [Y/n]` prompt; 11.5 README.md carries the five spec-required sections; 11.6 an `extract: false` ingest still writes `chunksProcessed` and `totalCost`; 11.7 the branding scanner (forbidden old-brand and malformed-new-brand regexes built by string concatenation so the scanner does not flag itself; the phase doc and this file are excluded; sanctioned exception lines are skipped); 11.8 `program.name() === 'chase'` and package.json name/bin; 11.9 settings load falls back to legacy `.llm-wiki-cli.json` read-only and the new file wins when both exist; 11.10 (v1.4.0 multi-provider extension + Qwen 2026-08-04) — provider save/load round-trip, legacy-without-provider loads as 'anthropic' with STRING slots migrating to `{ provider, model }` pairs, provider-aware `resolveModel` routing through openai and qwen tables, mocked-transport OpenAI and Qwen calls (Chat Completions URL, `authorization: Bearer`, `max_tokens` present and `max_completion_tokens`/`temperature` absent even with `options.temperature` set, leading system message, response/usage parsing, price-table cost, `provider` field in llm-calls.json), the exact missing-`OPENAI_API_KEY` / `DASHSCOPE_API_KEY` errors, `seedModelsForProvider` re-seeding for Anthropic/OpenAI/Qwen, a screen-driven Default Provider switch (only the Default Model row re-seeds — explicit slots are preserved), and the anthropic byte-identical request-body regression; 11.11 (v1.5.0 API-keys extension, FAKE keys + mocked undici) — `apiKeys` save/load round-trip (a missing block loads as nulls for the three built-in providers), per-provider key resolution (stored beats env, env-only fallback, neither → the exact extended missing-key error; anthropic `x-api-key` and openai/qwen `authorization: Bearer` header checks), `getApiKeyStatus` source reporting (stored/environment/none; source + last-4 only, never the full key), Settings-screen masking (rows show configured/from-environment + last4 or not-set; typed key material is masked and no full key appears in any rendered frame), stage → Save persistence / Escape-cancel / empty-submit-clears, and log hygiene (no key material reaches llm-calls.json or the console); 11.12 (v1.6.0 post-ingest AGENTS.md review shortcut, stubbed `ingestFn`) — the review hint shows only when the run proposed AGENTS.md updates, an app-level drive routes to the flow-only review screen with line-diff markers, Accept replaces `AGENTS.md` byte-identically (proposal kept), Reject leaves AGENTS.md AND `.state/proposed-agents.md` byte-identical with the 'No changes made.' message, and a MENU_ITEMS regression asserts the menu stays exactly five items. 11.13 (6 tests; v1.9.0: Enter on a model row replaces the old cycle sentinel) — Enter opens a provider-scoped raw model-id editor and persists `{ provider, model }`, a persisted custom id displays raw and is pre-filled on re-edit, switching the Default Provider PRESERVES the custom model (only Default Model re-seeds), test connection reports success/401/missing-key. 11.14 (7 tests) — add a custom provider and persist, delete a custom provider switches back to anthropic, custom request/response templates are filled and extracted, custom test connection, custom model names selectable and persist, custom provider config rows appear only when selected. 11.16 (5 tests) — `resolveSlotFromRouting`/`resolveProviderForCall` resolve each call type to its OWN `{ provider, model }` with an Anthropic/OpenAI/Qwen mixed table, `callLLM` dispatches three call types to three different APIs with each provider's own stored key, legacy STRING slots migrate to `{ provider, model }` pairs under the legacy global provider, `normalizeModelSlot` unit matrix, and the Settings frame shows the `Default Provider` row and `Provider · Model` prefixes on off-default slots. 11.18 (2 tests) — `jsonCorrector` seeds null and normalizes legacy-absent configs to null, `resolveSlotFromRouting` maps 'json-corrector' with the default fallback and to its own `{ provider, model }` when set, the composite slot round-trips through save/load, and the Settings frame shows the 'JSON Corrector Model' row with its cheap-tier recommendation label. The built-in providers exercised in this file are Anthropic, OpenAI, Qwen, and `custom:` providers; all tests are LLM-free. Pre-existing touches at the enumerated sites only (recorded in compliance-log entries): gates 11.1/11.10/11.11/11.13/11.14/11.16 DOWN-count walks past the model rows and seed-shape `toEqual`s gain `jsonCorrector: null` — assertion intents unchanged; 2026-08-28: the 11.12 `onReviewAgents` stub types adapt to the `WikiRef` payload (asserts `.slug`) — assertion intent unchanged.
* `phase-12.test.ts` — Phase 12 gates 12.1–12.8 (validation-feedback retry / reask, vision `04` §6): 12.1 Extractor invalid-JSON reask (correction-block contents, attempt-1 byte-identity via `attempt2.startsWith(attempt1 + '\n\n=== CORRECTION REQUIRED ===')`, log contexts `<ctx>` → `<ctx>#attempt2`, via a `callLLM` spy); 12.2 schema-issue reask feeding the exact `entities[6]: folder "..." must start with "entities/" or "topics/"` issue plus the slipped JSON verbatim (regression coverage for the UAT folder-prefix slip); 12.3 Extractor exhaustion aborts with `ExtractorError` after exactly 3 attempts; 12.4 synthesis preservation reask (strict entity attempt 1 drops a mention → attempt 2 receives the exact `Dropped mention (restore this exact text): ...` string; `metrics.feedbackRepairs === 1`; ALL FOUR synthesis stubs injected with `[^srcN]` citation markers so no real chain link runs); 12.5 DOX folder/root reask via `writeDoxIndexFn` (root attempt 1 missing `## Start Here` fed back verbatim; folder predicate is `context.folderPath !== ''`; exhaustion → deterministic contract with `## Statistics`); 12.6 AGENTS.md Updater reask via `callLLMFn` (missing `## Page Types` + `proposal is too short` errors + the invalid output verbatim; exhaustion → `## Proposed Additions (deterministic fallback)`); 12.7 repair accounting + prompt-quality warning (≥5-repairs branch: 8 folder repairs against 40 seeded `llm-calls.json` entries — seeds MUST be future-timestamped because `countLlmCallsSince` only counts entries at/after the in-run `runStartedAt`; 25%-of-calls branch: 1 repair against 3 seeded calls; clean run stays quiet and writes `feedbackRepairs: 0`); 12.8 HTTP 4xx never re-asked (helper-level throw propagation + a REAL `extractChunk` against a mocked-undici 404: exactly 1 request, no correction block in the body, error is not an `ExtractorError`; FAKE key saved/restored in `finally`); plus `buildCorrectionBlock`/`runWithFeedbackRetry` unit tests (exact block shape, attempt numbering, exhaustion result). Phase 16 v1.0.5 (JSON corrector, user-ratified 2026-08-23): gate 12.1 UPDATED to the corrector-aware flow (the spy now sees extractor → `json-corrector` diagnosis → extractor attempt 2; the corrector prompt carries the parse error + bounded evidence; the attempt-2 block carries the `Diagnosed cause and fix:` section) and NEW gates 12.9 (corrector-side failure degrades to the plain block; truncation via the `onResponseMeta` finish-reason tap gets the truncation NOTE + tail-only echo; schema failures never fire the corrector; helper units — async validator awaited, enhancer lands in the block, throwing enhancer swallowed, bounded evidence/echo helpers) + 12.10 (curation fires the corrector on a parse failure with the exact contexts; the `callLLMFn` seam bypasses it) + 12.11 (the cross-wiki JSON call equivalents). All LLM-free ($0): `vi.mock('undici')` transport or injected stubs
* `phase-13.test.ts` — Phase 13 gates 13.1–13.7 (output caps & prompt self-sizing, vision `04` §6 + `02` §4.8; 11 tests, all LLM-free, $0): 13.1 static source assertions (no `8192` literal remains in `agents/synthesis.ts`, no `2048` in `dox-writer.ts`, the Extractor's 16384 untouched) plus a behavior test (a `callLLM` spy sees all four synthesis call types at `maxTokens: 32768` and the DOX folder/root calls at 8192 — note the DOX folder calls only exist when the wiki has content, so the fixture materializes one chunk first); 13.2/13.3 prompt assertions (word-count rule absent from all four synthesis prompts; the ratified self-sizing block verbatim with a byte-identical first paragraph; the ⓣ topic variant's exactly-four adjustments; prompt heads unchanged; DOX prompt's catalog-line rule seated between the Pages and Statistics requirements); 13.4 sparse materialization (jane-doe 1 mention → `sparse: true` emitted after `aliases` before `sources`; john-smith 2 mentions + 1 claim → the boundary NOT-sparse case; acme-corp 3 mentions → not sparse; update-mode: the flag appears and disappears as mentions accumulate across runs) plus `isSparseEntity`/`enforceSparseInMarkdown` unit boundaries; 13.5 ingest re-imposition at both entity write points (strict: stub omits `sparse` for the sparse entity and emits it for the non-sparse one — the written page adds/removes it; permissive: same assertions on the permissive-written pages); 13.6 Settings screen DOX Writer recommendation labels (Anthropic Sonnet mid-tier and OpenAI GPT-5.6 Terra mid-tier rendered, old "strong contract writing" labels gone, other rows intact); 13.7 is encoded as the aggregate gate — the full key-less suite stays green with zero regressions (see this file's Verification section). Hermetic temp workspaces, injected `extractChunkFn`/`synthesize*Fn`/`writeDoxIndexFn` stubs, `vi.spyOn(llmClient, 'callLLM')`, and the ink fake-TTY harness
* `phase-14.test.ts` — Phase 14 gates 14.1–14.14 (topic & entity curation, phase doc §3; 37 tests, all LLM-free, $0): 14.1 decision-list validation rule classes (valid lists pass; unknown slug in each of the five buckets; two-bucket; missing-from-every-bucket; into-dropped; self-merge; entity merge-only rejects a non-empty "drop"; malformed/fenced JSON rejected); 14.2 merge-chain collapse (A→B, B→C resolves to a single `{from:[a,b],into:c}` entry; a pure cycle is rejected; a chain passes on the FIRST attempt through `curateTopics`); 14.3 reask + fallback trilogy (correction prompt is `prompt1 + '\n\n=== CORRECTION REQUIRED ==='` containing the offending slug; log contexts `curation-topics` → `curation-topics#attempt2`; validation exhaustion → decisions null + cause `validation-exhaustion`; `vi.mock('undici')` HTTP 400 → 1 transport call + cause `http-4xx`; mocked 429 → 6 transport calls (Phase 16 v1.0.2, user directive 2026-08-20: a 429 gets `RATE_LIMIT_MAX_ATTEMPTS` total attempts with the escalating stall; the instant sleeper keeps it wall-clock free) + cause `transport-exhaustion`; materialize-level fallback warns, records the report entry, and still writes every page; ingest-level fallback accumulates `metrics.curationFallbacks === 1`); 14.4 byte-identity regression guard — curation OFF vs curation ON-with-fallback in one workspace produce identical `updated:`-stripped content trees and byte-identical rolling memory; 14.5 topic merge (claims unioned + deduped by identical claim TEXT, title from `into`, from-slug pages never written, a dropped topic's claims survive on the entity and document pages); 14.6 entity merge (Odense fork pattern: one survivor, mention/claim/timeline union, relationship subject/object slugs rewritten incl. the self-loop, `mergedAliases` in pageData AND frontmatter, old pages absent); 14.7 `rewriteWikilinkTargets` unit tests (pipe/bare/prefix-collision/empty-map) plus an integration run (document-page link rewritten, frontmatter + citations untouched, `rewrittenLinks` recorded); 14.8 manual-edit veto (hash mismatch → merge skipped, `manualEditSkips` entry, conflicts.json entry with the curation reason, the edit survives); 14.9 update-mode deletions (merged-away/dropped pages removed, empty folder chains pruned but non-empty siblings kept, rolling memory reflects the curated set, the report records deletions); 14.10 the additive `curation` model slot (resolveModel mapping/null/override/legacy, setModelRouting normalization, `seedModelsForProvider` for all three providers, TUI frame carries 'Curation Model' + all three recommendation labels); 14.11 `curation-overrides.json` (created empty when absent, malformed/wrong-shape ignored with a warning, a `neverMerge` pair vetoes the merge and is forced into keep, `options.neverMerge` reaches both stubs); 14.12 two-round scaling (260 candidates → deterministic stem buckets [250, 10] → one reconciliation call over the survivors; a reconciliation transport failure falls back keep-all while round-1 merges stay applied); 14.13 prompt contract (both curation prompts carry the `=== LANGUAGE ===` block + `{agentsMd}`/`{candidates}` placeholders; en/en drops the block byte-identically, da/da carries the curation language rule; on-disk candidates join the input with `onDisk: true` on the second run; `.state/extracted/*.json` byte-identical across a merge run; rolling memory reflects the curated set); 14.14 is encoded as the aggregate gate — the full key-less suite stays green with zero regressions (see this file's Verification section). Hermetic temp workspaces; injected `curateTopicsFn`/`curateEntitiesFn` stubs (the `writeDoxIndexFn` precedent), `extractChunkFn` stubs, `vi.mock('undici')` transport tests, and the ink fake-TTY harness
* `phase-15.test.ts` — Phase 15 gates 15.1–15.7 (synthesis concurrency, phase doc §3; 7 tests, all LLM-free, $0): 15.1 a 20-entity/3-topic ingest under delay-stubs never exceeds 4 in-flight (a shared counter, never wall-clock assertions) while overlapping and completing every page; 15.2 synthesis-report entries stay in original page order under two different completion scrambles and are BYTE-IDENTICAL across runs (Date-only fake timers pin the timestamps; setTimeout stays real so delays still scramble); 15.3 the REAL `callLLM` against mocked undici with a FAKE key drives the production logPath append seam under the 4-worker load — llm-calls.json non-empty line count === the stub-call count (36) with every line JSON-parsing — and five concurrent preservation-failure conflict appends all survive (valid JSON, one entry per failed page); 15.4 per-page sequential-equivalence — entity-0 strict pass / entity-1 strict×3 → permissive attempt 2 / entity-2 full chain to template, exact attempt sequences + correction-block feedback carrying the page's own dropped-mention string, exact report entries in page order, `metrics.feedbackRepairs === 7`, per-page WARNING lines kept, one page's fallback never affects another's mode; 15.5 the aggregate `Synthesis: N/M pages complete (4 workers)` counter climbs 1..M per stage in completion order (entity stage before topic stage, no per-page spam, the pre-Phase-15 'Writing synthesis' line gone); 15.6 source-level proof (exactly two `await runPool(` in ingest.ts, none in materializer/dox-writer/curation/agents-updater/extract-chunk, `SYNTHESIS_POOL_SIZE` absent from the Settings files) plus a behavioral run whose instrumented `extractChunkFn`/`writeDoxIndexFn` in-flight counters stay at 1; plus `runPool` unit tests (input-order results under scrambled completion, reject-after-settle with unstarted items never starting). The Phase 14 keep-all curation stubs (`curateTopicsFn`/`curateEntitiesFn`) are injected at every ingest call so the curation stage never pollutes the LLM-free gates; 15.7 is encoded as the aggregate gate — the full key-less suite stays green with zero regressions (see this file's Verification section). Phase 16 semantic update (enumerated in `.state/phase-16-status.json`): all six ingest-level tests pass `poolStaggerMs: 0` — the 250ms production dispatch stagger would otherwise serialize pickups and break the overlap/scramble timing these pool-SEMANTICS gates pin
* `phase-16.test.ts` — Phase 16 gates 16.1–16.18 (run resilience, phase doc §3; 25 tests, all LLM-free, $0): 16.1 a 10-entity + 10-topic run with one exhausted-transport failure per stage (10% each — at, not over, the rate threshold) completes with both pages on the structured template, `finalMode: 'transport-fallback'` in the ordered report, the exact loud warnings, summary warning, and `metrics.transportFailures === 2`; 16.2 five consecutive transport-failed pages abort with the transport error (50 pages so the rate rule cannot fire) and two 4-streaks separated by successes complete (80 pages, 8/80 = 10%) proving the counter reset — per-index increasing delays make pool completion strictly index-ordered so the streaks are deterministic; 16.3 three spread failures of 20 (15%) abort while 1 of 20 (5%) completes; 16.4 a stubbed 404 aborts immediately — exactly one call for that page, zero fallbacks, no report append; 16.5 run 1 (8 pass + 2 quality-template, killed via a throwing updater stub) then run 2 skips the 8 (zero stub calls, `synthesisSkipped === 8`, skip progress line) and retries the 2 templates, and a direct extraction-JSON aggregate flip re-synthesizes exactly that one page; 16.6 materialize-level: a skip-eligible page is byte-preserved (in `preservedPages`, not `writtenPages`, still in `entityPages`) and a fingerprint-changed page is rewritten; 16.7 a 2-PDF run killed during PDF 2's extraction has PDF 1 checkpointed in `.state/ingestion.json` (exact completed-run record shape); the resume makes zero extraction calls for it, and the final ingestion.json + page tree byte-equal an uninterrupted run's (pinned Date); 16.8 staggered dispatch (injected clock shows pickups one 250ms stagger apart), the 600s `headersTimeout` present only at `maxTokens >= 32768`, and the exponential backoff asserted from `transportRetryDelayMs` plus an injected recording sleeper ([5000, 15000] — network-error mocks; 503 stalls under v1.0.3) — no wall-clock sleeping, plus an `isTransientTransportError` classification matrix; 16.9 both curation prompts carry no `keep` bucket instruction (and keep the 14.13 contract), validation derives the exact keep complement on slim outputs, legacy keep lists are accepted when consistent and rejected when contradictory; 16.10 a 200-candidate set with ~450-char slugs exceeds `CURATION_SIZE_TRIGGER_TOKENS` below the 250-count trigger and buckets (each bucket call independently validated with its own reask, reconciliation over survivors, per-round keep-all fallback intact); 16.11 kill-and-resume integration: a 2-PDF ingest killed mid-synthesis (transport fallback on page 9, fatal 404 on page 10) resumes with zero re-extraction, 8 skipped pages, 2 retried pages, and the final wiki tree + `.state` byte-equal to an uninterrupted run's (metrics.json excluded by design — a run-history audit; every reader-facing artifact compared); 16.12 is encoded as the aggregate gate — the full key-less suite stays green with zero unenumerated regressions (see this file's Verification section); 16.13 the 429/5xx stall floor (`transientStallDelayMs` = 1/5/15/45/90 min) + Retry-After semantics (a 7s header loses to the 60s floor; a 90s header wins); 16.14 five mocked 429s then a 200 with `maxRetries: 2` → exactly 6 total attempts with the escalating stalls recorded and the call resolves (the 429 budget exceeds the caller's bound); 16.15 a persistent 429 → exactly 6 attempts then the fail-loud `API error (HTTP 429) after 6 attempt(s)` (still classified transient); 16.16 5xx shares the ladder (503 ×2 then a 200 → 3 attempts with the exact [60000, 300000] stalls), network errors keep 3 attempts with the exact [5000, 15000] waits, and `maxRetries: 0` + 429/500 → 1 attempt, immediate throw (frozen default); 16.16b a persistent 500 → exactly 6 attempts then the fail-loud `API error (HTTP 500) after 6 attempt(s)` (still classified transient); 16.17 the `setStallWaitReporter` seam — the reporter receives the exact `{ waitSeconds, attempt, maxAttempts, statusCode }` per stall (429 and 500 both) and owns the stall message (console.warn suppressed; without it the warn fires with the `Rate limited (HTTP 429)` / `Provider error (HTTP 500)` lines) — plus the ingest-level leg: a stalled 429 during a real (mocked-transport) DOX run surfaces the line on the run's `onProgress` and the reporter never outlives the run (a post-run 429 warns on the console path); 16.18 (v1.0.4) every failed 429/5xx attempt persists to `.state/transport-stalls.jsonl` beside the call log — one record per attempt (timestamp, callType/context, provider, model, statusCode, attempt/maxAttempts, exhausted, waitSeconds, provider error message) with the exhausted final attempt included (waitSeconds 0), while `llm-calls.json` stays success-only so cost sums and the repair-rate denominator are untouched. 16.19–16.21 (Phase 16 v1.0.5 transport hardening + finish-reason tap, user-ratified 2026-08-23): every attempt carries an `AbortSignal` absolute deadline (large calls keep the 600s headers timeout; small calls pass no headers timeout; a `TimeoutError` throw is the bounded NETWORK class — 3 attempts with the exact [5000, 15000] waits and the transient-classified attempt-count error, never an unbounded hang); `Retry-After` is honored in both forms (delta-seconds and HTTP-date via `parseRetryAfterSeconds`, garbage ignored) but clamped to `RETRY_AFTER_MAX_SECONDS` (a 99999999s header waits exactly 5400s; a 120s-ahead date beats the 60s floor); `onResponseMeta` carries the provider stop reason (Anthropic `stop_reason` 'max_tokens', OpenAI `finish_reason` 'length', and a normal 'end_turn' leg). 16.22 (added 2026-08-27, caught LIVE by Phase 25's gate 25.9): the post-loop response-parse dispatch sends EVERY OpenAI-compatible built-in provider through `parseOpenAIResponse` — zhipu and deepseek had fallen into `parseAnthropicResponse`, so a 2xx Chat-Completions body parsed as EMPTY text with 0/0 tokens (the model's valid output silently discarded; every zhipu/deepseek `callLLM` in the main path returned ''). Hermetic temp workspaces, injected stubs, `vi.mock('undici')`, Date-only fake timers for byte-comparison legs, `poolStaggerMs: 0` at every ingest call
* `phase-17.test.ts` — Phase 17 gates 17.1–17.13 (entity graph and citation integrity, phase doc §3; 14 tests, all LLM-free, $0): 17.1 incoming mirror-attachment (two chunks with one `alpha — rel → beta` relationship each from distinct sources — beta carries both incoming records with verbatim evidence/page/source/pages, alpha carries both outgoing, an unknown object is skipped, a self-loop attaches once as outgoing only); 17.2 `writeEntityPage` renders the outgoing line unchanged plus incoming lines with the `(incoming)` marker, verbatim evidence, and correct `[^srcN]` keys, with the citation map and frontmatter `sources` covering the incoming records' sources; 17.3 the ratified sparse scope — an entity with ≤2 mentions, no claims, and ONLY incoming relationships keeps `sparse: true`, one outgoing relationship clears it; 17.4 `checkPreservation` names dropped incoming evidence in `droppedRelationships` and passes a complete page; 17.5 `relatedEntities` is exactly the deduplicated, sorted `{slug, title}` union (both directions + claim co-entities, minus the page itself; unknown-slug title fallback; `(none)` empty form); 17.6 both entity prompts carry the slot + updated wikilink rule with byte-confinement hash-pinned against the pre-Phase-17 templates (slot removed + old rule restored reproduces the recorded SHA-256), and the filled prompt (fixture data, `callLLM` spy) renders the slot with the en/en LANGUAGE-block removal intact; 17.7 island detection — a fixture wiki's `islands` lists exactly the zero-outgoing entity/topic pages, `orphaned` is unchanged, `index.md`/`sources/*`/`documents/*` exempt; 17.8 frontmatter re-imposition over model frontmatter (wrong `updated` → real write time under Date-only fake timers, partial `sources` → the full deterministic aggregation incl. incoming sources, fabricated field dropped, enforced block deep-equals the writer's own frontmatter, body byte-identical); 17.9 frontmatter creation when absent (the complete block prepended to a frontmatter-less page, `sparse: true` on the textbook-sparse `adhd-foreningen` analog); 17.10 `## Sources` normalization (full-workspace-path definitions rebuilt basename-form with deterministic ranges, in-prose markers byte-identical, `checkCitations` reports zero `missingSource`/`invalid` on the fixture); 17.11 the topic equivalents (frontmatter created when absent, never `sparse`, definitions normalized); 17.12 resume byte-stability — a 2-entity run with a relationship between them synthesizes both pages (enforcement exercised at the write points), the identical second run skips both (`synthesisSkipped === 2`, zero stub calls) and both pages are byte-identical; supplementary tests cover the §2.6 `missingFrontmatterSource` consistency check (entity/topic scope, basename matching, report-only) and the §2.1 `formatRelationships` direction labels (no-incoming output byte-identical to pre-Phase-17); 17.13 is encoded as the aggregate gate — the full key-less suite stays green with zero unenumerated regressions (see this file's Verification section). Hermetic temp workspaces, deterministic materialize/writer fixtures, Date-only fake timers for the `updated` pins, injected `extractChunkFn`/synthesis stubs + Phase 14 keep-all curation stubs with `poolStaggerMs: 0` at the ingest-level gates
* `phase-18.test.ts` — Phase 18 gates 18.1–18.5 (citation numbering alignment, phase doc §3; 6 tests, all LLM-free, $0): 18.1 the `citationMap` slot matches `buildCitationMap` exactly (keys, order, basenames, page ranges) for entity (mentions + outgoing + incoming + claims) and topic fixtures, cross-checked against the page writers' own `## Sources` lines; 18.2 all four synthesis prompts carry the `=== CITATION KEYS ===` section exactly once with byte-confinement hash-pinned (slot removal reproduces the Phase 17 templates); 18.3 an off-map `[^src9]` fails preservation with `extraMarkers` naming key + first line (entity and topic legs); 18.4 an extraMarkers-only failure engages the existing reask loop (attempt 2 carries the feedback, corrected page written, `metrics.feedbackRepairs === 1`); 18.5 is the aggregate full-suite gate (see Verification)
* `phase-19.test.ts` — Phase 19 gates 19.1–19.5 (stale-hash convergence, phase doc §3; 4 tests, all LLM-free, $0): 19.1 the kill-resume reproduction that CONVICTED the per-PDF checkpoint (historical pre-fix assertions recorded in `.state/phase-19-status.json`) — with the finally-guarded re-hash applied, the abort converges recorded == disk and the completed resume false-flags nothing, with the inverted stage-proof (recorded == final synthesized content, not the template render); 19.2 the post-fix end-to-end on an independent fixture (abort leaves recorded == disk, zero manual-edit lines, `assertHashInvariant`); 19.3 safe convergence — stale recorded hash over a provably-tool-written page (disk == current render) converges without a conflict entry and IS updated with the disk hash folded; a true human edit still conflicts; 19.4 the hash-consistency invariant across a two-PDF ingest with real curation merges; 19.5 is the aggregate full-suite gate
* `phase-20.test.ts` — Phase 20 gates 20.1–20.6 (wikilink repair + remediation, phase doc §3; 15 tests: 5 gates + 10 supplementary, all LLM-free, $0): 20.1 unique-prefix repair (`[[indikator-2]]` → `[[indikator-2-ekkokardiografi]]`, display untouched, checker-resolvable); 20.2 ambiguity stays broken with sorted candidates, zero matches unchanged; 20.3 alias-based repair with the language-aware alias rule (da slugifies the `… måling` alias to the real slug, en leaves it unrepairable); 20.4 remediation re-converges `pageHashes` for every modified page; 20.5 the `--dry` run is proven byte-silent by a recursive sha256 tree snapshot; 20.6 is the aggregate full-suite gate
* `phase-21.test.ts` — Phase 21 gates 21.1–21.9 (curation overhaul — deterministic pre-merge, confirm-deny, sticky decisions, phase doc §2.1–§2.3; 27 tests, all LLM-free, $0; fixtures drawn from the REAL 2026-07-28/29 rkkp-afdk/rkkp-akdb merge lists): 21.1 each signal family fires on its fixture pair (Godstrup transliteration fork da-auto/en-typo-propose, landspatientregistret+sundhedsvaesenet edit-distance, lpr alias, `Landspatientregisteret (LPR)` corpus abbreviation, peter-olsen-svenningsen subsequence, moeller-m-h initials, all 5 region name-forms ×3, indikator-2-ct-skanning indicator family, echocardiography/ekkokardiografi glossary) and produces ZERO false positives on 14 colocated-but-distinct controls (room-vs-clinic, numbered forks, distinct indicators, shared surname, lettered series, distinct modalities, city-vs-hospital); 21.2 the auto-apply tier is EXACTLY transliteration + alias (mixed 15-candidate fixture), neverMerge suppresses auto into `vetoed`, and two materialize-level legs prove lpr→landspatientregisteret (alias) and the Godstrup fork (transliteration, da) merge with NO LLM judgment and are recorded with their signals; 21.3 the confirm-deny path (prompt carries the pairs section with signal+evidence and strips it when empty; validation accepts confirm/deny/unjudged and rejects unproposed/duplicate/bucket-listed judgments; a stubbed confirm applies byte-identically to a model merge — unions, aliases, relationship repoints, wikilink rewrites — with the decision recorded under the pair's signal; a stubbed deny leaves both pages and records the denial); 21.4 run 2's input contains NO decided pairs (the survivor is pre-merged deterministically, the report distinguishes fromSticky vs decidedThisRun); 21.5 a sticky drop never re-enters the candidates and its claims stay on the entity page; 21.6 a keep-all pass cannot recreate a stuck merged-away page; 21.7 the `splits` escape hatch un-applies a recorded merge (both pages rebuilt, reversal logged, pair returns to OPEN candidates with the alias auto-tier vetoed, split consumed on re-decision); 21.8 neverMerge wins over auto-apply, confirm (validation veto), and sticky (un-applied with a logged reversal); plus supplementary decisions-file (create-empty/dedupe/malformed-backup) and signal-aware-bucketing (pairs never split, judged in the reconciliation call) tests; 21.9 is the aggregate full-suite gate (see Verification). Hermetic temp workspaces; injected `callLLMFn`/`curateTopicsFn`/`curateEntitiesFn` stubs. Phase 22 amendment note: the region/indicator formulaic families' assertions were UPDATED (enumerated in `.state/phase-22-status.json`) — region name-forms now assert ONE class-5 cluster family per core and the indicator number↔concept leg a class-3 cluster (the strict-identity indicator legs stay merge pairs)
* `phase-22.test.ts` — Phase 22 gates 22.1–22.10 (composite pages — Option C; phase doc §2.1–§2.4; the five-class rollup amendment, vision `02` §4.6 / `05` §6 amended 2026-07-29 user-ratified; backlog B22; 23 tests, all LLM-free, $0): 22.1 cluster validation — accepts a class-3 indicator+concept pair and a class-5 same-stem pair; rejects out-of-class (two-concept class 3, 3-member class 3, different-stem class 5, class 6, into-not-a-member, unknown slug), over/under-cap, double membership, merge/pair overlap, and neverMerge-vetoed clusters with exact error lists; confirm/deny/unjudged/duplicate/mismatched-copy proposal judgments; the over-cap error re-asks with the exact list in the correction block; detection reroutes region families to ONE class-5 cluster (cap >4 proposes nothing), indicator case-1 to class-3 (case-2 stays a pair), neverMerge vetoes a family whole; 22.2 assembly — the composite carries the unioned member-tagged evidence at the `into` path, member pages absent, the third entity's member reference remapped, the document page's member wikilinks rewritten (bare form gains the member title), aliases union, decision recorded with class + signal, checkLinks broken-free, schema clean; 22.3 the deterministic shell — members block (name/type/role/significance), `### <Member>` evidence groups, basename Sources, complete frontmatter (type composite, class, members, aliases, real updated, aggregated sources, member-type tags, NEVER sparse), intra-cluster relationship renders once as plain text; 22.4 synthesis values carry both members' slots + external-only relatedEntities + the union citationMap (callLLM spy); the two composite prompts are slot-additive against the Phase 18 baselines (Phase 7/17/18 slots + byte-equal wikilink/citation rules); 22.5 preservation fails per dropped member item (mention/claim/incoming evidence/off-map marker), passes on the shell; 22.6 sticky re-application rebuilds the composite with ZERO member pages and NO curation call; 22.7 a `splits` slug dissolves the cluster (entity pages rebuilt, composite removed, reversal logged, family re-proposed but not applied, split unconsumed); 22.8 the fingerprint ignores slugToTitle and flips on member-set/evidence changes, and an unrelated page change preserves the composite byte-for-byte under the resume contract; 22.10 the ingest synthesis stage is wired for composites (hermetic golden-master ingest with a stubbed cluster + stubbed composite synthesis: strict pass replaces the shell with the rich article — enforcers composed, report `pageType: 'composite'` finalMode `strict-synthesis`; strict preservation failure re-asks 3× with the exact dropped claim then the permissive pass wins (`permissive-synthesis`); double failure keeps the deterministic shell (`structured-template` + conflicts.json composite entry); run 2 with unchanged data makes ZERO composite/entity/topic LLM calls with the page byte-identical and `synthesisCompositesSkipped === 1`); supplementary: schema members/class rules, the DOX catalog line's composite member names, the cluster record round-trip with split consumption on `into`; 22.9 is the aggregate full-suite gate (see Verification). Hermetic temp workspaces; injected `callLLMFn`/`curateTopicsFn`/`curateEntitiesFn`/`extractChunkFn`/`synthesize*Fn` stubs, a `callLLM` spy
* `e2e.test.ts` — full-pipeline end-to-end test gated behind `RUN_E2E === '1'` (skipped by default so `npm test` stays LLM-free and $0): init, two golden masters, a full LLM ingest (`synthesis: true`, `doxLlm: true`), then page/link/citation/metrics assertions; 600s timeout; run deliberately with `RUN_E2E=1` and a key loaded
* `phase-23.test.ts` — Phase 23 gates 23.1–23.7 + 23.6b (comparison-table articles, backlog B21; phase doc §2.1–§2.4; vision `05` §9 + `03` §3.1 extended; 31 tests, all LLM-free, $0; fixtures mirror the observed RKKP indicator-table shape incl. the 2023→2024 indicator-renumbering DRIFT pattern): 23.1 the extractor schema accepts a well-formed `tables` array and rejects missing title/page/empty markdown with exact issues, off-range pages rejected, unknown entity slugs warn but pass, an absent array stays valid (pre-Phase-23 JSON), slug normalization covers subject/entities, and the agent fills a model-omitted array with `[]` (mocked `callLLM`); 23.2 assembly — the same table title from two sources accumulates as two dated sections on ONE page (in-chunk duplicates deduped), the DRIFT fixture (same subject, different title + columns) still lands on ONE page via the canonical subject slug with drift captions unioned into aliases and columns never force-merged, a renamed subject reconciles through a stubbed curation merge, an unresolvable subject falls back to the normalized-title slug, and empty/absent tables arrays produce no page and no folder; 23.3 the deterministic shell preserves the table markdown byte-for-byte inside its section, frontmatter complete (type comparison, title, real `updated` pinned by fake timers, aggregated sources, aliases, NEVER sparse), per-table entity links (known pipe-form, own subject plain, unknown plain), and `## Sources` basename-form cross-checked against `buildComparisonCitationMap`; 23.4 the comparison prompt is slot-additive (Phase 7/17/18 slots with the two rule sentences byte-equal to the entity baselines) and GENERIC (no corpus/registry-specific vocabulary), `buildComparisonRelatedEntities` is the sorted union minus the subject, and the filled prompt carries the dated sections/relatedEntities/citationMap with en/en LANGUAGE-block removal (`callLLM` spy); 23.5 ROW-VALUE preservation — `comparisonRowValues` parsing, the shell passes, a dropped row fails naming the row, an altered number fails naming the exact original value, a reformatted-but-value-complete section passes, dropped-citation and off-map-marker legs (entity/composite parity); 23.6b the bridge lists exactly the claims sharing the table entities (entity-less claims excluded) with live topic/entity links, is empty-form honest, and the enforcer re-imposes the section over model vandalism (missing → inserted before `## Sources`; fabricated → replaced); 23.6 a full materialize + deterministic DOX + `validateWiki` chain — zero broken links, zero schema invalid, the comparison page neither orphaned nor island, `comparisons/index.md` catalogs the page by its on-disk title (the JSON-quoting house style, phase-22 precedent), the wiki root's children/frontmatter include `comparisons/index.md` with the `Comparison pages: 1` statistic — plus an island-scope unit leg (zero-outgoing comparison page flagged, linked one not); the ingest-level stage legs (gate 23.4/23.5 stage) mirror the 22.10 composite stage — strict pass replaces the shell (enforcers composed incl. the rebuilt bridge, report `pageType: 'comparison'` finalMode `strict-synthesis`), a row-value defect re-asks 3× with the exact `Dropped table row value … value "82,2"` feedback then the permissive pass wins, double failure keeps the deterministic shell (conflicts.json comparison entry), and run 2 with unchanged data makes ZERO comparison/entity/topic LLM calls with the page byte-identical and `synthesisComparisonsSkipped === 1`; 23.7 is the aggregate full-suite gate (see Verification). Hermetic temp workspaces; injected `extractChunkFn`/`curate*Fn`/`synthesize*Fn` stubs, a `callLLM` spy, golden-master copies under two source names
* `phase-24.test.ts` — Phase 24 gates 24.1–24.15 (cross-wiki discovery layer; phase doc §2/§3; vision `03` §3.1/§4.1/§4.2/§6, `04` §3.2 Step 10, `05` §9.1, `07` §2.5/§2.6 — all amended 2026-08-09; 17 tests, all LLM-free, $0; synthetic two/three-wiki fixture workspaces with deterministic entity/topic pages): 24.1 exact-match resolver (identical title + alias-exact clusters, single-wiki excluded, fuzzy tier never fires, JSON mirror matches the markdown table row-for-row); 24.2 relationship graph (registry-subject and cross-wiki-span edges included, intra-wiki excluded, JSON mirror matches); 24.3 topic clusterer via injected stub (multi-wiki + cross-language cluster kept, single-wiki cluster excluded, cluster-page frontmatter `cross-wiki-topic` + path-qualified `members` with `.md`, no `wiki`/`sources`); 24.4 the cluster prompt contract (`{languageDirective}` block + `{topics}` slot + no-factual-claims rule + generic wording + en/en byte-identical block removal); 24.5 the cross-wiki DOX pass (both indexes with deterministic children/statistics, the workspace `## Cross-Wiki Discovery` section, byte-for-byte preservation across a later workspace re-composition, cross-wiki never a wiki catalog line, section omitted when artifacts are gone); 24.6 schema validation (both validators accept the new types, reject missing `clusterId`/`members`/`children`, a `wiki` field, unknown types); 24.7 path-qualified wikilink resolution (bogus target reported broken); 24.8 the resume fingerprint (unchanged → skip with zero stub calls and an untouched fingerprint file; new wiki → membership-changed re-run; edited content page → probe-relevant re-run); 24.9 uncertain isolation (proposed-cross-wiki-matches.json + entity-match-candidates.json carry the group, `entities.md`/registry stay empty); 24.10 the context summarizer (entity-summaries.json keyed by path-qualified id with title/type/sources; summary flows into the registry JSON); 24.11 predicate normalization (grouping + predicate-map.json + the graph's canonical predicates + the missing-predicate reask leg); 24.12 uncertain-review escalation via a `callLLM` spy on the REAL prompt path (cheap `cross-wiki-entity-match` call first, mid-tier `cross-wiki-uncertain-review` second, summaries + provenance in the review prompt, match verdict joins the registry, `resolveModelFromRouting` maps the two mid-tier call types to the synthesis slot and the five cheap ones to default); 24.13 hypothesis signals (grounding validation rejects out-of-subgraph wikis with reask, structured signals in proposed-signals.json, never wiki pages) + 24.13b (single-wiki subgraphs → no batch, no LLM call, empty signals file); 24.15 preflight (fewer-than-two-wikis skip; probe-not-relevant skip updates the fingerprint so the next run skips unchanged; multi-wiki changes never probe); the ingest-level integration leg (pass wired after the workspace index and before the updater, a throwing pass never aborts the ingest, disabled by default); plus a `listWorkspaceWikis` exclusion supplementary. Gate 24.14 is the aggregate full-suite gate (see Verification). Hermetic temp workspaces; injected component stubs, a `callLLM` spy, the golden-master copy for the ingest leg
* `phase-26.test.ts` — Phase 26 gates 26.1–26.11 (per-PDF sequential ingestion with patch amendment — Option B Patch; phase doc §2.1–§2.5; canon vision `04` §1 + §3.2 Step 9/§4 + `07` §3, user-ratified 2026-08-26; 18 tests: 17 LLM-free via injected stubs — `amendmentFn` (the `synthesizeEntityFn` precedent), extraction/curation/synthesis/dox seams — $0, + the ONE live gate): 26.1 the loop order (3 PDFs process strictly sequentially — extraction file order never interleaved, each PDF's synthesis/amendment completes before the next PDF's extraction, the curation pair exactly 3×, DOX events all after the loop via `doxLlm` + `writeDoxIndexFn` stubs) + the unit patch suite (add-evidence under an existing section, flag-contradiction without deleting the older claim, edit-prose unique-span, add-member coverage, unknown ops/invalid anchors rejected, fence-tolerant parse); 26.2 single-PDF equivalence (the frozen `tests/snapshots/phase-26-golden` replay — first-PDF path byte-identical); 26.3 the empty delta (unchanged evidence keeps the page skip-eligible, ZERO amendment calls); 26.4 the patch happy path (exactly PDF 2's items in the request, the merged page preserves old ∪ new verbatim, `patch-amended` report entry); 26.5 the reask loop (a parse failure then a preservation miss feeds the exact correction block — `=== CORRECTION REQUIRED ===` — then lands); 26.6 exhaustion → full synthesis (never half-patched, `strict-synthesis`); 26.7 composite `add-member` validation (composite-only, covered members only); 26.8 the curation-merge survivor veto (a survivor that absorbed a synthesized page takes FULL synthesis — zero amendment calls; sequenced keep-all-then-merge curation stub, remap-noise-free fixture); 26.9 abort/resume (abort mid-PDF-3 — PDFs 1-2 checkpointed, the PDF-2 patched page is skip-eligible on resume, only PDF 3 re-extracts, zero re-amendments) + the amendment-log gate (ONE JSONL episode, op-type counts, `patched` outcome); 26.10 metrics (`patchedPages`/`patchFallbacks` surface in the result and `.state/metrics.json`); 26.11 is the ONE LIVE gate — the real `writeAmendment` (no stub) against the synthesis slot pinned to `{ provider: 'zhipu', model: 'glm-5.3-flash' }` (self-skips without `ZAI_API_KEY`, the phase-02 `.env`-mirror loader), running the production validate → apply → merged-preservation loop (≤3 attempts, correction blocks fed back) and asserting the merged page preserves old ∪ new plus the llm-calls.json entry provider zhipu / model glm-5.3-flash / callType synthesis-amend; 480s timeout. Fixtures mirror the ratifying scenario (a 2023 report creates the page, a 2024 report adds one mention + one contradicting claim)
* `phase-25.test.ts` — Phase 25 gates 25.1–25.9 (generic-label disambiguation — Option E Variant B; phase doc §2.1–§2.4; canon `02` §4.6 class 6 + `05` §6 class 6 + §7 + `04` §3.2 Step 6b, all amended 2026-08-26; backlog B23; 14 tests: 13 LLM-free via the injected `disambiguateFn` seam + keep-all curation stubs, $0, + the ONE live gate): 25.1 the detector controls (the generic-label pattern families incl. `table-3`, token helpers excluding stopwords/numbers/label tokens; the two-source DPD/HOFTER divergent fixture IS flagged with exact per-source samples; single-source, same-register high-overlap, and non-generic (`rigshospitalet`) multi-source disjoint fixtures are NOT); 25.2 the prompt contract (the `{languageDirective}` block, `{slug}`/`{title}`/`{evidence}`/`{agentsMd}`/`{reEntryRules}` placeholders, member rules, the byte-identical RE-ENTRY strip; the filled prompt via `callLLMFn` carries the evidence + callType/maxTokens≥2048/context; the da/da curation directive; schema accepts a well-formed split + no-split and rejects the 1-member split, the member without sources, the doubly-mapped source, and the bare-renumber `indikator-3` — each naming the offending member; the reask correction block carries the member-naming errors with `#attempt2` contexts; exhaustion → 3 attempts + keep-one-page, thrown 4xx → 1 attempt + `http-4xx`; `resolveSlotFromRouting` maps 'disambiguate' to the curation slot with Default fallback; the re-entry validator fixes existing members' sources and rejects split:false); 25.3 the application (run 1 writes ONE class-6 composite at the generic slug with per-member evidence groups under the derived member titles, records the sticky sourceMap exactly; run 2 under Date-pinned fake timers makes ZERO judgment calls and reproduces the composite byte-identically); 25.4 the class-6 TOPIC composite (the unit shell — members block, per-member claim groups, complete topic frontmatter incl. the aliases union, never sparse; the materialize topic leg; the validator exception — class 6 legal under topics/, class 3 a validation error, class 7 out of range); 25.5 preservation + links (every per-member verbatim item present, every `[^srcN]` marker defined by the unioned map, `checkLinks`/`checkCitations`/`validateSchema` clean with golden-master copies in raw/, member pages absent, a `[[indikator-2|…]]` link from the hofter-registry page resolving to the composite); 25.6 the third source (a NEW meaning re-enters the judgment ONCE scoped to its placement — existing members + all sources in the request, evidence untouched, sourceMap + from grow, a 3-member composite; the same-meaning control routes deterministically with ZERO calls and the map still grows); 25.7 `splits` dissolves (the dissolved label is not re-proposed by the dissolving run, the ordinary entity page is rebuilt with BOTH meanings' evidence at the same path, the reversal logged in the summary and the report); 25.8 is the aggregate full-suite gate (see Verification); 25.9 is the ONE LIVE gate — the real `disambiguateLabel` (no stub) against the curation slot pinned to `{ provider: 'zhipu', model: 'glm-5.3-flash' }` (self-skips without `ZAI_API_KEY`, the phase-02 `.env`-mirror loader), asserting schema-valid JSON for BOTH the divergent and the same-register control evidence (either verdict is a PASS) and the llm-calls.json entry with provider zhipu/model glm-5.3-flash/callType disambiguate; 480s timeout. Fixtures mirror the ratifying evidence (DPD 'første behandlingskontakt senest 10 dage' vs HOFTER 'opereres senest 24 timer')

## Local Contracts

* Every test must trace to a numbered gate or requirement in a phase document; when a gate's literal test code cannot run on this platform, the restructured test must still verify the gate's pass criterion and the deviation must be recorded in `.state/phase-N-status.json`
* Tests must not make live LLM calls by default; live-call tests use `test.skipIf(!process.env.ANTHROPIC_API_KEY)` and are run deliberately with the key loaded
* Tests must not modify `test-pdfs/golden-master.pdf` (see `test-pdfs/AGENTS.md`)
* Ink 7 non-TTY harness conventions: capture frames via fake stdout and assert after `unmount()`; drive input via fake-TTY stdin (PassThrough with `isTTY`/`setRawMode` stubs); vitest workers do not support `process.chdir()` (ERR_WORKER_UNSUPPORTED_OPERATION), so hermetic flows render `App`/screens with their `workspace` props pointed at a temp directory instead; JSX only compiles in `.tsx` files — `.ts` gate files use `React.createElement`

## Work Guidance

* Platform is Windows/Git Bash: shellout assertions need fallbacks (e.g. `shasum` → `certutil` → `node:crypto`)

## Verification

* 2026-08-28 workspace-registry fix (user-reported bug): NEW `tests/tui/workspace-bootstrap.test.ts` (8 passed) + NEW `tests/tui/app-workspace.test.tsx` (4 passed) green, and the FULL key-less suite green — 577 passed + 16 skipped across 40 files (39 passed + 1 skipped — tests/e2e.test.ts), zero failures, zero unenumerated regressions (the enumerated touches: gate 11.12's `onReviewAgents` stub and the add-pdfs/ingest screen tests adapt to the `WikiRef` payload); `npx tsc --noEmit` clean (run via `dist/runtime-node.exe` — npm is not on this shell's PATH; `.env` stashed/restored byte-identical for the key-less run). Environment note: the merged checkout's `core.autocrlf=true` had smudged `prompts/*.txt`, `templates/AGENTS.md`, and `tests/snapshots/phase-26-golden/**` to CRLF, breaking three byte-asserting gates (25.2, 26.2, and one CRLF-sensitive prompt check) — the working copies were re-normalized to the committed LF and a scoped `.gitattributes` (`prompts/*.txt text eol=lf`, `templates/AGENTS.md text eol=lf`, `tests/snapshots/** text eol=lf`) pins them so future checkouts stay LF.
* 2026-08-27 Phase 26 implementation: `tests/phase-26.test.ts` green (17 passed + 1 skipped — the live gate 26.11 self-skipped key-less) and the FULL key-less suite green: 565 passed + 16 skipped across 38 files (37 passed + 1 skipped — tests/e2e.test.ts), zero failures, zero unenumerated regressions (the enumerated Phase 16/19/23/11 adaptations recorded in `.state/phase-26-status.json` deviations: 16.5/16.7/16.11 per-PDF atomicity + amendment path, 19.4 source-keyed amendment, 23.4/23.5 patch-amended resume + per-pass template retries, 11.5 README headings; the temporary `phase-26-golden-capture.test.ts` harness — which re-froze the golden snapshot on every run — was deleted after the capture, and the frozen `tests/snapshots/phase-26-golden` replays byte-identically in gate 26.2); `npx tsc --noEmit` clean; LIVE gate 26.11 green against the real glm-5.3-flash (1 call, happy path attempt 1, $0.003967) and LIVE UAT 26.1 green (2 PDFs, 5 patched pages incl. a flag-contradiction, final validation clean, $0.2288 — the amendment-vs-creation token finding recorded as a watch item in `.state/phase-26-status.json`). ZAI key from dist/.paper-chase.json injected via env; never printed.
* 2026-08-27 Phase 25 live-gate fix run (main agent; the Implementer sub-agent hit a 5-hour usage limit mid-fix): LIVE gate 25.9 run against the real glm-5.3-flash (ZAI key from dist/.paper-chase.json injected via env; never printed) — first run FAILED validation-exhaustion, exposing TWO defects: (1) the validator required exact full-path source echoes while the model naturally echoes basenames — fixed by deterministic basename normalization in `validateDisambiguationVerdict` (canonical full-path form retained in the normalized verdict/sticky sourceMap; ambiguous basenames not normalized; regression legs in gate 25.2); (2) `src/llm/client.ts`'s post-loop response-parse dispatch sent only openai/qwen through `parseOpenAIResponse`, so zhipu/deepseek 2xx bodies parsed as EMPTY text with 0/0 tokens — a latent bug in HEAD predating Phase 25 (present in 380e0ca), fixed by the four-provider condition + NEW gate 16.22. After both fixes: gate 25.9 GREEN (both invocations schema-valid, llm-calls.json entry provider zhipu/model glm-5.3-flash/callType disambiguate), gate 16.22 green, `npx tsc --noEmit` clean, FULL key-less suite 548 passed + 15 skipped across 37 files (36 passed + 1 skipped), zero failures (`.env` stashed/restored byte-identical, 171 bytes). Live pipeline spend ≈ $0.006 of the $6.00 cap (9 discarded-parse attempts across diagnosis runs + 2 successful gate calls + 4 direct probes).
* 2026-08-27 Phase 25 implementation: `tests/phase-25.test.ts` green (13 passed + 1 skipped — the live gate 25.9 self-skipped key-less) and the FULL key-less suite green: 546 passed + 15 skipped across 37 files (36 passed + 1 skipped file), zero failures, zero unenumerated regressions (the Phase 24 baseline of 493+14/34 plus the 13 new phase-25 tests plus pre-existing untracked `.state/upgrade/*.test.mts` debug scripts vitest discovers in this environment, all green); `npx tsc --noEmit` clean. Run with `.env` stashed to `.env.keyless-tmp` and restored byte-identical (the 2026-08-22 acceptance-run procedure; node invoked by absolute path since npm is not on this shell's PATH).
* 2026-08-26 provider restoration: DeepSeek and Zhipu/GLM built-in providers were restored from `feat/multi-provider-and-429-stall` and `Qwen 3.8 Flash` + `GLM-5.3-Flash` were added as selectable Sonnet-tier models. The previous removal note is superseded. Targeted re-verification after the restoration: `tests/phase-11.test.ts` passed (54/54) and `tests/phase-14.test.ts` passed (38/38) in the prior session; a fresh full `npm test` and `npx tsc --noEmit` could not be run in this environment because Node.js is not installed on the PATH.
* 2026-07-25 navigation-fix delta (post-Phase-16, compliance-log [2026-07-25 18:05]): `phase-06.test.ts` grows to 32 tests (new gates 6.17a–6.17d), and the stub bodies in `phase-07.test.ts` (7.12), `phase-12.test.ts` (12.7), `phase-13.test.ts` (13.1 behavior), and `phase-15.test.ts` (15.6) became context-aware complete catalogs to satisfy the new `missingCatalogLinks` enforcement — no assertion intents changed (12.7's exact repair counts preserved; 13.3's byte-gate untouched). Key-less: 336 passed + 14 self-skipped across 22 files; `npx tsc --noEmit` clean. NOTE: with `ANTHROPIC_API_KEY` set but the committed `wikis/test-wiki` fixture not materialized, the 12 live Phase 2 gates FAIL with ENOENT instead of self-skipping (pre-existing environment condition, verified identical on the unmodified baseline), and phase-16 gate 16.2 is timing-flaky under load (passes on re-run; untouched code path)

* 2026-08-30 Phase 16 v1.0.6 network/timeout stall ladder (user-ratified directive, born from the 2026-08-30 21:14 rkkp abort): re-cast phase-16 gates 16.8 (large-output headers timeout 600s → 900s; network-leg waits now [600000, 1200000]), 16.16 (network/timeout errors share the 6-attempt stall ladder; `maxRetries: 0` + network error → 1 attempt, immediate throw), 16.19 (timeout leg 6 attempts with waits [600000, 1200000, 1800000, 3600000, 5400000]); NEW gate 16.22 (`networkStallDelayMs` 10/20/30/60/90-min sequence, persistent network error → exactly 6 attempts then fail-loud, `transport-stalls.jsonl` statusCode 0 records with the thrown message, reporter `StallWaitInfo` with statusCode 0, ingest-screen progress line `Connection problem (network/timeout) — waiting 600s before retry (attempt 2/6)...`); NEW `tests/tui/ingest-screen.test.tsx` network-stall progress-line test (whitespace-normalized for 80-column wrap); pre-existing `tests/tui/app-workspace.test.tsx(148,7)` TS2769 generic-promise fix. FULL key-less suite green — 580 passed + 16 skipped across 40 files (39 passed + 1 skipped file — tests/e2e.test.ts), zero failures, zero unenumerated regressions; `npx tsc --noEmit` clean (run via `dist/runtime-node.exe` — npm is not on this shell's PATH); `.env` stashed/restored byte-identical (171 bytes). Vision `04` §6 and `07` §5 amended to state both ladders; root + src AGENTS.md updated.

## Child DOX Index

* `tui/` — TUI component tests (covered by this file's contracts; no separate child doc yet)

* 2026-09-02 Phase 27 per-PDF worker-process isolation (user-ratified canon amendment, vision `04` §1; born from the 2026-09-02 rkkp silent-death post-mortem): NEW `tests/phase-27.test.ts` — 12 tests / 11 gates: 27.1 engine-split equivalence (the true conductor shape — onlyPdfs run ×2 + finalizeOnly run — converges byte-for-byte to the single full run's durable tree, run-window state excluded; a selected run checkpoints ONLY its PDF), 27.2 protocol round-trip (three event shapes, noise-tolerant, chunk-safe reader + flush for mid-write deaths), 27.3 sequencing + merge (three spawns in strict order, lists concat/counters sum/finalize objects win) + 27.3b merge unit, 27.4 crash → same-PDF auto-retry → completion with `crash-log.jsonl` audit + visible retry line, 27.5 user-Skip deferral (nothing deleted, PDF unrecorded so the next run re-selects it, 4 crash records with the 4th `autoRetried: false`, panel surfaced, banner `1 deferred`), 27.6 tailLines bounding + record shape, 27.7 spawn resolver (dev tsx / staged bundle / loud missing-entry error / PAPER_CHASE_WORKER_CMD override), 27.8 AbortSignal kills the active worker and spawns nothing further, 27.9 cap = exactly 1+3 attempts with three 30 s backoff waits then the USER decision (never auto-defer), 27.10 worker args carry the ingest options verbatim (opt-ins and opt-outs), 27.11 doc gates (vision amendment text, root preference, phase doc, VERSION 1.0.30). NEW `tests/tui/ingest-screen.test.tsx` ×2: healthy conductor run renders the same lines/banner with NO crash panel; the crash panel renders (title/exit code/stderr tail/key legend) and `r` resolves the pending decision. ADAPTED `tests/tui/phase-01-screens.test.tsx` 'ingest screen runs ingestion': the REAL ingest rides the screen's `ingestFn` seam (the screen's production default is now the subprocess conductor). Crash gates use scripted `SpawnWorkerFn`s (deterministic spawn outcomes); the PAPER_CHASE_WORKER_FAULT env var is the live-UAT path. FULL key-less suite green — 594 passed + 16 skipped across 41 files (40 passed + 1 skipped — tests/e2e.test.ts), zero failures, zero unhandled errors; `npx tsc --noEmit` clean (via `dist/runtime-node.exe`); `.env` stashed/restored byte-identical.
