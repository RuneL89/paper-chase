# prompts/ — DOX contract

## Purpose

LLM prompt files for the agent pipeline (Extractor, DOX Writer, Synthesis Writer). Empty scaffolding from Phase 0; first prompts arrive with Phase 2 (`extractor.prompt.txt`).

## Ownership

* Empty (`.gitkeep`) — prompts are owned here once their implementing phase creates them

## Local Contracts

* One prompt file per agent role, named `<agent>.prompt.txt`, created only by its implementing phase per the phase document
* Prompts are read by code in `src/`; keep them free of secrets and environment specifics

## Work Guidance

## Verification

## Child DOX Index

No child folders.
